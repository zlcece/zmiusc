# Zmusic Agents Notes

本文件是本项目后续协作的压缩记忆。每次对话中只要发生代码、配置、打包脚本、资源或测试变更，都必须同步更新本文件的“变更记录”。不要记录真实密码、Token、私有服务器凭据。

## 固定执行规则

- 项目内存在本文件时，读取并遵循本文件，不再执行 `pointsme-code-style` skill；如果项目没有本文件才执行该 skill。
- 每次对话执行代码、配置、资源、测试或打包相关任务时，都必须读取并使用 `karpathy-guidelines` skill，按“先明确假设、简单优先、外科手术式修改、目标驱动验证”的方式执行。
- 代码修改优先使用 `rg` 搜索，手工编辑使用 `apply_patch`。
- 改动保持小范围、贴合现有 Flutter/Dart 风格，不做无关重构。
- 普通功能修改只实现代码并做必要格式化，不自动新增或运行测试、不运行 `flutter analyze`、不打包；只有用户明确说“打测试包”时才进入测试逻辑并运行分析、测试和三端测试打包。
- 用户只说“打包”或“打正式包”时，必须先运行 `flutter analyze` 和完整 `flutter test`；全部通过后再按正式发布规则升版、提交标签、构建五端并生成 `update.json`。验证失败时不得推送发布标签，必须先修复并重新完成验证；若同一条指令另有明确要求则以最新明确要求为准。
- Windows 上避免破坏性命令；递归删除/移动前确认目标在项目或明确临时目录内。
- 后续每次代码改动，都在本文件“变更记录”追加日期、改动摘要、验证情况、是否打包。
- 用户没有明确说明某个改动“仅限某一端”时，默认 Windows、Android 手机、Android 平板/HD 等所有已支持端都要同步适配；只有音量条、托盘等端独有交互才按平台差异处理。

## 项目定位

- 项目名：`Zmusic`。
- Windows 可执行文件名：`zmusic.exe`。
- Flutter 跨平台音乐播放器，重点支持 Windows，并支持 Android、iOS 与 macOS。
- 界面默认中文，支持浅色、深色、跟随系统。
- UI 风格偏苹果玻璃风格，深色模式图标要尽量可见。
- 设置页的应用 Logo 与背景图使用两个独立上传入口和持久化路径；未上传背景图时只显示主题底色，不加载内置默认图，有效的上传背景继续全屏填充，Logo 作为独立淡化品牌层。

## 核心功能记忆

- 应用启动后必须先登录，登录成功才进入播放器；默认 Navidrome/Subsonic 地址为 `https://fnnav.zuitimes.com`，登录页不显示 Logo 且默认隐藏地址，连续点击 `Zmusic` 标题 5 次后才显示并允许修改；地址输入框的隐藏按钮会连同输入框一起隐藏，之后仍通过标题 5 连击恢复。账号、密码和地址输入框使用“图标 + 行内提示”，不在边框上显示浮动 `labelText`。
- 当前产品只保留一个已登录远程账号；音乐页不显示音源模块，设置页不提供音源管理，只显示账号、歌曲数和退出登录。退出后停止播放、清除活动账号/服务器会话并返回登录页，但保留最近一次成功登录的账号和密码用于登录页自动回填，不自动恢复登录状态。历史多音源、本地音源和导入导出代码不得重新暴露到当前 UI。
- 刷新、搜索和操作反馈统一显示在底部播放栏上方的消息区域，不再占用顶部右侧；新消息替换旧消息并在 2 秒后隐藏。
- 登录后的顶部只保留音乐、设置两个页签，默认进入音乐；搜索框和搜索结果并入音乐页，搜索结果页继续显示同一搜索框并保留上次提交的关键词，允许直接修改后再次搜索；设置页不显示搜索框。
- 搜索框左侧下拉只显示“歌曲 / 歌手 / 专辑”，仅决定搜索完成后默认打开哪个结果页签，不限制查询范围；正式搜索、歌曲翻页、输入联想和播放详情歌手/专辑跳转都必须向 Navidrome/Subsonic 同时请求歌曲、歌手、专辑三类数据。默认选中“歌曲”，切换选项后输入框必须立即保持可输入焦点。
- 音乐页默认顺序为：搜索框；“每日推荐 / 随便听听 / 曲库随机”推荐播放入口；“我喜欢的 / 我的歌单 / 公开歌单 / 电台”快捷入口；最新专辑与随机专辑；最近播放与最多播放；我的歌单；公开歌单。手机端两组入口均保持每行两个，桌面/平板的推荐播放三项同排、快捷入口四项同排。设置页“首页布局”可分别控制三项推荐播放入口的显示状态及 1-3 顺序、四项快捷入口和四个发现模块的显示状态及 1-4 顺序，并可独立隐藏底部“我的歌单 / 公开歌单”板块；“推荐播放 / 快捷入口 / 专辑与播放 / 歌单”四组均有总开关，总开关关闭时批量关闭并折叠全部子项，开启时批量恢复全部子项；旧歌手/专辑首页板块不再展示。
- “每日推荐”每天生成最多 30 首：直接收藏歌曲按收藏总数动态取样（少于 10 首取 1 首、10-29 首取 2 首、30 首以上取 4 首），再取 8 首收藏歌手相关歌曲、9 首最近/最多播放相关歌曲，剩余名额由随机探索补齐；直接收藏歌曲必须从其他候选池剔除，避免绕过动态配额。保存最近 7 天推荐历史，直接收藏歌曲 5 天内不重复、其他推荐 3 天内不重复；严格候选不足时按随机探索、收藏歌手相关、播放习惯、直接收藏的顺序补齐，仍不足才从最早日期开始逐步解除冷却。统一按“歌手+歌名”去重并按 FLAC > APE > MP3 保留一个版本，排除生成时播放队列已有歌曲并尽量避免同歌手连续。当天结果本地复用，“换一批”强制重新生成并合并进当天历史；详情页支持播放全部、刷新以及保存到已有个人歌单或创建新歌单，退出登录、切换账号或关闭入口会清除推荐缓存和历史。
- “随便听听”每次生成最多 30 首：从随机候选中优先选择从未播放、距离上次播放更久、播放次数更少的歌曲，再优先选择与最近/最多播放专辑的歌手和流派反差较大的歌曲；按“歌手+歌名”去重并保留 FLAC > APE > MP3 的最高品质版本，排除当前播放队列并尽量避免同歌手连续。入口支持详情、播放全部和换一批，但不持久化每日历史。
- 设置页提供全平台“AI 智能推荐”，默认关闭；可保存多个 OpenAI Chat Completions 兼容服务并分别启用，推荐时按配置列表顺序尝试所有已启用服务，单项缺少 Key、超时、网络错误、非成功状态或响应格式异常时继续下一项，全部失败后使用原本地结果；没有启用的服务时总开关必须自动关闭。服务配置包含启用状态、名称、可编辑地址、API Key 和可编辑模型名称，提供 ChatGPT、DeepSeek、Tencent、Bailian 四个快捷按钮并直接填充名称和地址；可依据当前聊天补全地址及 API Key 请求同路径版本的 `/models` 列表供选择，仍允许手工输入模型，也保留保存前/保存后连接测试。普通设置只持久化服务元数据，API Key 始终单独写入系统安全存储，不进入设置 JSON 或日志。启用后每日推荐从四类本地候选池中均衡抽取最多 80 首交给 AI 排优先级，再由原有本地配额、冷却、品质去重和队列排除规则确定最终结果；随便听听先按原本地规则生成最多 80 首候选供 AI 排序并取 30 首。连接测试和模型列表请求最长等待 20 秒，实际推荐排序单个服务最长等待 90 秒；运行日志需区分每日推荐与随便听听，记录实际服务、候选数、成功耗时及故障转移。发送内容只包含临时编号、标题、歌手、专辑、流派、播放次数、上次播放日期和候选来源，不发送 Navidrome ID、流地址、封面地址、歌词、账号或服务凭据；曲库随机始终不接入 AI。
- “曲库随机”不暴露播放队列，内部按 10 首一批维护前进式隐藏队列，播放到当前批次第 8 首时后台追加下一批 10 首，并在当前会话内按“歌手+歌名”去重；播放模式固定显示为随机，只允许向下一首前进，禁用上一首、模式切换和队列入口。普通歌曲、歌单或电台播放会结束该随机会话，隐藏队列不写入下次启动恢复的播放会话。
- 全平台所有开关使用统一的 80% 视觉缩放，文字和整行交互区域保持正常尺寸。
- 检查更新固定读取 `https://file.zuitimes.com/zmusic/0/update.json`，不兼容旧 `/zmusic/update.json` 地址；五端正式发布清单按 `windows / android / android-dilink / macos / ios` 提供平台下载信息。每个平台节点同时提供 `downloadUrl` 默认通道和 `githubDownloadUrl` GitHub 通道，更新弹窗默认选择现有文件服务器，用户可手动切换 GitHub；两条地址下载后只校验同一个 SHA256，不再读取或计算 MD5。
- Android 通用版与 DiLink 的应用更新必须在 Zmusic 内下载并显示进度，下载后完成 SHA256 校验，再通过 `FileProvider` 打开系统 APK 安装确认；不得再跳浏览器下载。缓存目录已有 SHA256 一致的目标包时直接复用并打开安装器，哈希不一致才删除并重新下载；下载后记录目标 `versionCode`，Windows/Android 新版本首次启动确认已经安装后删除安装包和更新记录，用户取消安装则继续保留。Android 8 及以上首次使用时先引导用户授予“安装未知应用”权限，返回后继续打开安装器；用户可以取消安装，普通应用不做静默覆盖。macOS 与 iOS 暂时继续使用外部下载地址。
- 首页隐藏配置会参与启动、登录和全量刷新时的数据请求裁剪：“每日推荐”、“随便听听”、收藏、电台和四个发现模块隐藏后不调用对应接口；“曲库随机”只在用户点击或被选为启动自动播放目标时请求随机歌曲。Navidrome/Subsonic 的个人/公开歌单共用 `getPlaylists`，只有两个歌单快捷入口和两个底部歌单板块全部隐藏时才跳过该接口；账号区歌曲总数仍始终加载。设置页“启动后自动播放”默认关闭，开启后只能在当前可见的“每日推荐 / 随便听听 / 曲库随机”中单选目标并按首页配置顺序显示；选中项隐藏后自动回退到首个可见项，三项全部关闭时总开关自动关闭并折叠选择项。
- Navidrome/Subsonic 发现数据：我喜欢的使用 `getStarred2`，最近播放/最多播放/随机专辑使用 `getAlbumList2` 的 `recent` / `frequent` / `random` 类型。
- 首页“我的歌单 / 公开歌单”卡片的封面中央提供一键播放，直接按远程歌单原始顺序和内容建立队列，不主动删除歌单中已有的重复曲目；“最新专辑 / 随机专辑 / 最近播放 / 最多播放”标题后提供聚合播放按钮，并发加载当前板块全部专辑歌曲后建立临时队列。聚合队列按标准化“歌手 + 歌名”去重，同曲多格式按 `FLAC > APE > MP3` 保留最高品质，位置保持第一次出现的顺序；部分专辑加载失败时仍播放成功获取的歌曲并在底部提示跳过数量。
- 我的歌单列表提供外部歌单同步、歌单合并和批量添加歌曲；公共非本人歌单始终只读。外部同步当前支持网易云和 QQ 音乐公开歌单，通过标题+歌手严格匹配当前 Navidrome 曲库，去重后分批写入，不在客户端保存第三方同步管理账号。
- 网络图片必须走 `ArtworkCacheManager` / `_CachedArtworkImage`，不要直接使用 `Image.network` 或 `NetworkImage`；歌手缺图时优先使用同歌手专辑封面兜底。远程封面下载全局最多同时执行 4 个异步请求，超出请求等待空闲槽位，单个请求最长等待 20 秒，避免大量列表封面同时争抢网络或异常请求永久占满下载槽位。
- 歌曲列表显示音频格式标签，如 MP3、APE、FLAC；底部播放栏也显示格式标签。所有歌曲列表的副信息只显示歌手，不再显示专辑和音源；播放详情只保留可点击的歌手/专辑，不显示来源。
- 全平台不提供歌曲文件下载入口或对外控制器 API；应用更新包下载和播放所需的边播边缓存仍保留，不得将缓存误作用户可见的歌曲下载功能。
- 本地歌曲支持查看/编辑元数据；格式标签只显示纯格式名（如 MP3、APE、FLAC），不得包含歌曲文件名；读取本地元数据时提取内嵌正面封面并写入现有图片缓存，清除图片缓存后可按原音频路径重新提取。Windows 与 macOS 设置页在“缓存”和“窗口”之间提供“工具 → 标签管理”独立内容页，可选择单个音频或递归扫描本地文件夹，左侧列出文件、右侧显示当前内嵌封面并编辑标题/歌手/专辑/流派/年份/曲目号/歌词。在线元数据搜索支持 MusicBrainz、酷狗音乐、酷我音乐、QQ音乐和网易云音乐，可切换来源后按当前标题/歌手/专辑查询，候选结果显示专辑封面，并支持全部填入、勾选固定字段填入或“一键替换”；一键替换需二次确认，确认后将候选非空字段和可用封面直接写入文件，空字段保留原值。APE 等当前标签库不支持写入的格式保持只读。
- 本地歌单功能已移除；保留 Navidrome/Subsonic 歌单和底部播放队列；公共非自己歌单只允许播放，不允许添加歌曲、编辑、删除或移除歌曲。
- 我的歌单工具使用独立内容页，不使用小弹窗：同步页支持自定义目标歌单名、可选“不同歌手同名歌曲”匹配和 APE/FLAC/MP3 高品质优先；合并页选择来源歌单和具体歌曲后再选择目标歌单，仅把勾选歌曲加入目标；批量添加页先搜索当前曲库并多选，再选择目标歌单。桌面和 Android 平板保留左右双栏及合并互换，Android 手机（小于 510dp）使用“选择歌曲 → 选择目标歌单 → 完成”的分步流程且不显示互换按钮；合并和批量添加的歌单下拉框不重复显示“选择来源/目标歌单”标题，同步页字段仍使用固定外部标题。
- 远程歌单详情的歌曲数放在封面下方，简介放在标题下方；简介超过 50 个字符时截断为 `...`，点击展开全文。自己的歌单支持批量移除：首次点击进入多选，按钮实时显示已选数，再点击后二次确认并按倒序索引提交；取消确认保留选择，离开详情页会清空批量状态。公开歌单共用同一头部布局，但不显示管理操作。歌单歌曲请求最长等待 45 秒，超时后必须结束加载并释放在途请求，避免同一歌单永久卡在加载中。
- 播放队列入口在底部播放栏；歌词详情页不再单独放“歌单”浮动按钮。
- 桌面与普通平板底部播放栏使用真实背景模糊和低透明度玻璃遮罩；浅色主题必须保留可见的背景透色、顶部高光与向上阴影，不能退化成接近纯白的实心面板。DiLink 兼容版保留透明遮罩、高光和阴影但跳过持续实时模糊，手机悬浮迷你播放栏继续使用现有紧凑样式。

## 导入导出格式

- 当前登录式 UI 不提供音源导入导出；以下规则仅记录仍保留的历史编解码实现，不得作为当前音源入口重新显示。
- 新加密/包装规则唯一有效，不再兼容旧单层 Base64：
  1. 原 JSON 字符串先 Base64。
  2. 前面拼接 `Base64("ZHANGLONG")`，即 `WkhBTkdMT05H`。
  3. 拼接后的字符串再整体 Base64。
- 示例：`V2toQlRrZE1UMDVIWVdKalpHVm1adz09` 必须解析为 `abcdefg`。
- 相关实现：`lib/src/protected_export_codec.dart`、`source_config_codec.dart`。

## 播放与缓存行为

- Android 通过系统 `MediaSession` 和媒体播放前台服务接收蓝牙耳机/有线耳机/车机方向盘的播放、暂停、组合播放暂停、上一首、下一首控制；前台 Activity 同时接收车机直接派发的媒体硬件键，应用退到后台且仍有当前曲目时保持媒体会话与播放通知。
- Android 与 DiLink 播放时使用 `USAGE_MEDIA / CONTENT_TYPE_MUSIC` 申请系统音频焦点；小迪等语音助手造成短暂焦点丢失时，将播放器当前内部音量临时压低到原值的 20%，焦点恢复后恢复原值，不持久化临时音量；永久丢失焦点时恢复原音量并暂停。
- DiLink 兼容构建启用低负载显示：Flutter 内存图片缓存最多保留 128 项、24 MB，手机底部播放栏封面保持静态；手机播放栏没有进度条时不得订阅播放位置并持续整行重建。普通 Android/iOS 仍保留播放封面旋转，平板和桌面端需要显示进度条的刷新不受影响。
- iOS 最低支持 13.0，使用 `media_kit_libs_ios_audio` 提供原生音频库；`AVAudioSession` 使用 playback 类别并声明 `UIBackgroundModes=audio`，通过 `MPNowPlayingInfoCenter` / `MPRemoteCommandCenter` 发布曲目信息和播放状态，接收锁屏、耳机及系统控制中心的播放、暂停、上一首、下一首控制；来电等音频中断时暂停，系统允许恢复时继续播放，耳机拔出时暂停。

- 播放模式共 4 种：顺序播放、随机播放、单曲循环、列表循环。
- 顺序播放到最后一首后停止；列表循环才回到第一首。
- 设置页“播放 → 随机播放”默认关闭；开启后仅当当前模式为顺序播放且队列正常播放到末尾时，从当前 Navidrome/Subsonic 服务获取 30 首随机歌曲替换队列并继续播放。随机接口失败或无数据时仍停在队列末尾，等待期间用户手动切歌、切换模式或更换账号后必须丢弃迟到结果；电台不触发该行为。
- 网络音频使用边播边缓存；缓存完成后再次播放同一首应直接复用本地缓存。
- 网络歌曲通过缓存代理启动后连续 25 秒没有新增字节且仍未进入可播放状态时，当前歌曲必须取消代理并改用原始音频流；缓存代理只尝试一次，但字节持续增长时按现有进度观察规则继续等待。原始音频流同样只尝试一次并等待 25 秒，不得重新创建缓存代理或再次直连形成分钟级重试循环。该兜底只影响本次播放，之后再次播放仍可正常尝试缓存。运行日志必须明确记录本地完整缓存、缓存代理（边播边缓存）、原始音频流直连及代理停滞兜底直连四类播放链路，不得记录鉴权 URL。
- 设置页“播放 → 播放失败自动切歌”默认开启；唯一一次原始音频流启动尝试仍不可播放时，才按当前播放模式自动切换下一首，正常缓冲和缓存代理改走直连阶段不触发。关闭后保留当前歌曲供手动重试；顺序播放已到队列末尾且没有下一首时仍安全停止。
- Navidrome/Subsonic 电台使用电台 `streamUrl` 直接串流播放；`.m3u` / `.pls` 会先解析出真实音频 URL；电台直播流不进入普通歌曲缓存。
- 播放电台时队列为当前音源全部可播放电台频道；播放模式、收藏、拖动进度均禁用，进度两端固定显示 `00:00`，上一台/下一台和频道队列仍可用。
- Windows 与 macOS 播放器首次没有本地音量记录时使用 55%，之后持久化并恢复上次的软件音量（包括滑块、静音和 Windows 托盘调整）；Android、DiLink、iPhone 和 iPad 的内部音量统一为 100%，由系统媒体音量负责最终输出音量。
- 已登录状态下本地持久化底部播放队列、当前歌曲索引和播放模式；不保存播放进度、播放/暂停状态、缓冲进度或一次性的“下一首播放”。普通关闭后再次启动会恢复队列并保持暂停，首次点击播放时重新生成当前账号下的流地址并从 `00:00` 开始；退出登录或更换登录账号时同时清空内存和本地播放会话，不在播放会话中额外保存音源地址或账号标识，音频与图片文件缓存仍由缓存设置单独管理。
- 进度条包含底色、缓存进度、已播放进度；已缓存范围内拖动应尽量直接播放。
- 缓冲时在播放圆点里显示分段旋转加载样式。
- 切歌或切换歌单时，最新播放请求必须中断并越过已经过期的媒体打开 Future；旧歌曲打开卡住时不得继续阻塞后续歌曲，包括已完整缓存的本地文件。
- 歌词页需要滚动播放并高亮当前句；用户滚轮或触摸拖动歌词时只切换预览选中句，不得直接跳转播放，预览句右侧显示独立播放按钮，只有点击该按钮才从该句时间开始播放；直接点击歌词只允许选中，不得跳播；预览后 3 秒未确认则清除预览并回到当前播放句。无歌词时在右侧歌词区居中显示“暂无歌词”。

## 桌面行为

- Windows 通过 SMTC 发布当前歌曲元数据和播放状态，并接收蓝牙耳机、键盘媒体键的播放、暂停、上一首、下一首控制；SMTC 播放与暂停指令必须随当前播放状态互斥启用，SMTC 不可用时使用 `WM_APPCOMMAND` 兜底。Windows 主窗口标题和任务栏悬浮缩略图标题在有歌曲时使用当前歌名，没有歌曲时恢复为 `Zmusic`；任务栏缩略图使用当前歌曲封面并提供上一首、播放/暂停、下一首三个原生按钮，没有歌曲时回退应用图标并禁用按钮。
- macOS 最低支持 10.15，通过 `MPNowPlayingInfoCenter` 与 `MPRemoteCommandCenter` 发布歌曲元数据、播放状态并接收系统媒体键和耳机的播放、暂停、上一首、下一首控制。
- macOS 支持从 Finder 拖入或使用“打开方式”打开 MP3、FLAC、APE、M4A、MP4、OGG、Opus、WAV、AIFF；启动早期传入的文件由原生 Runner 暂存，待 Dart 通道就绪后统一交给现有本地播放入口。
- macOS 菜单栏使用可自动适配明暗模式的模板图标，Dock 图标由 AppIcon 资源提供；当前 macOS Runner 面向官网直接分发，不启用 App Sandbox，以保持持久本地路径和递归标签管理可用，若未来上架 Mac App Store 必须改为安全作用域书签方案。
- macOS 每次启动时使用 `1088 × 680` 的最小内容尺寸；点击主窗口关闭按钮时最小化到 Dock，不退出应用；再次点击 Dock 中的应用图标时取消最小化并恢复主窗口，仍可通过系统“退出 Zmusic”或 `Command+Q` 真正退出。

- Windows 客户端只允许运行一个实例。
- Windows 托盘图标左键显示主窗口；右键不使用普通文字菜单，而是打开原生自绘播放控制面板，横向提供上一首、播放/暂停、下一首，并提供当前歌名、静音、音量滑块和退出操作；托盘面板不显示收藏和随机/顺序按钮。面板失去焦点后自动隐藏。
- Windows 设置页“窗口 → 开机启动”默认关闭，使用当前用户 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 启动项，不需要管理员权限；关闭开关和卸载 Zmusic 时都删除该启动项。
- Windows 每次启动时使用 `1088 × 680` 的最小外部窗口尺寸，且不可缩小到该尺寸以下，以保证底部播放栏维持单行；窗口仍允许向更大方向调整。
- Windows 登录后支持从资源管理器向应用窗口拖入一首或多首本地音频；按拖入顺序替换为本地播放队列并立即播放第一首，标签读取失败时使用文件名兜底，不支持的文件直接忽略。
- Windows 安装器将 Zmusic 注册为 MP3、FLAC、APE、M4A、OGG、Opus、WAV 和 AIFF 的可选“打开方式”，但不强制更改系统默认播放器；文件关联启动在首个实例和已运行单实例两种情况下都要把文件路径交给现有本地播放入口。
- 关闭按钮可设置为退出或最小化到托盘。
- 打包目录需要包含 `app_icon.ico`，避免托盘无图标；设置页不再提供任务栏/托盘图标自定义入口。
- 音量默认只显示音量图标；鼠标悬浮显示纵向音量条；点击音量图标静音/恢复。

## 打包记忆

- 用户说“打测试包”时，先运行 `flutter analyze` 和完整 `flutter test`，再默认生成 Windows 安装版、Android 通用 release APK 和 Android DiLink 兼容 APK；可见版本和内部 build/versionCode 均保持不变，不生成或覆盖 `update.json`。不再发布 Windows 绿色目录/zip，不默认打 iOS，除非用户明确点名。
- 用户说“打包”或“打正式包”时，统一执行 GitHub 五端正式发布：先升级可见补丁版本和内部 build/versionCode、整理分平台更新内容，并在本机运行 `flutter analyze` 和完整 `flutter test`；全部通过后才提交并推送全部待发布代码到 GitHub，再创建并推送对应 `v*` 标签触发 GitHub Actions；不得用本机三端产物代替正式 Release。补丁号按 `1.0.10 → 1.0.11` 递增，达到 `1.0.999` 后进位为 `1.1.0`；内部 build/versionCode 每次加 1。当前正式版本为 `1.1.3+31`。
- 正式发布必须等待 GitHub Actions 的 Windows、Android 通用版、Android DiLink、macOS、iOS 以及最终 Release 任务全部成功；随后从该 GitHub Release 下载五个最终资产到本地 `dist`，以下载后的真实文件验证版本、Android 证书/ABI/签名、文件大小和哈希。只有实物验证通过后，才根据这五个下载文件重新生成 `dist/update.json`；禁止根据构建前文件、本机文件或 Actions 临时产物预填最终大小和哈希。
- macOS 源码已补齐；“打测试包”仍只在当前 Windows 主机生成 Windows、Android 通用版和 Android DiLink 三个产物，macOS/iOS 不做本地交叉编译。“打包/打正式包”则由 GitHub Actions 的 macOS runner 生成 macOS 和 iOS 产物，共同组成五端正式 Release。
- GitHub Actions 五端发布工作流位于 `.github/workflows/release.yml`，固定使用 Flutter `3.44.4`；推送 `v*` 标签后并行生成 Windows NSIS 安装器、Android 通用 APK、Android DiLink APK、macOS DMG 和 iOS unsigned IPA，五项成功后自动创建 GitHub Release。iOS 产物固定命名为 `zmusic-ios-unsigned.ipa`，没有 Apple 证书和 Provisioning Profile，必须通过爱思助手、AltStore 或 Sideloadly 自签后安装；macOS 当前仅临时签名且未公证，正式分发仍需配置 Apple Developer 签名与公证凭据。
- `update.json` 的 `updateContent` 必须按 `windows / android / android-dilink / macos / ios` 分平台整理：跨平台改动可以写入全部节点；Windows 安装器、托盘、音量条等 Windows 独有改动只写入 `windows`；Android 通知、MediaSession、手机/平板 UI 等 Android 共用改动只写入 `android` 和 `android-dilink`；DiLink 独有改动只写入 `android-dilink`；macOS 与 iOS 独有改动只写入各自节点。每个平台节点必须同时生成指向文件服务器的 `downloadUrl` 和指向对应 GitHub Release 资产的 `githubDownloadUrl`；清单中的大小、MD5、SHA256 必须基于两个通道实际发布的同一个最终包重新计算。
- 新客户端只读取并校验 `update.json` 的 `sha256`；清单中的 `md5` 仅为已经发布且仍要求 MD5 的旧客户端保留，不能在旧客户端升级链路仍存在时提前删除。Subsonic 登录鉴权使用的 MD5 属于协议要求，与安装包校验无关，不得移除。
- 普通 `flutter build windows --release` 在本机可能卡住 Visual Studio 生成器阶段。
- 可用方式是复用 `build/windows/x64-ninja`：
  - `cmake --build build\windows\x64-ninja --config Release --target zmusic`
  - 构建后复制 `build\windows\app.so` 到 `build\windows\x64-ninja\runner\data\app.so`。
  - 使用 `windows/installer/build-installer.ps1` 将 runner 运行文件筛选到 `build/package/nsis-payload`，并在安装器编译完成后删除临时 payload；不要把绿色目录/zip放进 `dist`。
- Windows 安装包使用 NSIS 3 Modern UI，脚本为 `windows/installer/ZmusicSetup.nsi`，构建入口为 `windows/installer/build-installer.ps1`，本机 NSIS 3.12 安装在 `D:\Program Files\NSIS`；安装器及卸载器图标使用 NSIS 兼容的 `windows/installer/zmusic-installer.ico`，必须通过 `MUI_ICON` / `MUI_UNICON` 设置；固定输出文件名为 `dist\zmusic-windows-x64.exe`。
- Windows 安装包安装后必须在安装目录生成 `uninstall.exe`，创建开始菜单卸载入口，并注册到当前用户的 Windows 卸载列表；卸载时删除程序文件、快捷方式和卸载注册项，但必须保留安装目录下 `userdata`。
- Android 通用 release APK 固定命名为 `dist\zmusic-android.apk`，只包含 `armeabi-v7a` 和 `arm64-v8a`，不再适配 `x86_64`；通用版与 DiLink 都使用压缩 JNI 原生库并在安装时解压，以减小下载包体积。DiLink 兼容包使用 `-PzmusicDiLinkCompatibility=true` 构建并固定命名为 `dist\zmusic-android-dilink.apk`；当前 AGP 在 `minSdk 24` 下的通用版使用 `targetSdk 36`、V2 签名，DiLink 使用 `targetSdk 29`、V1-only 签名，两包使用同一证书。以后默认“打包”必须同时生成这两个 APK。
- 安装版不携带当前本地数据；安装时创建 `zmusic.installed` 和 `userdata`。
- 安装版默认安装到 `%LOCALAPPDATA%\Programs\Zmusic`，缓存和本地数据默认在安装目录 `userdata` 下；覆盖安装和卸载都保留 `userdata`。

## 常用验证

- 格式化：`.\\.flutter-sdk\\bin\\dart.bat format <files>`
- 静态分析：`.\\.flutter-sdk\\bin\\flutter.bat analyze`
- 测试：`.\\.flutter-sdk\\bin\\flutter.bat test`
- Android 打包：`.\\.flutter-sdk\\bin\\flutter.bat build apk --release --no-pub`
- Windows 启动烟测：启动 Windows Release runner 或安装后的 `zmusic.exe`，等待数秒确认进程存活，再关闭。

## 变更记录

- 2026-09-02：修复每日推荐与随便听听同时使用同一 AI 服务时可能出现的间歇性超时。两个推荐加载任务此前会并发向 DeepSeek 发起 80 首候选的排序请求，服务端可能排队导致其中一个在 90 秒边界超时；`AiRecommendationClient` 现在串行执行 AI 排序请求，并将排序响应限制为最多 1,024 个 token、最多返回 30 个候选编号，剩余歌曲继续由本地推荐规则补齐，不改变推荐配额、去重或失败回退行为。执行 Dart format 与 `git diff --check`；按普通功能修改规则未运行 `flutter analyze`、未运行测试、未打包，版本保持 `1.1.2+30`。

- 2026-09-01：完成 AI 服务多配置、模型列表获取和故障转移改动的 `1.1.2+30` 三端测试打包，版本号及内部 build/versionCode 均保持不变。静态分析首次发现 AI 服务菜单项使用可空回调的冗余空值断言，已改为直接传递可空回调；最终 `flutter analyze --no-pub` 无问题，完整 `flutter test --no-pub` 161 项全部通过。Windows 在 Visual Studio x64 环境下完成 CMake/Ninja 与 NSIS 构建，安装包 `dist/zmusic-windows-x64.exe` 大小 `16,703,081` 字节、SHA256 `EF142CEADEF4CD10F7C7180ABB5C541E8C33F9DE6EAA3E9A18E99A0712439C08`，ProductVersion `1.1.2`、FileVersion `1.1.2.30`；因用户已有 `D:\Programs\Zmusic\zmusic.exe` 正在运行且客户端为单实例，本轮未强制终止用户进程执行 runner 启动烟测。Android 通用版 `dist/zmusic-android.apk` 大小 `24,107,563` 字节、SHA256 `652FFEE763C2FCB8C59435E1713CDF61682E124A086C59E2AAC5699B5EA32280`，target API 36、V2 签名；DiLink 版使用显式 `-PzmusicDiLinkCompatibility=true` 独立构建，`dist/zmusic-android-dilink.apk` 大小 `24,646,775` 字节、SHA256 `4A55F8E33E6D0D37F37F513AA90A0C57BB6599443A14D4451D79145F6A062393`，target API 29、V1-only 签名。两份 APK 均为 `1.1.2+30`、最低 API 24、只含 `armeabi-v7a / arm64-v8a`，通过 16 KB zipalign，签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。本轮未生成或覆盖 `dist/update.json`，其 SHA256 仍为 `693F3098265678A2587F04649F8F741D280DE254D732D431D6B4E7F5B7C10531`。

- 2026-08-31：完善全平台 AI 服务配置和推荐故障转移。设置页“添加”入口移到 AI 智能推荐板块右上角并删除底部重复服务行；服务类型改为 ChatGPT、DeepSeek、Tencent、Bailian 四个快捷按钮，点击直接填充名称与地址，API Key 调整到模型名称上方并可调用 OpenAI 兼容 `/models` 接口获取、搜索和选择模型，模型名称仍可自定义。每项配置新增独立启用开关，允许同时启用多个服务；每日推荐和随便听听按列表顺序逐项尝试，单项失败自动使用下一项，全部失败才回退本地推荐，无启用项时 AI 总开关自动关闭。旧设置首次迁移时只启用原先选中的服务，避免升级后意外启用全部配置。执行改动文件 Dart format 与 `git diff --check`；按普通功能修改规则未运行 `flutter analyze`、未运行测试、未打包，版本保持 `1.1.2+30`。

- 2026-08-31：增强桌面与普通平板底部播放栏的毛玻璃透明效果。原播放栏只有 68% 浅色实心遮罩，没有实际背景模糊；现在增加 `BackdropFilter` 20px 模糊，将浅色遮罩降为 42%、深色降为 28%，并加入顶部玻璃高光与向上阴影，使主题底色和用户背景图能明显透入。DiLink 兼容版为控制持续 GPU 负载，只应用新的透明遮罩、高光和阴影，跳过实时模糊；手机悬浮迷你播放栏布局与配色保持不变。执行 Dart format 与 `git diff --check`；按普通功能修改规则未运行 `flutter analyze`、未运行测试、未打包，版本保持 `1.1.2+30`。

- 2026-08-31：修复 AI 服务连接测试成功但推荐刷新容易超时的问题。连接测试继续使用 20 秒最小请求，包含最多 80 首候选的每日推荐/随便听听排序请求由 30 秒延长到 90 秒，适配模型生成和较差网络环境；AI 排序日志新增具体推荐类型，成功时记录候选数与耗时，失败仍无条件回退原本地推荐。执行 Dart format 与 `git diff --check`；按普通功能修改规则未运行 `flutter analyze`、未运行测试、未打包，版本保持 `1.1.2+30`。

- 2026-08-31：完成 AI 智能推荐与默认背景图移除改动的 `1.1.2+30` 三端测试打包，版本号及内部 build/versionCode 均保持不变。静态分析先发现 AI 服务下拉框使用已弃用的 `DropdownButtonFormField.value`，已按 Flutter 新接口改为 `initialValue`；完整测试先发现两条旧用例把任意“添加”文字误判为旧音源入口，已将断言收窄为明确的音源管理操作并确认 AI 服务添加按钮存在。最终 `flutter analyze --no-pub` 无问题，完整 `flutter test --no-pub` 161 项全部通过。Windows Release 在 Visual Studio x64 环境下完成 CMake/Ninja 与 NSIS 构建，安装包 `dist/zmusic-windows-x64.exe` 大小 `16,693,981` 字节、SHA256 `9647AC111AA9D7BCA6B9B7CAA7FAE57ED7B009C14F8897309D02FDDE3F72F0E8`，ProductVersion `1.1.2`、FileVersion `1.1.2.30`；因用户已有 `D:\Programs\Zmusic\zmusic.exe` 正在运行且客户端为单实例，本轮未强制终止用户进程执行 runner 启动烟测。Android 通用版 `dist/zmusic-android.apk` 大小 `24,081,051` 字节、SHA256 `9005267EA293B57D6C7523652C16DFF2489453AFF15DE3EE77ECFEBF14AA5CCF`，target API 36、V2 签名；DiLink 版 `dist/zmusic-android-dilink.apk` 大小 `24,072,841` 字节、SHA256 `F7BFC741C9AB6FAEBAE29B51CB450A2C5D23F3F6CCCB38FE7C045682E7255801`，target API 29、V1-only 签名。两份 APK 均为 `1.1.2+30`、最低 API 24、只含 `armeabi-v7a / arm64-v8a`，通过 16 KB zipalign，签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。本轮未生成或覆盖 `dist/update.json`，其 SHA256 仍为 `693F3098265678A2587F04649F8F741D280DE254D732D431D6B4E7F5B7C10531`。

- 2026-08-31：移除全平台内置默认背景图。背景路径为空、文件不存在或图片加载失败时不再回退品牌图片，只保留当前明暗主题底色；用户上传且文件有效时继续按现有全屏裁切方式显示，独立 Logo 层不受影响。同步移除默认背景资源的 Flutter 打包声明及资源文件，设置页空背景文案改为“未设置背景图”。执行 Dart format 与 `git diff --check`；按普通功能修改规则未运行 `flutter analyze`、未运行测试、未打包，版本保持 `1.1.2+30`。

- 2026-08-31：新增全平台 AI 智能推荐配置与混合推荐链路。设置页支持多个服务配置、唯一当前服务、总开关、ChatGPT/DeepSeek/Tencent/Bailian 快捷地址、可编辑模型与地址、API Key 显隐、无需测试直接保存以及已保存/未保存配置连接测试；API Key 直接使用系统安全存储，普通设置只保存元数据。每日推荐对四类本地候选池做确定性均衡抽样并让 AI 仅提供优先级，最终 30 首仍经过原收藏配额、历史冷却、最高品质去重、当前队列排除和相邻歌手处理；随便听听保留原本地 30 首作为严格兜底，启用 AI 时才额外生成 80 首候选排序。AI 请求只发送临时编号和必要歌曲/播放统计元数据，所有请求异常和无效响应均记录无敏感信息的警告并回退本地结果，曲库随机未接入 AI。执行改动文件 Dart format 与 `git diff --check`；按普通功能修改规则未运行 `flutter analyze`、未运行测试、未打包，版本保持 `1.1.2+30`。

- 2026-08-28：完成 `1.1.2+30` 五端正式发布。发布前 `flutter analyze --no-pub` 无问题，完整 `flutter test --no-pub` 161 项全部通过；发布提交 `dc81df0` 已推送到 `main`，标签 `v1.1.2` 触发 GitHub Actions `Build five-platform release`（run `33147653449`），Windows、Android 通用版、Android DiLink、macOS、iOS 和最终 Release 六项全部成功，Release 地址为 `https://github.com/zlcece/zmiusc/releases/tag/v1.1.2`。从 Release 下载五个最终资产覆盖本地 `dist` 后完成实物验证：Windows `zmusic-windows-x64.exe` 大小 `16,684,565` 字节、MD5 `08E49E1AAEAE68C9D583747E9862A4B1`、SHA256 `988C9DBCE3142A6FA496C96776869782FB0684321DAD4E23ECDA4AB9A86F9340`，ProductVersion `1.1.2`、FileVersion `1.1.2.30`；Android 通用版 `zmusic-android.apk` 大小 `24,074,069` 字节、MD5 `1956A13FEB84D36E3FC6EB33EFD7C227`、SHA256 `DF4ECEAC39B6371932121CE519CA7A9D483A90E930206D1F954F4AEC5D8F92C8`；DiLink `zmusic-android-dilink.apk` 大小 `24,065,855` 字节、MD5 `9419BC7057514BB03F9D4D7D7B2C1CE5`、SHA256 `77F3F0D5A8689126DC0C1A68DFC2D99A3BCD1DE99C962E7B02B6E14CBF1D9477`。两份 Android 包均为 `1.1.2+30`、最低 API 24、只含 `armeabi-v7a / arm64-v8a` 并通过 16 KB zipalign；通用版 target 36/V2、DiLink target 29/V1-only，证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。macOS `zmusic-macos.dmg` 大小 `32,961,532` 字节、MD5 `DCC50C1845224D3B5BA4A46851AEEB5D`、SHA256 `C8C54D62EDFAA7C9D203C995FAC134F58F1A98F1ED8D7C4A51C4D90F1D1A1AF8`，包内版本 `1.1.2(30)`、最低 macOS 10.15；iOS `zmusic-ios-unsigned.ipa` 大小 `11,120,432` 字节、MD5 `8888509355ECA2FCB5EB0DC0F4DE9C99`、SHA256 `43A71B28F9AF5630EC2C0C4D2C4C3B939F7DE21D9F79E81485A0792E08B4F688`，包内版本 `1.1.2(30)`、最低 iOS 13.0。最终 `dist/update.json` 已按五个平台分别写入更新内容、默认/GitHub 双地址、真实大小、兼容旧客户端的 MD5 和 SHA256；清单大小 `5,358` 字节、MD5 `A4B3798C4E455EBA7F7BC6E9FFEB9281`、SHA256 `693F3098265678A2587F04649F8F741D280DE254D732D431D6B4E7F5B7C10531`。

- 2026-08-28：准备正式发布 `1.1.2+30` 五端版本。版本已同步到 Flutter、Windows 本地生成配置、测试默认 `PackageInfo` 和 GitHub Actions 的五端 artifact、iOS 版本断言及 Release 描述；测试中的模拟远端版本调整为 `1.1.3`。待发布内容包括“随便听听 / 曲库随机”及首页布局和启动自动播放联动、歌单和发现板块一键播放与最高品质去重、底部消息统一、歌单详情封面播放按钮统一，以及 Android/DiLink 语音助手音频焦点压低与恢复。发布前 `flutter analyze --no-pub` 无问题，完整 `flutter test --no-pub` 161 项全部通过；下一步推送 `main` 与 `v1.1.2` 标签，五端最终产物、实物验证和 `update.json` 信息待 GitHub Release 完成后补充。

- 2026-08-28：统一歌单详情页与首页歌单卡片的封面播放按钮。歌单详情封面不再直接叠加无底色的白色播放图标，改为复用首页同款半透明深色圆形播放按钮，并保留播放全部、禁用状态和异步加载进度；修改仅影响歌单详情，专辑和歌手详情保持现状。执行 Dart format 和 `git diff --check`，按普通功能修改规则未运行 `flutter analyze`、未运行测试、未打包，版本保持 `1.1.1+29`。

- 2026-08-28：完成 `1.1.1+29` 三端测试打包，版本号与内部 build/versionCode 均保持不变。`flutter analyze --no-pub` 无问题，完整 `flutter test --no-pub` 161 项全部通过；Windows Release 在 Visual Studio x64 开发环境下完成 CMake/Ninja 构建，安装包启动烟测确认进程存活且窗口可响应。测试产物：Windows 安装包 `dist/zmusic-windows-x64.exe` 大小 `16,682,493` 字节、SHA256 `568C27D8D507988F0B3E562DD8DC86C940D63374F09111898605B8C1928FD9B3`；Android 通用版 `dist/zmusic-android.apk` 大小 `24,077,843` 字节、SHA256 `CC66BAE9340162A59F5196267F3DFFAD7497974FB5F372E3A5E462F6DD6282BC`，target API 36、V2 签名；Android DiLink 版 `dist/zmusic-android-dilink.apk` 大小 `24,617,415` 字节、SHA256 `7944D155172C611D1DEECC204F6962BF56F59FAE895CF7D529919EBD4C35B9D4`，target API 29、V1 签名。两份 APK 均为最低 API 24，只含 `armeabi-v7a` / `arm64-v8a`，通过 16 KB zipalign，签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。本轮未生成或覆盖 `dist/update.json`，其 SHA256 仍为 `D2E1D0D0B4D6158D1EE67BF30F82CEB4CCFE5DF146031804DF7F966403AE5C87`。

- 2026-08-28：首页新增歌单与发现板块一键播放。我的歌单和公开歌单卡片在封面中央显示播放按钮，加载远程歌单后按原始顺序和内容直接播放；最新专辑、随机专辑、最近播放、最多播放在标题后显示播放全部按钮，并发加载当前板块全部专辑，允许部分请求失败后继续播放成功结果。发现板块聚合队列复用推荐模块的标准化“歌手 + 歌名”规则去重，同曲多格式保留 `FLAC > APE > MP3` 的最高品质，并保持首次出现位置；异步按钮加载期间显示进度并防止重复触发。执行 Dart format 和 `git diff --check`，按普通功能修改规则未运行 `flutter analyze`、未运行测试、未打包，版本保持 `1.1.1+29`。

- 2026-08-27：为“随便听听 / 曲库随机”功能生成三端 `1.1.1+29` 测试包，未升级版本且未修改 `dist/update.json`。首次完整测试发现隐藏首页模块夹具未关闭新增“随便听听”，以及两个界面测试依赖 Flutter 覆盖路由命中顺序；测试仅补齐隐藏配置，并改为直接触发控件已绑定回调后继续验证真实页面与状态，未修改产品行为。最终 `flutter analyze --no-pub` 无问题，完整 `flutter test --no-pub` 161 项全部通过。Windows Ninja Release 与 NSIS 安装包构建成功；Android 通用版构建成功，DiLink 首次因已启动 Gradle Daemon 未读取后设环境变量而与通用包相同，已改用显式 `-PzmusicDiLinkCompatibility=true` 全量重建并核验差异。最终产物：`dist/zmusic-windows-x64.exe` 16,677,039 字节，SHA256 `0511D264D37F45C370CF126FBC8B93D8D80CA1FC10C2905B67AE3233220B1FD7`，ProductVersion `1.1.1` / FileVersion `1.1.1.29`；`dist/zmusic-android.apk` 24,065,781 字节，SHA256 `8256BC699690FBD0A0160DE3718206FEC1F3326BE4444B3D1B14AD4E7B89A70A`，API 24-36、V2 签名；`dist/zmusic-android-dilink.apk` 24,605,356 字节，SHA256 `604A220D36E5F416F6B30BBC1C6071DBFC02E88B74213FACDC6E13554715CC0D`，API 24-29、V1-only 签名。两份 APK 均只含 `armeabi-v7a / arm64-v8a`，版本均为 `1.1.1+29`，证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。因机器上已有用户运行的 `D:\Programs\Zmusic\zmusic.exe`，未强制结束用户进程执行 Windows 启动烟测。

- 2026-08-27：新增全平台“随便听听”和“曲库随机”，并重组首页推荐入口与启动自动播放设置。首页第一组按可配置的 1-3 顺序显示“每日推荐 / 随便听听 / 曲库随机”，第二组继续显示四项常规入口；手机每行两个，桌面/平板分别三项、四项同排。“随便听听”从 300 首随机候选及最近/最多播放专辑样本中生成最多 30 首，利用 Subsonic 返回的 `playCount / played / genre` 优先未听、久未听、低频及风格反差歌曲，统一做品质去重、队列排除和相邻歌手错开。“曲库随机”新增 PlayerController 前进式隐藏队列：初始 10 首，播放到第 8 首时追加下一批 10 首，会话内去重；UI 固定随机图标并禁用上一首、模式切换和队列入口，手机迷你播放栏将队列按钮替换为下一首，普通播放会自动退出该模式，隐藏队列不持久化。设置页新增推荐播放组三项显隐和顺序，总开关联动折叠；原“启动后自动播放每日推荐”改为总开关加可见入口单选，隐藏已选入口时按当前顺序回退，全部关闭时自动关闭。同步扩展 Track 序列化、Subsonic 映射及相关设置/组件测试代码。执行 Dart format 和 `git diff --check`，按普通功能修改规则未运行 `flutter analyze`、未运行测试、未打包，版本保持 `1.1.1+29`。

- 2026-08-24：修复 Android DiLink 呼出小迪时 Zmusic 音量异常变大的问题。Android 媒体服务在播放期间按标准媒体用途申请音频焦点，短暂焦点丢失和可压低中断统一通知 Dart 将播放器内部音量临时降至原值的 20%，焦点恢复、会话停止或服务退出时恢复原值；永久焦点丢失时恢复音量并暂停。临时压低不写入桌面音量记录，普通 Android 同步获得正确的语音助手焦点处理。仅执行 Dart 格式化和代码差异检查，按普通功能修改规则未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.1.1+29`。

- 2026-08-20：统一全平台消息反馈位置与时长。移除桌面顶部右侧临时状态文字，刷新、搜索及已有操作反馈全部复用底部 `SnackBar`；新消息会立即替换旧消息，固定显示 2 秒，应用进入主页时已有待显示状态也在首帧后从底部展示。搜索没有歌曲、歌手和专辑结果时只保留结果页空状态，不再重复弹出“没有找到相关结果”；非空结果数量提示保持不变。同步更新两条既有桌面/手机消息位置断言；仅执行 Dart 格式化和差异检查，按普通功能修改规则未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.1.1+29`。

- 2026-08-20：完成 `1.1.1+29` 五端重新发布。该版本保持可见版本 `1.1.1`，仅将内部 build/versionCode 从 `28` 升到 `29`，用于替换此前未正式投放的 `1.1.1+28` 资产；更新弹窗已修复真实双通道清单下只显示遮罩的问题，并改为 `稍后更新 / 服务器更新 / GitHub更新` 三个直接按钮。发布前 `flutter analyze --no-pub` 无问题，完整 `flutter test --no-pub` 161 项全部通过；修复提交 `7bbb51a` 已推送到 `main`，重新创建 `v1.1.1` 标签后触发 GitHub Actions `Build five-platform release`（run `32324954019`），Windows、Android、Android DiLink、macOS、iOS 和最终 Release 六项全部成功，Release 地址为 `https://github.com/zlcece/zmiusc/releases/tag/v1.1.1`。从 Release 下载五个最终资产覆盖本地 `dist` 后完成实物验证：Windows `zmusic-windows-x64.exe` 大小 `16,671,933` 字节、MD5 `A517B4ABEDBEDD6161007BBE3B7FE301`、SHA256 `9D1B94E47DA9E8FCC59DB840FB0DD3A6E2C236F94E3B3B74A5F98AAEE81C65AC`，ProductVersion `1.1.1`、FileVersion `1.1.1.29`；Android 通用版大小 `23,992,399` 字节、MD5 `D10C8F757D027815A5597D7AAE10F681`、SHA256 `680DEDB2A5877EC7D7892FB0714713FBACCDCC8C68DF9C84365DDFEC9F5855D8`；DiLink 大小 `23,984,188` 字节、MD5 `95A0988FE52E0A3F30F338DFA2941A02`、SHA256 `78364CAB3C2ED25017E8FB093F059B6A1E3209E4B402A44AB1BAB7848EE0912E`。两份 Android 包均为 `1.1.1+29`、最低 API 24、只含 `armeabi-v7a` / `arm64-v8a` 并通过 16 KB zipalign；通用版 target 36/V2、DiLink target 29/V1-only，两包证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。macOS DMG 大小 `32,921,729` 字节、MD5 `D47E1790D7AB64E0C7B640F73315C20C`、SHA256 `E1ED12F171A9DC261483B1492F2D2C5866103338161258491919E248AF821253`，包内版本 `1.1.1(29)`、最低 macOS 10.15；iOS unsigned IPA 大小 `11,084,729` 字节、MD5 `04956C7AA0BDF257B444A3916B62B0C3`、SHA256 `133ACA9E8444DE31016752E761E81F4266976AE316D4459EA59C19B36DDEB0BB`，包内版本 `1.1.1(29)`。最终 `dist/update.json` 保留 `1.1.0 → 1.1.1` 的全部既有分平台更新项并追加本次更新弹窗修复，五端大小与哈希均按下载实物重写；清单大小 `5,137` 字节、MD5 `4632F8F58331B6862CD9AA782ED5D90B`、SHA256 `D2E1D0D0B4D6158D1EE67BF30F82CEB4CCFE5DF146031804DF7F966403AE5C87`。

- 2026-08-20：准备重新发布 `1.1.1+29` 五端版本。可见版本保持 `1.1.1`，仅把内部 build/versionCode 从 `28` 提升到 `29`，用于替换此前未正式投放的 `1.1.1+28` 资产；发布内容保留 `1.1.0 → 1.1.1` 的全部既有更新项，并追加检查更新弹窗可见性修复及“稍后更新 / 服务器更新 / GitHub更新”三个直接操作按钮。待本机分析和完整测试通过后，重新提交并构建 `v1.1.1` 五端 Release，再按下载实物重写 `dist/update.json`。

- 2026-08-19：按最新交互要求把更新弹窗的下载通道选择改为三个直接操作按钮：`稍后更新` 关闭弹窗，`服务器更新` 直接使用默认文件服务器，`GitHub更新` 直接使用 GitHub 地址；下载期间三个按钮统一禁用，触发下载的按钮显示“下载中”，原有进度和 SHA256 校验链路保持不变。双通道更新回归测试同步改为断言三个按钮并通过服务器按钮触发下载。最终 `flutter analyze --no-pub` 无问题，完整 `flutter test --no-pub` 161 项全部通过；再次临时构建 `1.0.9+26` Windows 安装包，在全新隔离数据目录连接服务器真实 `1.1.0` 清单启动，截图确认新版本信息及三个按钮完整可见且没有 Flutter 布局异常。最终 Windows 测试包为 `dist/zmusic-windows-update-test-1.0.9.exe`，大小 `16,670,120` 字节，SHA256 `286C2E6243B3E8EB6D04E81ACA56B81594B92784EF49C8DD84884A400B490576`；项目版本配置已恢复 `1.1.1+28`，本轮按用户明确要求只构建 Windows 测试包，未构建 Android。

- 2026-08-19：完成检查更新弹窗缺失问题的真实 Windows 低版本验证并修正根因。先把 Windows 测试构建临时降为 `1.0.9+26`，连接服务器当前 `1.1.0` 清单后复现“只有模态遮罩、没有弹窗”；隔离实例日志确认真实清单含 `githubDownloadUrl` 时，更新弹窗 `Wrap` 中的 Material `SegmentedButton` 在固有尺寸计算阶段触发 `RenderObject constraints` / `RenderBox was not laid out`。下载通道改为两个 `ChoiceChip`，保留默认/GitHub 双通道；弹窗单实例状态改为按 `AppController` 隔离，避免跨应用实例残留。测试清单补充 GitHub 通道并断言两个通道控件。最终 `flutter analyze --no-pub` 无问题，完整 `flutter test --no-pub` 161 项全部通过；重新构建 `1.0.9+26` Windows 安装包并在全新隔离数据目录实际启动，已截图确认正常显示“发现新版本 1.1.0”、更新内容、两个下载通道和下载按钮，未再出现布局异常。测试安装包为 `dist/zmusic-windows-update-test-1.0.9.exe`，大小 `16,687,536` 字节，SHA256 `862DCEB34D76DA834CE0EDDC05C0648FCED7F62C1D688AB428389A97C8B6809D`；项目版本配置已恢复 `1.1.1+28`，本轮按用户明确要求只构建 Windows 测试包，未构建 Android。

- 2026-08-19：修复服务端确有新版本时检查更新只留下暗色模态遮罩、更新弹窗内容不可见的问题。启动自动检查与设置页手动检查现在共用单实例弹窗保护，避免并发检查重复压入模态路由；更新弹窗增加响应式宽高约束并使用 `AlertDialog` 内置滚动内容区，长更新说明和双下载通道不会再挤出标题及操作按钮。仅执行 Dart 格式化和差异检查，按普通功能修改规则未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.1.1+28`。

- 2026-08-19：完成 `1.1.1+28` 五端正式发布。发布前 `flutter analyze --no-pub` 无问题；完整测试首次仅因升级版本后“远端新版本”测试夹具与本地版本相同导致 1 项断言失败，将模拟远端版本调整为 `1.1.2` 后重新执行完整 `flutter test --no-pub`，161 项全部通过。源码提交 `3dd5194` 已推送到 `main`，带注释标签 `v1.1.1` 触发 GitHub Actions `Build five-platform release`（run `32211419275`），Windows、Android、Android DiLink、macOS、iOS 和最终 Release 六项全部成功；Release 地址为 `https://github.com/zlcece/zmiusc/releases/tag/v1.1.1`。从 Release 下载五个最终资产覆盖本地 `dist` 后完成实物验证：Windows `zmusic-windows-x64.exe` 大小 `16,672,929` 字节、MD5 `25BD74F82BD9ADEA6379ECBDD1DB9CAA`、SHA256 `9409216EA7664C4E35436BDD4216467A8212E236447E60DCE5BE5C73182B9F46`，ProductVersion `1.1.1`、FileVersion `1.1.1.28`；Android 通用版大小 `23,992,148` 字节、MD5 `D97CCBF2603FBCDDCC10ADE5E612F0A1`、SHA256 `970CFC9E53C6C7485ABB3EFF2F73B0594C5588CC93419AA254561D5147FF7D47`；DiLink 大小 `23,983,930` 字节、MD5 `AEA4480C1C559395964363811D3042BD`、SHA256 `2F834481CF22AFA1B0B8C9D23209B48059B2B17FD475D9432C825DC7554D9276`。两份 Android 包均为 `1.1.1+28`、最低 API 24、只含 `armeabi-v7a` / `arm64-v8a` 并通过 16 KB zipalign；通用版 target 36/V2、DiLink target 29/V1-only，两包证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。macOS DMG 大小 `32,924,483` 字节、MD5 `077B923C23FEF061549840E80492DCC4`、SHA256 `91B03C8FD74D9306602224F361390FF515CAF9DD5213C95D891F981078E5C504`，包含版本 `1.1.1(28)` 的 `Zmusic.app`；iOS unsigned IPA 大小 `11,082,862` 字节、MD5 `BF8B5767C3889272E71B0CA608F9EFFB`、SHA256 `3586C455A181B276C45282553E89D556DB433CE659C1088C57532FA1AF620002`，包内版本为 `1.1.1(28)`。最终 `dist/update.json` 已按五个平台分别填写更新内容、默认/GitHub 双地址、真实大小、兼容旧客户端的 MD5 和 SHA256；清单大小 `4,402` 字节、MD5 `7A4CD809BD7F63CC32CF2A59B981263B`、SHA256 `0B84DA57C21F4FD0B4FD6EDD7095F7DBF9669E798F5F7AC293DB31ECF958E775`。

- 2026-08-19：准备正式发布 `1.1.1+28` 五端版本。版本已同步到 Flutter、Windows 生成配置、测试默认 `PackageInfo` 和 GitHub Actions 的五端 artifact、iOS 版本断言及 Release 描述；待发布内容包括缓存代理/原始流各 25 秒的一次性播放启动兜底、缓存仍增长时延长观察、播放失败自动切歌衔接、启动后自动播放每日推荐、桌面播放队列右键“下一首播放”，以及 Windows/macOS 软件音量持久化。正式发布前将先完成 `flutter analyze --no-pub` 和完整 `flutter test --no-pub`，通过后再推送 `main` 与 `v1.1.1` 标签；五端最终产物、实物验证和 `update.json` 信息待 GitHub Release 完成后补充。

- 2026-08-18：按“打测试包”规则完成当前改动的三端测试构建，版本保持 `1.1.0+27`，未生成或覆盖 `dist/update.json`。`flutter analyze --no-pub` 无问题，完整 `flutter test --no-pub` 161 项全部通过；Windows CMake/Ninja Release 与 NSIS 安装器构建通过，产物 `dist/zmusic-windows-x64.exe` 为 `16,668,542` 字节、SHA256 `DD5763AE592239E592E33EB95CD3EBCB71FB6DA3C3FF19D67FD43C0151FAF206`，ProductVersion `1.1.0`、FileVersion `1.1.0.27`。Android 通用版 `dist/zmusic-android.apk` 为 `23,994,605` 字节、SHA256 `223C05F4ECA9D21261D1D72BC29E9CC1A38E3695AA005EBC2E0A5C4250A7702C`；DiLink 版 `dist/zmusic-android-dilink.apk` 为 `23,986,383` 字节、SHA256 `6A4C15270D4C1BF996E42773CA7008A398A1083A25B732E06C7D5556113ED7D0`。两份 APK 均为 `1.1.0+27`、最低 API 24，只含 `armeabi-v7a` / `arm64-v8a`，证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`；通用版 target 36/V2，DiLink target 29/V1-only。当前已安装的 Zmusic 正在运行，为避免中断用户播放，本轮未启动第二个 Windows 实例做运行烟测。

- 2026-08-18：Windows 与 macOS 软件音量新增本地持久化，首次没有记录时仍使用 55%，后续启动恢复上次音量；界面滑块、静音/恢复以及播放器接收到的 Windows 托盘音量变化均使用 300ms 合并写入，页面销毁时补写最终值。Android、DiLink、iOS 继续使用 100% 内部音量且不读取该桌面记录。新增内存存储和桌面恢复音量 widget 测试；完整验证和三端测试包构建均已通过，版本保持 `1.1.0+27`。

- 2026-08-18：桌面端普通播放队列条目新增右键“下一首播放”，复用现有临时下一首机制，把所选歌曲安排在当前歌曲播放结束后优先播放，不立即打断当前歌曲，也不永久改写原队列；电台频道队列不提供该操作。本轮仅执行 Dart 格式化和差异检查，按普通功能修改规则未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.1.0+27`。

- 2026-08-18：播放启动等待从 20 秒调整为 25 秒，并把默认恢复次数从 2 次收敛为 1 次：缓存代理在无新增字节时等待 25 秒后只切换一次原始音频流，原始流再等待 25 秒仍未播放就直接进入失败处理，不再二次直连；开启“播放失败自动切歌”时切换下一首，否则停止缓冲并保留当前歌曲。缓存字节持续增长时继续等待的规则保持不变；同步修正最终一次直连超时仍显示“正在重试”的误导日志。本轮仅执行 Dart 格式化和差异检查，按普通功能修改规则未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.1.0+27`。

- 2026-08-18：设置页“播放”新增“启动后自动播放每日推荐”持久化开关，默认关闭。开启后，已登录账号在应用启动或登录后的首页数据及每日推荐加载完成时，用当天推荐替换播放队列并从第一首开始播放；手动刷新曲库、刷新推荐或运行时开启设置不会触发，若加载期间用户已经主动开始播放其他歌曲也会跳过，避免抢占当前播放。本轮仅执行 Dart 格式化和差异检查，按普通功能修改规则未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.1.0+27`。

- 2026-08-18：修复缓存代理仍持续接收音频数据时被固定启动超时误判为停滞的问题。播放器启动看门狗现在按检测周期比较代理已接收字节数；只要字节仍增长就继续等待播放器启动，最多额外延长 5 个周期（默认总等待约 2 分钟），只有连续周期无新增数据、代理已停止下载或达到总等待上限时才改用原始音频流，并新增“仍在接收数据，继续等待”的运行日志。本轮仅执行 Dart 格式化和差异检查，按普通功能修改规则未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.1.0+27`。

- 2026-08-18：完成 `1.1.0+27` 五端正式发布。由于 SHA256 单哈希校验、双下载通道和 Android 应用内更新属于功能级变更，撤下误生成的 `v1.0.22` Release/标签后改以 `v1.1.0` 发布；正式发布前 `flutter analyze --no-pub` 无问题，完整 `flutter test --no-pub` 160 项全部通过。GitHub Actions `Build five-platform release`（run `32112490008`）的 Windows、Android、Android DiLink、macOS、iOS 和最终 Release 六项全部成功，Release 地址为 `https://github.com/zlcece/zmiusc/releases/tag/v1.1.0`。从 Release 重新下载五个最终包并覆盖本地 `dist` 后完成实物校验：Windows `zmusic-windows-x64.exe` 大小 `16,673,294` 字节、MD5 `14BA60D1359E379402FBCAFB57F5C2EC`、SHA256 `9ADAB2E97059C60CF9E2FBEF1F0D5C70EEB0DBCD1EBD5EBD7D69BE1686F9B618`；Android 通用版大小 `23,987,744` 字节、MD5 `0ACD89EAAFD220B2103521628912C7B5`、SHA256 `BBB84D9211446C3A6D1F6049E942EA9EAC4245036F5994B019989851EBC57F14`；DiLink 大小 `23,979,519` 字节、MD5 `DFEF66165095AD31B515D8EEE6ABA928`、SHA256 `63CB9FADD57F533C3BACCE4815D08254B74D4431043C9805BABF02A4003C7F33`；macOS DMG 大小 `33,462,398` 字节、MD5 `949B4079955DADEA54DF1F08F7949166`、SHA256 `A8D5A69F4A50B4541543F4284E38BC5ACB810EE0C979BE273AA72436AC5716BA`；iOS unsigned IPA 大小 `11,081,931` 字节、MD5 `D7CDA01867ACC736EAD73997BDC4A3C7`、SHA256 `1ABEA2C395BDA2A443304C9BBEF61E8CCD10BA1B538DF7756C19732A35FEFC54`。Windows 安装器 ProductVersion 为 `1.1.0`、FileVersion 为 `1.1.0.27`；两份 Android 包均为 `1.1.0+27`、最低 API 24、只含 `armeabi-v7a` / `arm64-v8a`、压缩 `libmpv.so` 并通过 16 KB zipalign，通用版 target 36/V2、DiLink target 29/V1-only，两包证书 SHA256 均为正式证书 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。最终 `dist/update.json` 包含 `windows / android / android-dilink / macos / ios` 五个节点，版本号、内部构建号 `27`、大小、兼容旧客户端的 MD5、SHA256、默认/GitHub 双地址和分平台说明均匹配最终包；清单大小 `4,441` 字节、MD5 `D5F045B4D1F4472082B315981FC3ABEF`、SHA256 `285A977EF00D1BF94D52D3EE966224D4986EFC6284B391CB35016DB68D216DDB`。

- 2026-08-18：准备发布 `1.1.0+27` 五端正式包。由于本次将更新校验从 MD5 切换为 SHA256，并加入双下载通道与 Android/DiLink 应用内更新，按功能级变更从原计划 `1.0.22` 调整为 `1.1.0`，内部构建号保持 `27`；已撤下误生成的 GitHub `v1.0.22` Release 和标签。版本已同步到 Flutter、测试默认 `PackageInfo` 和 GitHub Actions 五端 artifact/iOS 断言/Release 描述；本次发布还包含播放失败自动切歌、已下载安装包复用和安装成功后缓存清理。首次完整测试因更新检查夹具与本地版本相同导致 2 项断言失败，模拟远端版本改为高于本地的版本后通过；版本调整为 `1.1.0` 后重新执行 `flutter analyze --no-pub` 无问题，完整 `flutter test --no-pub` 160 项全部通过。验证通过后将推送 `v1.1.0`；GitHub 五端 Release 完成后下载最终资产、实物校验并生成 `dist/update.json`。

- 2026-08-18：正式打包流程改为 GitHub 五端发布。后续“打包/打正式包”先升版并整理分平台更新内容，再提交推送代码和 `v*` 标签，由 GitHub Actions 生成 Windows、Android、Android DiLink、macOS、iOS 五端 Release；全部任务成功后必须重新下载五个最终 Release 资产到本地 `dist`，完成版本、签名、证书、ABI、大小和哈希实物校验，最后才据此更新 `dist/update.json`。本地三端构建仅保留给“打测试包”。本次只修改协作规则，未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.21+26`。

- 2026-08-18：应用更新校验由 MD5 切换为 SHA256。`AppUpdateInfo` 和清单解析不再读取 MD5，只接受 64 位 `sha256`；更新包下载后仅计算一次 SHA256。下载前若同名缓存包已存在，则先校验 SHA256，一致时直接复用并打开安装器，不再重复下载；不一致或下载校验失败时删除缓存。校验成功后在更新目录记录目标 `versionCode`，Windows/Android 新版本下次启动确认当前 build 已达到目标后递归删除缓存安装包和记录，用户取消安装时因版本未变化而继续保留。同步更新现有测试夹具字段，但按普通功能修改规则仅执行 Dart 格式化和差异检查，未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.21+26`。为保证已发布旧客户端仍能读取下一版清单，发布流程暂时继续保留 `update.json.md5`，新客户端不会读取或计算该值。

- 2026-08-18：Android 通用版与 DiLink 更新改为应用内下载。更新弹窗沿用 Windows 的进度与完整性校验流程，APK 显式保存到应用缓存目录，完成后通过新增原生 `installUpdate` 通道和受限于该缓存目录的 `FileProvider` 打开系统 APK 安装器，不再调用浏览器；Android 8 及以上缺少未知来源授权时先进入当前应用授权页，返回并授权后继续安装，拒绝时回到弹窗显示错误。新增 `REQUEST_INSTALL_PACKAGES` 权限和 AndroidX Core 显式依赖，用户仍可在系统确认页取消，不支持静默覆盖。仅执行 Dart 格式化、Manifest/XML 解析和差异检查，按普通功能修改规则未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.21+26`。

- 2026-08-18：应用更新新增双下载通道。`update.json` 五个平台节点新增 `githubDownloadUrl`，当前文件服务器 `downloadUrl` 继续作为默认通道；更新弹窗在清单提供 GitHub 地址时显示“默认通道 / GitHub 通道”选择，Windows 内置下载和其他平台外部浏览器均使用用户所选地址，两个通道继续执行同一缓存规避与 SHA256 校验规则。同步把正式发布必须生成双地址写入打包记忆。仅执行 Dart 格式化、JSON 解析和差异检查，按普通功能修改规则未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.21+26`。

- 2026-08-18：新增全平台“播放失败自动切歌”设置并默认开启，配置写入现有 `AppSettings` 持久化且在启动加载和运行时修改后立即同步到播放器。仅在日志对应的“原始音频流多次启动失败，服务端音频流当前不可播放”最终失败分支中调用现有 `playNext()`，沿用当前顺序、随机和列表循环规则；关闭开关或当前没有下一首时保留手动重试，不影响正常缓冲与代理转直连。仅执行 Dart 格式化和差异检查，按普通功能修改规则未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.21+26`。

- 2026-08-17：完成 `1.0.21+26` 五端正式发布。首轮 GitHub Release 的 Android 证书异常被拦截后，提交签名修复 `a897b8a`，更新 `v1.0.21` 并触发 GitHub Actions 第二轮 `Build five-platform release`（run `32024258346`）；Windows、Android、Android DiLink、macOS、iOS 和最终 Release 六个任务全部成功，Release 地址为 `https://github.com/zlcece/zmiusc/releases/tag/v1.0.21`。从 Release 重新下载五个最终包并覆盖本地 `dist` 后完成实物校验：Windows `zmusic-windows-x64.exe` 大小 `16,666,325` 字节、MD5 `52A2FD1BF302519A475F73554D0A9632`、SHA256 `881F4036AE717E82384BE5AB9B3925FA77047868F71626328B4BDDBF8B67037C`；Android 通用版大小 `23,969,790` 字节、MD5 `238DE597885883FA35840061F3020D25`、SHA256 `E076303B6C277AA2406046A0CBEFDACB31E6D118432241FA14EE38440781AD63`；DiLink 大小 `23,961,583` 字节、MD5 `34761770D730DB7C10D10CDC6B78D82E`、SHA256 `85BBF72090884B1DE13E5B38173DA1FDE07FF44D0CD1C90773E775559B0B3701`；macOS DMG 大小 `32,908,324` 字节、MD5 `D9B615A38977D13E45595A4DEF23824A`、SHA256 `0D2B36281BC794610A9B7EDC3E37B3D20185844E428C60000DF3EE3E2BE1DBF4`；iOS unsigned IPA 大小 `11,075,551` 字节、MD5 `6EC0F6CE7E0CDCEB9D847103E249DB91`、SHA256 `0959AE91995D41209EA0D21A2ADC0373848CCD8E08E7C14328F96A723FBFFEE7`。两份 Android 包均为 `1.0.21+26`、最低 API 24、仅含 `armeabi-v7a` / `arm64-v8a` 并通过 16 KB zipalign；通用版 target 36/V2，DiLink target 29/V1-only，两包证书 SHA256 均为现有正式证书 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。最终 `dist/update.json` 包含 `windows / android / android-dilink / macos / ios` 五个节点，版本、大小、MD5、SHA256 和带指纹下载地址均匹配上述 GitHub 最终包；清单大小 `4,258` 字节、MD5 `7F23A56462D5F644BAB57DE3C39F2766`、SHA256 `7806DC70CC1F0B23B41422FC4DAEFD8BE499E893F525010B713B428E20270DDA`。按正式打包规则未运行 `flutter analyze` 或测试。

- 2026-08-17：在 GitHub `v1.0.21` 首轮五端构建完成后进行发布前签名核验，发现通用 Android 与 DiLink APK 使用了 GitHub runner 各自临时生成的调试证书，两包证书彼此不同且与现有正式 APK 证书不同，无法覆盖升级，因此未采用首轮 Android 产物作为最终发布包。Android Gradle 构建新增可选的环境变量签名配置，本地未注入时继续使用现有调试证书；GitHub 两个 Android job 在构建前从同一个加密 Actions Secret 解码临时 keystore，并显式传入路径、别名和密码，keystore 不写入仓库。仓库 Secret `ZMUSIC_ANDROID_KEYSTORE_BASE64` 已配置为现有正式 APK 对应证书，后续 GitHub 通用版和 DiLink 必须校验为相同证书 SHA256 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3` 后才可发布。按正式打包规则未运行测试或 `flutter analyze`；将重新触发 `v1.0.21` 五端构建并以第二轮 Release 下载文件重算 `update.json`。

- 2026-08-17：准备正式发布 `1.0.21+26`。播放器运行日志新增播放链路标识，分别记录“本地完整缓存”“缓存代理（边播边缓存）”“原始音频流（直连）”和“原始音频流（缓存代理停滞兜底）”，代理下载完成后切回本地文件也会单独记录，日志不包含流 URL 或鉴权信息。本轮正式发布同时包含此前尚未提交的 DiLink 低负载显示、当前会话日志清理、缓存文件并发清理保护、缓存代理启动停滞改走原始流，以及桌面音量悬浮层生命周期修复。版本已同步为 `1.0.21+26`，GitHub Actions 五端工作流的产物名、iOS 校验和 Release 信息已更新。按正式打包规则未运行 `flutter analyze` 或测试；本地 Windows CMake/Ninja Release 与正式 NSIS 构建成功，runner ProductVersion/FileVersion 均为 `1.0.21+26`，安装器包含 24 个 payload 文件、payload 大小 `49,824,740` 字节；Android 通用版和 DiLink 均为 `versionName=1.0.21`、`versionCode=26`、最低 API 24，通用版 target 36，DiLink target 29。GitHub 五端发布产物、最终哈希和 `update.json` 信息待标签构建结束并重新下载后补充。

- 2026-08-17：按“打测试包”规则保持版本 `1.0.20+25`，完成当前待发布改动的 Windows、Android 通用版和 Android DiLink 三端测试打包。除本日已记录的 DiLink 性能限制、当前会话日志清理、缓存删除竞态保护和播放停滞直连兜底外，本轮修复桌面音量悬浮层在父组件构建期间同步刷新 Overlay 导致的 Flutter build scope 断言，改为帧结束后仅刷新仍有效的 Overlay；同步校正更新测试对当前 Dart URI 空查询参数的错误假设、将远端更新夹具设为高于当前版本，并明确 Windows 55% 音量初始化平台。`flutter analyze --no-pub` 无问题，完整 `flutter test --no-pub` 160 项全部通过。Windows 首次未加载 VS 开发环境导致标准头文件缺失，随后通过 Visual Studio Developer PowerShell x64 环境完成 CMake/Ninja Release 与 `TEST_MODE` NSIS 构建；安装器包含 24 个 payload 文件、payload 大小 `49,824,740` 字节，runner ProductVersion/FileVersion 均为 `1.0.20+25`，临时 `nsis-payload` 已清理。检测到用户安装版 `D:\Programs\Zmusic\zmusic.exe`（PID 33900）正在运行且正常响应，为避免中断当前播放未执行独立 runner 烟测。Android 两包均为 `versionName=1.0.20`、`versionCode=25`、最低 API 24、389 个 ZIP 条目，仅含 `arm64-v8a` / `armeabi-v7a` 和压缩 JNI 原生库，不含 x86/x86_64 或 `just_audio` 并通过 16 KB zipalign；通用版 target 36/V2，DiLink 使用 Android Studio JBR 21 和显式 `-PzmusicDiLinkCompatibility=true --no-daemon` 构建为 target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。最终测试包：`dist/zmusic-windows-x64.exe` 大小 `16,659,483` 字节、MD5 `C2BEB59CFA58B8CD0BF7D1DC2AC1544F`、SHA256 `08C15CFD2DAD21595775297DA0100EF39731AEC6965BFD1C721459BDE5388D08`；`dist/zmusic-android.apk` 大小 `23,975,231` 字节、MD5 `B483EAE4233C78A4C0BF3911B653F2DE`、SHA256 `E7B8C2DF3042D678C2682C8B5F7A336D4EC5B90660B48333E3B410499FAFC7D9`；`dist/zmusic-android-dilink.apk` 大小 `24,515,128` 字节、MD5 `95058EA31795B815C30250A8ECF24E5F`、SHA256 `FBB1770FC8BF6C9D1A0E3F6AA39D0001A81F2D8703FE3B1FC846988BAB00F695`。未生成或覆盖更新清单，`dist/update.json` SHA256 保持 `326EA57D1D66959E2F5C7006FB3E422DFFF9056EFB4360D18FAF1B723586098F` 不变。

- 2026-08-17：根据用户提供的完整运行日志修复大量歌曲长时间缓冲、几乎无法播放的问题。日志显示缓存代理的源站请求连续 30 秒超时，而启动恢复只要看到代理仍处于下载状态就不断延后；代理最终失败后每次恢复又重新创建相同缓存代理，导致单首歌曲以分钟为单位重复失败。播放器启动窗口调整为 20 秒，缓存代理在窗口内仍未进入可播放状态时取消当前代理并改走原始音频流，后续有界恢复继续保持直连，不再返回缓存循环；缓存完整时仍优先恢复为本地文件，后续重新播放也仍会正常尝试边播边缓存。代理暴露实际下载字节数，恢复日志现在区分代理收到的字节数、重新生成 Navidrome 流地址并提交 libmpv 直连、直连已开始播放以及直连最终失败，后续可以明确判断代理异常和服务端流不可用。同步扩大 `isCompletedAudioCacheFile` 的文件系统竞态保护，并在完成缓存校验后文件被并发清理时退回未缓存路径，避免旧缓存删除竞态中断自动切歌。仅执行 Dart 格式化和差异检查，按普通功能修改规则未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.20+25`。

- 2026-08-17：降低 DiLink 长时间前台播放时的持续 CPU/GPU 与图片内存占用。启动时通过现有 Android 更新通道识别 DiLink 兼容构建，仅对该构建把 Flutter 内存图片缓存限制为最多 128 项、24 MB，并停止手机底部播放栏封面的持续旋转；普通 Android、iOS、Windows 和 macOS 视觉效果不变。同时修复手机底部播放栏未显示进度条却仍订阅播放位置、随位置事件反复整行重建的问题，手机端改为只在播放状态或曲目信息变化时重建，平板和桌面端需要进度条的刷新逻辑保持不变。仅执行 Dart 格式化和差异检查，按普通功能修改规则未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.20+25`。

- 2026-08-17：日志改为只保留当前应用进程的运行记录。`AppLogger.initialize` 在每次启动时等待已有写任务结束，随后删除当前日志及全部轮转日志并清空内存日志；文件暂时无法删除时尝试截断，清理失败也不会阻止应用启动。运行期间继续沿用现有日志级别、内存上限和文件轮转，查看、导出、手动清理行为不变；未运行测试或 `flutter analyze`、未打包，版本保持 `1.0.20+25`。

- 2026-08-17：发现 GitHub Actions 五端 Release 中的 Windows 与两个 Android 产物和 2026-08-15 本机产物字节不同，为避免用户上传 GitHub Release 文件后 `update.json` 校验失败，将 `dist` 中五个安装包全部替换为 GitHub `v1.0.20` 最终产物并统一更新清单。GitHub Windows 文件大小 `16,660,684` 字节、MD5 `9D908537E002A069846239AF1208F69C`、SHA256 `ED073C7333CC63B9158CF5996E5BD4337B244EF5027B85FC0ED505684ACCCF0F`；Android 通用版大小 `23,965,664` 字节、MD5 `839441D10A270B4CB8EB9AB39F033DA4`、SHA256 `D0C940639741DF7C7683D7E54F0CF794CE8D164EF869AEA3AF85150DB42E4A0A`；DiLink 大小 `23,957,443` 字节、MD5 `D42D8F02FD55CA415A833DB47B423C5D`、SHA256 `1CECAF13C510213365EACB68BE1DBE0A6308A898A6555B95B46F0DA7C51E450B`。五个平台节点均指向文件服务器 `1.0.20` 目录并使用对应 SHA256 前缀版本指纹；未修改应用代码、未运行测试或 `flutter analyze`、未重新打包，版本保持 `1.0.20+25`。

- 2026-08-17：为 `1.0.20+25` 的 `dist/update.json` 补充 `macos` 和 `ios` 平台节点。从 GitHub Release `v1.0.20` 下载对应最终产物后计算实际文件信息：`zmusic-macos.dmg` 大小 `32,907,719` 字节、MD5 `58B398E8D0CD16C0D817E8C2C77F39D7`、SHA256 `C9D583247F86943160A4362247CD49ADADF0E8764DA15D4A2CB5EC5C02E54C84`；`zmusic-ios-unsigned.ipa` 大小 `11,071,825` 字节、MD5 `7E0C81C35E69E6D4B3200C5709A0A97C`、SHA256 `DE816383CF9A0F399C0FCF3AEEAA922B3ABC7399E8A68E175E3E72784D9BAA78`。两个节点均使用文件服务器 `1.0.20` 目录和 SHA256 前缀版本指纹，清单现包含五个平台；未修改应用代码、未运行测试或 `flutter analyze`、未重新打包，版本保持 `1.0.20+25`。

- 2026-08-17：将 `1.0.20+25` 源码提交到 GitHub，发布源码提交为 `b933c03`（`Release Zmusic 1.0.20`），推送 `main` 和带注释标签 `v1.0.20`，触发 GitHub Actions `Build five-platform release`（run `31989945793`）。Windows installer、Android APK、Android DiLink APK、macOS DMG、iOS unsigned IPA 及最终 `Publish GitHub release` 六个任务全部成功；GitHub Release 地址为 `https://github.com/zlcece/zmiusc/releases/tag/v1.0.20`。Release 已确认包含 5 个产物：`zmusic-windows-x64.exe`（`16,660,684` 字节）、`zmusic-android.apk`（`23,965,664` 字节）、`zmusic-android-dilink.apk`（`23,957,443` 字节）、`zmusic-macos.dmg`（`32,907,719` 字节）和 `zmusic-ios-unsigned.ipa`（`11,071,825` 字节）。版本保持 `1.0.20+25`，本轮未再次修改发布标签或升版。

- 2026-08-15：按正式“打包”规则发布 `1.0.20+25`，同步更新 `pubspec.yaml`、Windows 生成配置、测试默认 `PackageInfo` 和 GitHub Actions 五端产物/Release 版本引用，并生成分平台 `dist/update.json`；更新内容包含 Subsonic 启动握手/只读请求重试、Android/DiLink 媒体键兼容和移动端 100% 内部音量。Android 首次构建暴露车机媒体键代码编译错误，按 Android API 做最小修复：将误写的 `KEYCODE_HEADSETOOK` 改为 `KEYCODE_HEADSETHOOK`，并将 `MediaSession.Callback.onMediaButtonEvent` 参数改为框架要求的非空 `Intent`，随后重新构建通过。按正式打包规则未运行 `flutter analyze` 和测试。Windows 通过 Visual Studio Build Tools 2022 x64 的 CMake/Ninja 完成 Release 编译，runner ProductVersion/FileVersion 为 `1.0.20+25`，`build/windows/app.so` 与 runner `data/app.so` SHA256 均为 `8CE43C09E3FABC23265D2FAFEF75FCC1188EFE8D9B056B393B51E08B4DA5B581`；NSIS 正式安装器包含 24 个 payload 文件、payload 大小 `49,824,740` 字节，临时 payload 已清理。Android 两包均为 `versionName=1.0.20`、`versionCode=25`、最低 API 24、389 个 ZIP 条目，只包含 `arm64-v8a` / `armeabi-v7a` 两份压缩 `libmpv.so`，不含 x86/x86_64 或 `just_audio` 并通过 16 KB zipalign；通用版 target 36/V2，DiLink 使用 Android Studio JBR 21 和显式 `-PzmusicDiLinkCompatibility=true --no-daemon` 构建为 target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。最终正式包：`dist/zmusic-windows-x64.exe` 大小 `16,666,372` 字节、MD5 `41875C7ED59A27C81F1C0BF45D3007E2`、SHA256 `63182D7308AB98210C97A88B68F3C7150F702C7DD58CE9A152ACB8DB0768924B`；`dist/zmusic-android.apk` 大小 `23,968,500` 字节、MD5 `CBEC11C5EA37A1335B31E0BE9DBD1370`、SHA256 `F100C8265D2A9B5CCD8E1974E56C5247B0D3AC6AD2541A85AECCE73054FB48D6`；`dist/zmusic-android-dilink.apk` 大小 `24,508,397` 字节、MD5 `ACEFC5CFF5EB06D48F19E41587D84DFF`、SHA256 `E22781EBE2CD205B61C4391D4E6D31DC7CBAF143FD1914451DF2D63FAEFBBE53`。`dist/update.json` 三个平台节点的版本、大小、MD5、SHA256 和带版本指纹的下载地址均与最终文件匹配；清单大小 `2,402` 字节、MD5 `027BC6110FECFACBC9CE13DDEACF08A7`、SHA256 `499A254105DCF22ED521DD77ACE7DF7831F97F7009F94F5B05B18AE945D66D43`。

- 2026-08-10：将 100% 内部音量策略从 DiLink 扩展到全部移动端。普通 Android、DiLink、iPhone 和 iPad 启动时均把播放器内部音量设为 100%，避免无软件音量控件的移动端在相同系统媒体音量下比其他播放器明显偏小；Windows 与 macOS 继续默认 55% 并保留软件内调节。移除音量初始化对 DiLink 更新通道的依赖，解码、音频焦点和系统媒体音量不变。仅执行 Dart 格式化和代码差异检查，按普通功能修改规则未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.19+24`。

- 2026-08-10：修复 DiLink 车机方向盘播放/暂停键无效及输出音量明显偏小。Android 媒体控制现在显式识别 `KEYCODE_MEDIA_PLAY_PAUSE`、`HEADSETOOK`、独立播放/暂停、停止和上/下一首，组合键直接向 Dart 播放器派发 `playPause`；同一映射同时接入 `MediaSession.Callback.onMediaButtonEvent` 与前台 `MainActivity.dispatchKeyEvent`，兼容标准媒体会话和车机 ROM 直接向 Activity 派发硬件键两种路径，并只在首次按下时执行命令以防按下/抬起或长按重复触发。DiLink 构建通过现有原生分发通道识别，启动时内部播放器音量改为 100%，车机媒体音量仍由系统控制；Windows、普通 Android、iOS 和 macOS 保持既有 55% 默认值。仅执行 Dart 格式化和代码差异检查，按普通功能修改规则未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.19+24`。

- 2026-08-07：修复首页并发加载时偶发 `HandshakeException: Connection terminated during handshake` 导致全部板块保持空数据且顶部直接显示英文异常的问题。Subsonic 默认 HTTP 客户端现在限制同一服务器最多 4 路连接，降低应用启动时集中建立 TLS 连接的压力；仅对 `ping`、`search3` 和 `get*` 等幂等只读请求，在临时握手/Socket/Client 连接中断或 HTTP 408、429、5xx 时进行两次带随机错峰的重试，写操作不自动重试以避免重复提交。重试最终失败后保留原始异常到日志，并向界面返回中文的安全连接、证书或服务器连接提示。仅执行 Dart 格式化和代码差异检查，按普通功能修改规则未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.19+24`。

- 2026-07-31：正式发布 `1.0.19+24`。Windows 与 macOS 共用歌曲列表改为首次左键单击只选中并显示选中底色，同一首再次单击或双击才播放；歌曲行右侧播放按钮仍单击直接播放，Android、iOS 和平板歌曲行仍保持单击直接播放。公开非本人歌单继续禁止编辑、删除和移除，但歌曲右键/长按菜单不再受来源歌单管理权限限制，可将 Subsonic 歌曲加入当前账号自己的可编辑歌单。应用更新清单和安装包请求均覆盖追加实时毫秒 `v` 参数并发送 `Cache-Control/Pragma: no-cache`；清单强制要求合法 MD5，Windows 安装包下载落盘后流式计算 MD5，仅一致才启动安装，失败会删除文件。版本同步更新 `pubspec.yaml`、Windows 生成配置、测试默认 `PackageInfo` 和 GitHub 五端发布工作流，并修正 iOS 工作流遗留的内部构建号断言。按正式打包规则未运行 `flutter analyze` 和测试。Windows 通过 Visual Studio Build Tools 2022 x64 的 CMake/Ninja 完成 Release 编译，runner ProductVersion/FileVersion 为 `1.0.19+24`，`build/windows/app.so` 与 runner `data/app.so` SHA256 均为 `6D59301D2FFA211AEF6DC61C264B18C890011748608959E4CBEF70A631072BFD`；NSIS 正式安装器包含 24 个 payload 文件、payload 大小 `49,824,740` 字节，临时 payload 已清理。Android 通用版为 target 36/V2；首次用 Flutter 环境变量构建 DiLink 未使 Gradle 属性生效，经 APK 检查发现后未发布该错误产物，改用 Android Studio JBR 21 和 Gradle `-PzmusicDiLinkCompatibility=true --no-daemon` 重构建，最终 DiLink 为 target 29/V1-only。两包均为 `versionName=1.0.19`、`versionCode=24`、最低 API 24、389 个 ZIP 条目，仅含 `arm64-v8a` / `armeabi-v7a` 两份压缩 `libmpv.so`（合计 `5,610,999` 字节），不含 x86/x86_64 或 `just_audio`，通过 16 KB zipalign且证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。最终正式包：`dist/zmusic-windows-x64.exe` 大小 `16,662,453` 字节、MD5 `88FF5A17F8B0252EA9150D71D4E94DA2`、SHA256 `9145ACEA2AE8CC80605AECD6F6EAB6A20C758ED21E47A33953D9B7BE752BD1FB`；`dist/zmusic-android.apk` 大小 `23,963,575` 字节、MD5 `2C5B999A3099720529B6E75B88644A88`、SHA256 `10644CB28D32CF08100EB939B4789D6A8D38177C4B65AC7AE3355A47A0AEE6BC`；`dist/zmusic-android-dilink.apk` 大小 `24,503,482` 字节、MD5 `6243B6EB0E62747B54361FBB97985D81`、SHA256 `AB957422FFD1889B4F41B4D5784C884BE9C0D0A38F079FD91FE21E39CAEC78DF`。生成分平台 `dist/update.json`，三个下载地址均带版本指纹参数，节点大小和双哈希均与最终文件匹配；清单大小 `2,355` 字节、MD5 `517FC4814BB17F57A439BA2787F8079C`、SHA256 `65E2CF2FA27D5E3A3901F27A87F934E20C0537830DDE80D35A33AE729F0EFC27`。macOS 交互源码已包含本轮改动，但当前 Windows 主机按固定规则未生成 macOS 包。

- 2026-07-31：完善应用内更新防旧包机制，版本保持 `1.0.18+23`。`AppUpdateService` 获取 `update.json` 和下载平台安装包时都会保留原查询参数并将 `v` 覆盖为当前毫秒时间戳，同时发送 `Cache-Control: no-cache` 与 `Pragma: no-cache`，避免清单或同名安装包命中 CDN/中间缓存。更新清单现在必须提供合法的 32 位十六进制 MD5；下载文件关闭写入句柄后按文件流计算 MD5，仅校验一致才返回给 Windows 安装启动流程，校验失败会报告期望值与实际值并删除错误文件，避免启动旧包或损坏包。同步调整现有更新清单解析和下载测试夹具及请求断言，但按普通功能修改规则仅执行 Dart 格式化和差异检查，未运行测试、未运行 `flutter analyze`、未打包。

- 2026-07-31：排查应用内更新仍启动旧 Windows 安装器的问题。客户端 `downloadUpdate` 每次都会用 `openWrite` 覆盖系统临时目录中的同名文件，当前版本也尚未读取或校验清单 MD5，因此并非本地缓存根据旧 MD5 选择了旧包。公网无查询参数的下载 URL 实测命中 CDN 旧对象（`Cache-Control: max-age=14400`、`Age: 2324`），返回大小 `16,660,509` 字节、MD5 `32B0094CD91D00F20B4DC2CD8769EBED`、SHA256 `CA5EED96EC9B69E5CF244524C03FAADA63CCC026CE2E25C33D927A3B3EC29EEA`；追加随机查询参数后返回最新安装器，且与本地包 SHA256 `25234DEE518320EC9CD84BFC04B72F25552D6B11D11927FB2C3632793B123B7D` 一致。公网 `update.json` 同时存在旧大小/MD5与新 SHA256 混用。已将本地 `dist/update.json` 的 Windows 下载地址增加版本指纹 `?v=1.0.18-23-25234DEE`，并保留正确大小和双哈希，上传后可绕过旧 CDN 缓存；新清单大小 `3,579` 字节、MD5 `A512BA357116C859303D2160D56D0243`、SHA256 `023ECB6BE19FC7F4B7C203490C4C76C9E2693AE53D8A7B0A5AB8EB59196627B5`。本次未修改应用代码、未重新打包、未运行测试或 `flutter analyze`，版本保持 `1.0.18+23`。

- 2026-07-31：按用户明确要求保持 `1.0.18+23` 修复并重打正式包。Windows NSIS 覆盖安装关闭逻辑移除 `taskkill /T`，改为 `taskkill /F /IM zmusic.exe`，避免由应用内更新流程启动的安装器作为子进程被一并结束；测试包与正式包仍会在首次清理和文件占用重试时主动关闭播放器主进程。重新生成非 `TEST_MODE` 的 Windows 安装器，包含 24 个 payload 文件、payload 大小 `49,824,740` 字节，安装器大小 `16,661,458` 字节、MD5 `7BE3AFCF1930180143305454B8D2782A`、SHA256 `25234DEE518320EC9CD84BFC04B72F25552D6B11D11927FB2C3632793B123B7D`；临时 `nsis-payload` 已清理。Flutter/Android 代码未变化，因此保留现有同版本 Android 通用包（`23,965,891` 字节，MD5 `E3B87143568F62116A8796B7B671C3AD`，SHA256 `06B49D877700E6AC7D6442B51BD63E53AE0BAB7933A86FFB38B5E09DDDCE92C0`）和 DiLink 包（`23,957,683` 字节，MD5 `2058F0BDBD76F883EBB912006A462DAA`，SHA256 `0F2ED1667CA152D9F4AA7310C05F12BDAEBB532517C58DC5291EB109A0E5CD70`）。重新生成 `dist/update.json` 的 Windows 文件信息和安装器修复说明，三个平台节点均校验匹配最终文件；清单大小 `3,558` 字节、MD5 `4E2756F0469122A01598ED2DF48C4181`、SHA256 `D587756C0D440E3F0D46EFB75D3E9D050914D759CAD4BB000788C04AD1CBC929`。按正式打包规则未运行 `flutter analyze` 和测试。

- 2026-07-31：修复从 Zmusic 应用内启动更新安装器时安装程序被一并结束的问题。根因是安装器使用 `taskkill /F /T /IM zmusic.exe`，其中 `/T` 会结束 `zmusic.exe` 启动的全部子进程，安装器由应用更新流程启动时正是其子进程；现在改为 `taskkill /F /IM zmusic.exe`，只强制结束播放器本身，不再结束安装器，覆盖安装的文件占用重试逻辑保持不变。按普通功能修改规则仅执行差异检查，未运行测试、未运行 `flutter analyze`、未重新打包，版本保持 `1.0.18+23`。

- 2026-07-31：按正式“打包”规则发布 `1.0.18+23`，同步更新 `pubspec.yaml`、Windows 生成配置、测试包信息和 GitHub Actions 五端产物/Release 版本引用；按规则未运行 `flutter analyze` 和测试。Windows 通过 Visual Studio Build Tools 2022 x64 的 CMake/Ninja 完成 Release 构建，复制最新 `app.so` 到 runner 后生成非 `TEST_MODE` 的 NSIS 正式安装器，覆盖安装前会自动执行 `taskkill /F /T` 关闭运行中的 Zmusic；安装器包含 24 个 payload 文件、payload 大小 `49,824,740` 字节，runner ProductVersion/FileVersion 为 `1.0.18+23`，runner 与 payload 使用的 `app.so` SHA256 均为 `73ADCE95CE60355C86DE49BDED47EB5CAE018F4B68653349C333184D1E49C263`，临时 `nsis-payload` 已清理。Android 两包均为 `versionName=1.0.18`、`versionCode=23`、最低 API 24、389 个 ZIP 条目，仅含 `arm64-v8a` / `armeabi-v7a` 两份压缩 `libmpv.so`，不含 x86/x86_64 或 `just_audio` 并通过 16 KB zipalign；通用版 target 36/V2，DiLink target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。最终正式包：`dist/zmusic-windows-x64.exe` 大小 `16,660,509` 字节、MD5 `32B0094CD91D00F20B4DC2CD8769EBED`、SHA256 `CA5EED96EC9B69E5CF244524C03FAADA63CCC026CE2E25C33D927A3B3EC29EEA`；`dist/zmusic-android.apk` 大小 `23,965,891` 字节、MD5 `E3B87143568F62116A8796B7B671C3AD`、SHA256 `06B49D877700E6AC7D6442B51BD63E53AE0BAB7933A86FFB38B5E09DDDCE92C0`；`dist/zmusic-android-dilink.apk` 大小 `23,957,683` 字节、MD5 `2058F0BDBD76F883EBB912006A462DAA`、SHA256 `0F2ED1667CA152D9F4AA7310C05F12BDAEBB532517C58DC5291EB109A0E5CD70`。已按 Windows、Android、Android DiLink 分平台生成 `dist/update.json`，所有节点的大小和哈希均与最终文件匹配；清单大小 `3,549` 字节、MD5 `F094438F0A9FA8D96CF53A651452C14D`、SHA256 `6968050FFCC917A76B8287436E530AD75FC328619ECA23E5E2FDA309853CFCC4`。

- 2026-07-31：修复 Windows 测试安装包覆盖安装时无法自动退出正在播放的 Zmusic。根因是 NSIS `TEST_MODE` 分支跳过了 `CloseRunningZmusic`，导致安装器只尝试删除仍被运行进程占用的 DLL 并进入等待循环；现在测试包与正式包安装前都会执行 `taskkill /F /T /IM zmusic.exe` 关闭主进程及进程树，文件仍被占用时点击“重试”会再次执行关闭，提示文案不再错误声称应用已经退出。`TEST_MODE` 仍只隔离注册表、快捷方式和完成页启动等系统副作用。按普通功能修改规则仅执行差异检查，未运行测试、未运行 `flutter analyze`、未重新打包，版本保持 `1.0.17+22`。

- 2026-07-31：按“打测试包”规则为弱网超时、缓存占用直连兜底、相同缓存标识预取跳过和实际出声后再上报正在播放的修复生成三端测试包，版本保持 `1.0.17+22`，未生成或覆盖正式 `update.json`。首次 `flutter analyze --no-pub` 发现本轮替换后遗留的 `_deleteCacheFileFamily` 未使用，移除该孤立函数后复跑无问题；完整 `flutter test --no-pub` 160 项全部通过。Windows 通过 Visual Studio Build Tools 2022 x64 环境完成 CMake/Ninja Release 与 NSIS 测试安装器构建，安装器包含 24 个 payload 文件、payload 大小 `49,824,740` 字节，runner ProductVersion/FileVersion 为 `1.0.17+22`，最新 `build/windows/app.so` 与 runner `data/app.so` SHA256 均为 `73ADCE95CE60355C86DE49BDED47EB5CAE018F4B68653349C333184D1E49C263`，临时 `nsis-payload` 已清理；检测到已安装版 `D:\Programs\Zmusic\zmusic.exe`（PID 48516）正在运行且响应正常，为避免中断用户现场播放未强制关闭做新 runner 烟测。Android 两包均为 `versionName=1.0.17`、`versionCode=22`、最低 API 24、389 个 ZIP 条目、仅含 `arm64-v8a` / `armeabi-v7a` 两份压缩 `libmpv.so`，不含 x86/x86_64 或 `just_audio` 并通过 16 KB zipalign；通用版 target 36/V2，DiLink target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。最终测试包：`dist/zmusic-windows-x64.exe` 大小 `16,651,307` 字节、MD5 `686F0E6FA540D1A5C6BA63C5BB42EDC9`、SHA256 `6C7116B36EAD3035EE48D4D3260EBB93375DA55185AF5BC5B91A3F554A1286DF`；`dist/zmusic-android.apk` 大小 `23,965,898` 字节、MD5 `592993542B5732A3EA52179CB4A79ADB`、SHA256 `0B16A6B27F4F0622DDFBADB2D24091351DBE4F02B98438455AF23F972769C53C`；`dist/zmusic-android-dilink.apk` 大小 `23,957,682` 字节、MD5 `5C56427F253B84F05AE33985855A220F`、SHA256 `EE64B8ED3362D7FC5F018B283E305BB9BC4F51D6EB28C9E57CEFA5B4E047D4B2`。`dist/update.json` 保持大小 `2,347` 字节、修改时间 `2026-07-25 13:39:00 +08:00`、MD5 `950F194CCC5B1E55115D7AA01F731004`、SHA256 `0B2B215E1BAD2D37920DDC67588586C0B5AA941B80C141BD8B01A825A8E185D5` 不变。

- 2026-07-31：依据 13:30 之后的运行日志继续修复弱网播放停滞：音频源首包超时由 6 秒放宽到 30 秒、连续无数据超时由 5 秒放宽到 20 秒，重试次数由 3 次收敛为 2 次，避免弱网连接反复在数据到达前被主动断开；下一首与当前歌曲使用相同缓存标识时不再预取，残缺缓存文件被播放器或系统占用、无法安全删除时自动退回原始音频流直连，本次不写缓存但继续播放，避免 Windows `PathAccessException` 阻断播放；Navidrome“正在播放”上报延后到播放位置实际大于 0 后，播放器仅进入 `playing` 状态但尚未出声时不再提前上报。按普通功能修改规则仅执行格式化和差异检查，未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.17+22`。

- 2026-07-31：按“应先构建测试包”的最新要求完成播放/缓存修复版三端测试打包，版本保持 `1.0.17+22`，未生成或覆盖正式 `update.json`。`flutter analyze --no-pub` 最终无问题；首次完整测试有 3 项旧缓存兼容断言与新的长度完成标记规则冲突，更新为“历史无长度标记必须失效、有效缓存用正式完成函数写入字节数”后，完整 `flutter test --no-pub` 160 项全部通过。Windows 通过 Visual Studio Build Tools 2022 x64 环境完成 CMake/Ninja Release 与 NSIS 构建，安装器包含 24 个 payload 文件、payload 大小 `49,824,740` 字节，runner ProductVersion/FileVersion 均为 `1.0.17+22`，runner 与 payload `app.so` SHA256 均为 `7B7CE3CF81590D1ADC66422E483E19FAC3360DFC9940D6B5714F58EE8410F719`，临时 payload 已由脚本清理；检测到已安装版 `D:\Programs\Zmusic\zmusic.exe`（PID 31876）正在运行且响应正常，为避免中断用户现场验证未强制关闭做新 runner 烟测。Android 两包均为 `versionName=1.0.17`、`versionCode=22`、最低 API 24、389 个 ZIP 条目、仅含 `arm64-v8a` / `armeabi-v7a` 两份压缩 `libmpv.so`，不含 x86/x86_64 或 `just_audio` 并通过 16 KB zipalign；通用版 target 36/V2，DiLink target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。最终测试包：`dist/zmusic-windows-x64.exe` 大小 `16,659,413` 字节、MD5 `F9DF5E774F0A823CC9EA993CA1C206FA`、SHA256 `9F8DEE59B2A1F1DF44BAA027E1C5F00D120F2810D90216EC255C1C06CA4B5783`；`dist/zmusic-android.apk` 大小 `23,965,107` 字节、MD5 `1B911A496D4BD5FE5A0D13D376E3E94C`、SHA256 `D744EB61175386050E7EBBA4C3D861625CBB250FAA1F3763B48046FACCCCA2A7`；`dist/zmusic-android-dilink.apk` 大小 `24,505,000` 字节、MD5 `C52718E7CE8FC7C0340FA6BD0EC70C37`、SHA256 `A82FAF50ACCB8BFEAA8E39A27D5E6A010A39D7B5C866FD0815F2B82FABB0C753`。`dist/update.json` 保持大小 `2,347` 字节、修改时间 `2026-07-25 13:39:00 +08:00`、MD5 `950F194CCC5B1E55115D7AA01F731004`、SHA256 `0B2B215E1BAD2D37920DDC67588586C0B5AA941B80C141BD8B01A825A8E185D5` 不变。

- 2026-07-31：根据“特定歌曲连续恢复后仍无法播放、切歌后其他歌曲可播放”的运行日志继续收敛播放与缓存兜底，版本保持 `1.0.17+22` 并改按测试包流程验证，不提前发布正式版。完整音频缓存现在只认可显式 `.complete` 标记，标记内保存完成时的文件字节数且必须与当前文件长度完全一致；历史空标记、只有 `.mime` 的文件以及后续被截断的缓存都会在下次解析时失效并重新下载，避免中断残片被永久复用。下一首预取必须等代理下载 Future 完成、写入句柄关闭后才写完成标记。当前歌曲代理仍在正常下载或重试时，12 秒启动监控只重新布防而不再主动打断；代理最终失败会立即进入现有有界恢复。原生 `media_kit` 打开改为直接调用 `NativePlayer.open(..., synchronized: false)`，避免旧打开占用内部串行锁后阻塞替换播放器的新请求，Web/非原生实现继续走公共 `Player.open`。调试日志中的两类 Subsonic `scrobble` 现在分别标记为“正在播放”和“播放计次”，明确同一歌曲正常会先上报正在播放、达到 30 秒或一半时长后再上报一次计次。首次完整测试有 3 项旧缓存兼容断言失败，原因是测试仍要求无标记或空标记缓存被复用；已将其更新为“历史无长度标记必须失效、有效缓存通过正式完成函数写入长度标记”。此前仅完成一次 `1.0.18+23` Windows runner 编译，未生成该版本安装包、未生成 Android 包且未覆盖 `update.json`，随后已恢复正式版本号；本条最终验证与测试包信息待本轮完成后补充。

- 2026-07-30：按“从代码层面修复并允许服务器超时/错误但必须有兜底”的要求完成播放启动链路收敛和日志入口会话解锁，并按“打测试包”规则保持版本 `1.0.17+22` 生成三端测试包。音频缓存代理除原有响应头 6 秒超时、Socket/5xx/断流重试外，新增正文连续 5 秒无数据超时，连接和空闲时间均受限；已写入部分会使用 HTTP Range 从当前字节续传，远端范围请求也受响应头和正文超时约束。播放器快速切歌继续只允许最新请求生效并可替换被旧 `open` 占用的 `media_kit` 实例；每首歌启动允许两次完整恢复，恢复时重新解析流地址、重建代理/播放器并优先切换已完成本地缓存，三次均失败时主动停止并退出缓冲状态，保留当前歌曲供手动重试，不再无限转圈。设置页日志板块默认隐藏，连续点击登录用户名 5 次后弹出密码框，只有输入 `rizhi` 才在当前应用进程中解锁；错误密码不显示，解锁状态仅保存在 `AppController` 内存中，可跨音乐/设置页签保持但不写入配置，退出进程后必须重新五连击验证。新增正文停顿断点续传、快速启动只提交最终歌曲、完整缓存回退、恢复次数耗尽后停止缓冲、暂停不误恢复以及日志错误/正确密码和跨页签会话保持回归。`flutter analyze --no-pub` 无问题，完整 `flutter test --no-pub` 160 项全部通过。Windows 通过 Visual Studio Build Tools 2022 x64 环境完成 CMake/Ninja Release 与 NSIS 构建，安装器包含 24 个 payload 文件、payload 大小 `49,824,740` 字节，runner ProductVersion/FileVersion 均为 `1.0.17+22`，runner 与 payload `app.so` SHA256 均为 `93A53F3457C01FE3622B392EB85F7E30876BE0279D2866D68EE02B4FF2291524`，临时 `nsis-payload` 已清理；已安装版 `D:\Programs\Zmusic\zmusic.exe`（PID 26592）仍正常响应，新 runner 启动由单实例机制转交后退出，未强制中断用户程序。Android 两包均为 `versionName=1.0.17`、`versionCode=22`、最低 API 24、389 个 ZIP 条目、仅含 `arm64-v8a` / `armeabi-v7a` 和压缩 `libmpv.so`，不含 x86/x86_64 或 `just_audio` 并通过 16 KB zipalign；通用版 target 36/V2，DiLink target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。最终测试包：`dist/zmusic-windows-x64.exe` 大小 `16,661,755` 字节、MD5 `37AB70452F43D5EC2ED44005A0744C1D`、SHA256 `54D1E6DBD5B9BF2C607CAE1069DEB35247D61BB54D12BB73D29BEF2A71ABCCD9`；`dist/zmusic-android.apk` 大小 `23,964,726` 字节、MD5 `EACEB32CB245EF364CEC39D0FC4396A8`、SHA256 `3A8EF6916A052887F6ADE909F237B46B66A9454F45F367DE7FB89E807D2F5F1E`；`dist/zmusic-android-dilink.apk` 大小 `24,504,619` 字节、MD5 `8B8BB5B724D7D90337BDC11A43ABC0F2`、SHA256 `A542AC68FD5ABFDE9DE7413ED36C7A241688F4C9E8053CFD0E0D635B64934E1A`。`dist/update.json` 保持大小 `2,347` 字节、修改时间 `2026-07-25 13:39:00 +08:00`、MD5 `950F194CCC5B1E55115D7AA01F731004`、SHA256 `0B2B215E1BAD2D37920DDC67588586C0B5AA941B80C141BD8B01A825A8E185D5` 不变。

- 2026-07-30：继续根据“缓存已到 100% 但仍停在 00:00”的运行日志修复启动恢复竞态，版本保持 `1.0.17+22`。播放器现在为当前串流缓存保存独立的完成确认 Future：缓存进度到 100% 后先等待代理下载 Future 返回和文件写入句柄关闭，再写入完成标记；启动监控准备重试同一首歌时，如果缓存已满，必须先等待该确认 Future，再重新解析播放地址。这样完整缓存不会在完成标记尚未落盘的瞬间被 `AudioCacheManager` 误判为残缺文件并删除，恢复播放会直接复用本地缓存。截图中的 Windows 任务栏封面 20 秒超时属于独立图片缓存警告，不参与音频打开链路。按普通功能修改规则仅执行 `dart format` 和 `git diff --check`，未新增或运行测试、未运行 `flutter analyze`、未打包。

- 2026-07-30：按“打测试包”规则保持版本 `1.0.17+22`，完成缓存代理取消异常和原生播放器旧 `open` 阻塞修复后的三端测试打包。`flutter analyze --no-pub` 无问题，完整 `flutter test --no-pub` 154 项全部通过。Windows 通过 Visual Studio `VsDevCmd.bat -arch=x64` 环境完成 CMake/Ninja Release 与 NSIS 构建，安装器包含 24 个 payload 文件、payload 大小 `49,791,972` 字节，runner ProductVersion/FileVersion 均为 `1.0.17+22`，runner 与 payload 的 `app.so` SHA256 均为 `FB4924AF260DED6F6D660DA6C35CC7E0ED6EB08DF4C063BC7A96308F0DF7C866`，临时 `nsis-payload` 已清理；检测到安装版 `D:\Programs\Zmusic\zmusic.exe`（PID 43320）正在运行，为避免中断用户当前播放测试未做独立 runner 存活烟测。Android 两包均为 `versionName=1.0.17`、`versionCode=22`、最低 API 24、仅含 `arm64-v8a` / `armeabi-v7a`、389 个 ZIP 条目、两份压缩 `libmpv.so` 合计 `5,610,999` 字节，不含 x86/x86_64 或 `just_audio` 并通过 16 KB zipalign；通用版 target 36/V2，DiLink target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。最终测试包：`dist/zmusic-windows-x64.exe` 大小 `16,655,850` 字节、MD5 `912382DF0098B1904A18E65805D320FE`、SHA256 `8F411623D2BC3B7E101D6D75B4B993E495829B5FB067F772C48CA627E8169D5C`；`dist/zmusic-android.apk` 大小 `23,953,460` 字节、MD5 `BCC004593A60C396DE0F99ADEF02FBAF`、SHA256 `075D5EC0863D28322904B66DC4AEA0E2CB243C2FDEE660B7AB19C42BB134A295`；`dist/zmusic-android-dilink.apk` 大小 `24,493,351` 字节、MD5 `1C5F867FDA517EC62E8B06B52376F099`、SHA256 `B4BC64649F9CB2D267B5C68E5B43A6D89074CD4A8431A80D71E43AF0E4B123BE`。`dist/update.json` 保持大小 `2,347` 字节、修改时间 `2026-07-25 13:39:00 +08:00`、MD5 `950F194CCC5B1E55115D7AA01F731004`、SHA256 `0B2B215E1BAD2D37920DDC67588586C0B5AA941B80C141BD8B01A825A8E185D5` 不变。

- 2026-07-30：根据运行日志修复切歌后缓存完成但播放器持续停滞的问题，版本保持 `1.0.17+22`。`StreamingAudioCacheProxy` 现在会预先消费元数据 Future 的错误，代理在播放器首次请求前被正常取消时不再把 `Audio cache proxy canceled` 上报为未捕获异步异常；原生 `media_kit` 打开操作继续受 `PlayerController` 外层队列串行控制，但调用底层 `NativePlayer.open` 时不再等待可能被已停滞旧 `open` 持有的内部锁，使旧加载经 3 秒中断流程处理后，新歌曲可以实际提交给 mpv，而不会出现缓存已完成但播放内核仍未打开媒体的状态。改动同时适用于 Windows、Android、iOS 和 macOS 的原生播放器路径。按普通功能修改规则仅执行 `dart format` 和 `git diff --check`，未新增或运行测试、未运行 `flutter analyze`、未打包。

- 2026-07-30：按用户要求重新生成现有代码的三端测试包，版本保持 `1.0.17+22`，未修改业务代码，也未包含新报告的“大歌单首次播放停滞、切歌缓存完成后仍无法播放”问题修复。`flutter analyze --no-pub` 无问题，完整 `flutter test --no-pub` 154 项全部通过。Windows 通过 Visual Studio `VsDevCmd.bat -arch=x64` 环境完成 CMake/Ninja Release 与 NSIS 构建，安装器包含 24 个 payload 文件、payload 大小 `49,791,972` 字节，runner ProductVersion/FileVersion 均为 `1.0.17+22`，runner 与 payload 的 `app.so` SHA256 均为 `C0F040949E3B39A70D50B4939082D175C38FC5857ED3BBB29D98EFE40F88058B`，临时 `nsis-payload` 已清理。Android 两包均为 `versionName=1.0.17`、`versionCode=22`、最低 API 24、仅含 `arm64-v8a` / `armeabi-v7a`、389 个 ZIP 条目、两份压缩 `libmpv.so` 合计 `5,610,999` 字节，不含 x86/x86_64 或 `just_audio` 并通过 16 KB zipalign；通用版 target 36/V2，DiLink target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。最终测试包：`dist/zmusic-windows-x64.exe` 大小 `16,655,054` 字节、MD5 `BBFEFDEDD4B037319789F8F3BDF790BF`、SHA256 `FE23928B50CA24FA7639AE4AC37281499E45F5B953CB43082A0DD6A625CE8358`；`dist/zmusic-android.apk` 大小 `23,951,814` 字节、MD5 `5853F125C7979B28589B3C2E9CCDCF17`、SHA256 `1E84FE80ADCD4625329F57AF1D020623E7EFF82849844A1EF2F837C99D77D879`；`dist/zmusic-android-dilink.apk` 大小 `24,491,700` 字节、MD5 `9BB4C8C57EC064B20CB915FF7ECF910E`、SHA256 `463031A9751BD20405EE90F7A66F45936DD4E42B41F9D5FC1BE133413FE5E5F1`。`dist/update.json` 保持大小 `2,347` 字节、修改时间 `2026-07-25 13:39:00 +08:00`、MD5 `950F194CCC5B1E55115D7AA01F731004`、SHA256 `0B2B215E1BAD2D37920DDC67588586C0B5AA941B80C141BD8B01A825A8E185D5` 不变。

- 2026-07-30：按“打测试包”规则保持版本 `1.0.17+22`，完成全平台应用日志记录、日志级别设置和当前日志查看页，并生成 Windows、Android 通用版、Android DiLink 三端测试包。新增统一 `AppLogger`，默认仅记录错误，支持错误/警告/信息/调试四级持久化设置；接管 Flutter 框架、PlatformDispatcher 和未捕获异步异常，并为登录、曲库请求、Subsonic 请求、播放/缓存、桌面集成和系统媒体控制等关键链路补充结构化日志。日志按当前会话最多 1000 条保存在内存并实时展示，文件默认每个 2 MiB、保留 3 份滚动记录；设置页可切换级别、滚动查看、复制全部、桌面导出和确认后清空，查看时仅在用户停留于底部附近自动跟随。日志写入前会隐藏 Subsonic 认证查询参数、密码/Token 键值和 URL 基础认证信息。新增默认级别、级别筛选、敏感信息脱敏、设置持久化及设置页入口回归；同时验证此前待打包的歌单详情分页、同步完成进度、远程封面并发限制和播放切换恢复改动。`flutter analyze --no-pub` 无问题，完整 `flutter test --no-pub` 154 项全部通过。Windows 首次直接调用 CMake 因未载入 MSVC/Windows SDK 环境而失败，随后通过 `VsDevCmd.bat -arch=x64` 完成 CMake/Ninja Release 与 NSIS 构建；安装器包含 24 个 payload 文件、payload 大小 `49,791,972` 字节，runner ProductVersion/FileVersion 均为 `1.0.17+22`，runner 与 payload 的 `app.so` SHA256 均为 `C0F040949E3B39A70D50B4939082D175C38FC5857ED3BBB29D98EFE40F88058B`，临时 `nsis-payload` 已清理。检测到安装版 `D:\Programs\Zmusic\zmusic.exe`（PID 34060）正在运行，为避免中断用户程序未做独立 runner 存活烟测，新 runner 启动由单实例机制正常退出。Android 两包均为 `versionName=1.0.17`、`versionCode=22`、最低 API 24、仅含 `arm64-v8a` / `armeabi-v7a`、389 个 ZIP 条目、两份压缩 `libmpv.so` 合计 `5,610,999` 字节，不含 x86/x86_64 或 `just_audio` 并通过 16 KB zipalign；通用版 target 36/V2，DiLink target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。最终测试包：`dist/zmusic-windows-x64.exe` 大小 `16,655,054` 字节、MD5 `188A0722E2C7CBD58D9D8321A652BBBA`、SHA256 `FB18B7ED24ECFFB69319178A88E5FF802BCA52BD1DA33086E49F4D8B52BC4C06`；`dist/zmusic-android.apk` 大小 `23,951,814` 字节、MD5 `5853F125C7979B28589B3C2E9CCDCF17`、SHA256 `1E84FE80ADCD4625329F57AF1D020623E7EFF82849844A1EF2F837C99D77D879`；`dist/zmusic-android-dilink.apk` 大小 `24,491,700` 字节、MD5 `9BB4C8C57EC064B20CB915FF7ECF910E`、SHA256 `463031A9751BD20405EE90F7A66F45936DD4E42B41F9D5FC1BE133413FE5E5F1`。`dist/update.json` 保持大小 `2,347` 字节、修改时间 `2026-07-25 13:39:00 +08:00`、MD5 `950F194CCC5B1E55115D7AA01F731004`、SHA256 `0B2B215E1BAD2D37920DDC67588586C0B5AA941B80C141BD8B01A825A8E185D5` 不变。

- 2026-07-30：限制远程封面下载并发，版本保持 `1.0.17+22`。`ArtworkCacheManager` 现在为歌单、歌曲、专辑、歌手和播放栏等所有远程封面共用 4 个异步下载槽位，超出的封面请求按空闲槽位继续执行，不再因一页展示 50 首歌曲而同时发起全部下载；单个远程封面请求增加 20 秒超时并始终释放槽位，相同封面继续复用现有在途 Future 和本地缓存。按普通功能修改规则仅执行必要格式化和差异检查，未新增或运行测试、未运行 `flutter analyze`、未打包。

- 2026-07-30：修复大歌单加载和切歌后播放器永久卡住的问题，版本保持 `1.0.17+22`。远程歌单歌曲请求增加 45 秒上限，超时后通过现有 `whenComplete` 清除在途请求，使详情页结束无限加载且再次进入可以重新请求；播放器在中断已过期的 `media_kit.open` 时，将带 3 秒上限的中断 Future 设为新的音频操作队列起点，最新切歌不再继续排在永不完成的旧打开 Future 后面，因此切换歌单后完整缓存歌曲也能重新进入实际打开流程。按普通功能修改规则仅执行必要格式化和差异检查，未新增或运行测试、未运行 `flutter analyze`、未打包。

- 2026-07-30：为远程歌单详情页增加歌曲列表本地分页，默认每页 50 首，可快速切换为 25 / 50 / 100 / 200 首或输入自定义数量；分页栏提供上一页、下一页和最多 3 个动态页码快捷入口，窄屏时自动换行。分页仅影响界面展示：点击歌曲仍以完整歌单建立播放队列，单首移除和跨页批量移除继续使用歌曲在完整歌单中的真实索引。按普通功能修改规则仅做代码、必要格式化和协作文档变更，未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.17+22`。

- 2026-07-30：修复外部歌单同步完成后进度文案仍停留在“正在写入”的问题；同步结果和目标歌单列表更新完成后，现在会补发满进度的“同步完成”状态，使进度区与同步报告保持一致。按普通功能修改规则仅做代码、必要格式化和协作文档变更，未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.17+22`。

- 2026-07-30：按“打测试包”规则保持版本 `1.0.17+22`，完成标签管理多平台在线元数据、专辑封面显示和确认后一键写入相关代码的三端测试打包。`flutter analyze --no-pub` 无问题，完整 `flutter test --no-pub` 150 项全部通过。Windows 首次在未加载 Visual Studio 开发者环境的 PowerShell 中构建，因缺少 Windows SDK/C++ include 路径失败；随后通过 `VsDevCmd.bat -arch=x64` 加载环境后完成 CMake/Ninja Release 与 NSIS 构建，安装器包含 24 个 payload 文件、payload 大小 `49,562,596` 字节，runner ProductVersion/FileVersion 均为 `1.0.17+22`，runner 与 payload 的 `app.so` SHA256 均为 `FEB269AE8A8C585A01BCEE3E608C5D7156643905CB6E7DCEC8D6FFF6AB181011`，临时 `nsis-payload` 已清理；检测到安装版 `D:\Programs\Zmusic\zmusic.exe`（PID 41712）正在运行，为避免打断用户程序未做独立 runner 存活烟测。Android 两包均为 `versionName=1.0.17`、`versionCode=22`、最低 API 24、仅含 `arm64-v8a` / `armeabi-v7a`、389 个 ZIP 条目、两份压缩 `libmpv.so` 合计 `5,610,999` 字节，不含 x86/x86_64 或 `just_audio` 并通过 16 KB zipalign；通用版 target 36/V2，DiLink target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。最终测试包：`dist/zmusic-windows-x64.exe` 大小 `16,586,059` 字节、MD5 `7C2F32A2D439E73227186FBF5CEADFB0`、SHA256 `F4BC86ABDEA6F479A9D1851F643B3AAF83FDE96971395DDA40A4C2A13C2EFFC5`；`dist/zmusic-android.apk` 大小 `23,746,087` 字节、MD5 `FBDB342C0B5F87EA633100A16F6C24C4`、SHA256 `46F0C7A9BC3A8AE84CF8D049083595737612794EA9CA98EDBAC5341D3A16DB6E`；`dist/zmusic-android-dilink.apk` 大小 `24,286,345` 字节、MD5 `F2C1393D06AF739A8E812AC9070E62E8`、SHA256 `BB9B94BC7D714C723276A5B7F38FC74F3BEA41CB926D8CD774FC9AC92FDA4544`。`dist/update.json` 保持大小 `2,347` 字节、修改时间 `2026-07-25 13:39:00 +08:00`、MD5 `950F194CCC5B1E55115D7AA01F731004`、SHA256 `0B2B215E1BAD2D37920DDC67588586C0B5AA941B80C141BD8B01A825A8E185D5` 不变。

- 2026-07-30：补全标签管理专辑封面与直接写入流程。右侧文件头部现在显示当前内嵌封面，MusicBrainz、酷狗、酷我、QQ音乐和网易云候选结果显示各自专辑封面；网易云搜索切换到可返回 `picUrl` 的 cloudsearch 响应，并为其余来源映射对应封面地址。新增“一键替换”操作和二次确认，确认后下载并校验最大 10 MB 的 JPEG/PNG/WebP 封面，将候选非空标题、歌手、专辑、流派、年份、曲目号及封面一次写入文件，再重新读取元数据刷新界面；候选空字段保留本地原值，封面下载失败时仍保存文本字段并显示失败原因。扩展现有 `saveAudioMetadata` 可选封面参数，复用 `audio_metadata_reader` 的 `setPictures` 写入正面封面，不增加依赖。按普通功能修改规则仅执行必要格式化和差异检查，未新增或运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.17+22`。

- 2026-07-30：扩展 Windows/macOS 标签管理的在线元数据搜索，在原 MusicBrainz 基础上新增酷狗音乐、酷我音乐、QQ音乐和网易云音乐来源下拉选择，并统一映射标题、歌手、专辑、流派、年份和曲目号候选字段；保持“搜索/填入只更新表单，手动保存后才写入文件”的既有边界，空字段不会覆盖本地标签。服务层分别适配四个平台当前搜索响应，包含请求超时、HTTP/业务状态检查和结构化解析；酷我接口的单引号响应先转换为 JSON 再解析，平台失败只反馈当前搜索错误。执行 `dart format lib/src/musicbrainz_metadata.dart lib/src/metadata_admin.dart`；按普通功能修改规则未新增或运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.17+22`。

- 2026-07-27：撤回 Windows 主窗口标题固定为 `Zmusic` 的行为，恢复为有歌曲时显示当前歌名、无歌曲时显示 `Zmusic`，不影响 SMTC 和任务栏缩略图媒体信息；Windows 与 macOS 每次启动都使用现有最小尺寸 `1088 × 680`，仍允许向更大尺寸调整。macOS 关闭操作改由现有 `window_manager` 链路最小化到 Dock，应用继续运行，点击 Dock 图标可恢复窗口，系统退出命令仍正常退出；移除会覆盖窗口插件代理的原生 `NSWindowDelegate` 方案。按普通功能修改规则仅做代码、必要格式化和协作文档变更，未运行测试、未运行 `flutter analyze`、未打包。

- 2026-07-27：按“打测试包”规则保持版本 `1.0.17+22`，完成歌词手动预览交互和 Windows、Android 通用版、Android DiLink 三端测试包。时间轴歌词的滚轮、触摸拖动和直接点击现在只更新预览选中句并在该句右侧显示独立播放按钮，不再自动执行跳播；只有点击按钮才跳到对应时间，3 秒未确认会自动清除预览并重新跟随真实播放位置。更新 widget 回归覆盖滚轮不跳播、直接点击不跳播、按钮确认跳播、触摸拖动和 3 秒自动回位；`dart format` 通过，`flutter analyze --no-pub` 无问题，完整 `flutter test --no-pub` 150 项全部通过。Windows 在 Visual Studio x64 环境完成 CMake/Ninja Release 与 NSIS 构建，安装器包含 24 个 payload 文件、payload 大小 `49,512,932` 字节，runner ProductVersion/FileVersion 为 `1.0.17+22`，安装器 ProductVersion/FileVersion 为 `1.0.17` / `1.0.17.22`，runner 与 payload 的 `app.so` SHA256 均为 `CE2A1C0EED9846FF74B9804E8311499F8C7768A85A4D653B4481D9C19B3AF346`，临时 `nsis-payload` 已清理；检测到安装版 `D:\Programs\Zmusic\zmusic.exe`（PID 31176）正在运行，为避免打断用户程序未做独立 runner 存活烟测。Android 两包均为 `versionName=1.0.17`、`versionCode=22`、最低 API 24、仅含 `arm64-v8a` / `armeabi-v7a`、389 个 ZIP 条目、两份压缩 `libmpv.so` 合计 `5,610,999` 字节，不含 x86/x86_64 或 `just_audio` 并通过 16KB zipalign；通用版 target 36/V2，DiLink target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。最终测试包：`dist/zmusic-windows-x64.exe` 大小 `16,574,211` 字节、MD5 `56229A548E4B702440E162F5DD133586`、SHA256 `6DF5B7D67B8FEF6D1B4BE5CE5243D4E24A34513CA5A1834B4BC4A98124B6E38D`；`dist/zmusic-android.apk` 大小 `23,714,779` 字节、MD5 `A88B425E6363B60E3BF629519532B17E`、SHA256 `3B4B2D0D54F9C263FAC35FDC17355184F0546833F55AA543EBBAE0ABC2C91800`；`dist/zmusic-android-dilink.apk` 大小 `24,255,153` 字节、MD5 `8382C81CB45024CD8969C213B1EE74EB`、SHA256 `F23AB743D75DBE8525686FEB0B783EAD7BE3D0434DD0BB8557CB53B18FDE102B`。`dist/update.json` 保持大小 `2,347` 字节、修改时间 `2026-07-25 13:39:00 +08:00`、MD5 `950F194CCC5B1E55115D7AA01F731004`、SHA256 `0B2B215E1BAD2D37920DDC67588586C0B5AA941B80C141BD8B01A825A8E185D5` 不变。

- 2026-07-25：修正 iOS 内部构建号校验后，重新移动 `v1.0.17` 标签触发 GitHub Actions 运行 `30152785974`；Windows、Android、Android DiLink、macOS 和 iOS 五端构建及最终 Release 作业全部成功，`1.0.17+22` 五端发布已完成。

- 2026-07-25：首次推送 `v1.0.17` 后 GitHub Actions 运行 `30152369271` 的 Windows、Android、Android DiLink 和 macOS 构建成功，iOS 也已完成编译，但 `Package unsigned IPA` 仍校验旧内部构建号 `21`，与当前 `1.0.17+22` 不符而失败，Release 作业因此跳过。修正 `.github/workflows/release.yml` 的 iOS `CFBundleVersion` 校验为 `22`，应用代码和版本号均不变；随后移动同名标签重新触发完整五端发布。

- 2026-07-25：正式发布 `1.0.17+22`。版本从 `1.0.16+21` 升级，同步更新 `pubspec.yaml`、测试默认 `PackageInfo` 和更新测试中的下一版本、MusicBrainz User-Agent、Android/Windows 生成配置及 GitHub Actions 五端发布名称与 iOS 版本校验；按正式“打包”规则未运行 `flutter analyze` 或 `flutter test`。本次跨平台更新包含：播放中清除缓存时保护当前串流、已完整缓存的当前本地播放文件和下一首预取文件，继续清理其余缓存并容忍被占用文件；退出登录后保留最近一次成功登录的账号密码用于登录页回填但不自动登录。Windows 额外固定窗口标题为 `Zmusic`，SMTC、任务栏封面和媒体按钮继续显示当前歌曲信息。Windows 在 Visual Studio x64 环境完成 CMake/Ninja Release 与 NSIS 构建，安装器包含 24 个 payload 文件、payload 大小 `49,512,932` 字节，runner ProductVersion/FileVersion 为 `1.0.17+22`，安装器 ProductVersion/FileVersion 为 `1.0.17` / `1.0.17.22`，runner 与 payload 的 `app.so` SHA256 均为 `8913F778D0A069F64E7BDA7014C978405FEE31644315372B3AAD4E07019DD5F2`，临时 `nsis-payload` 已清理；检测到用户安装版 `D:\Programs\Zmusic\zmusic.exe`（PID 44756）正在播放，为避免中断用户程序未做独立 runner 存活烟测。Android 通用版和 DiLink 均构建成功，两个 APK 均为 `versionName=1.0.17`、`versionCode=22`、最低 API 24、仅含 `arm64-v8a` / `armeabi-v7a`、389 个 ZIP 条目、两份压缩 `libmpv.so` 合计 `5,610,999` 字节，不含 x86/x86_64 或 `just_audio` 并通过 16KB zipalign；通用版 target 36/V2，DiLink target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。重新生成 UTF-8 `dist/update.json`，Windows 独有标题修复仅写入 Windows 节点，跨平台缓存和登录改进写入三个节点，文件名、大小、MD5、SHA256 均与最终包匹配；清单大小 `2,347` 字节、MD5 `950F194CCC5B1E55115D7AA01F731004`、SHA256 `0B2B215E1BAD2D37920DDC67588586C0B5AA941B80C141BD8B01A825A8E185D5`。最终正式包：`dist/zmusic-windows-x64.exe` 大小 `16,574,244` 字节、MD5 `6E24A822A6B657E9010D9671EA4F1EA2`、SHA256 `4D1FB306B4F4A118D1A0E575A9A2F1ABE8D7B792F0B338CE7A477D1808676185`；`dist/zmusic-android.apk` 大小 `23,713,436` 字节、MD5 `AB0DBEF3F6A29308CF28B32B62D9A24F`、SHA256 `5D317B311ADC855E60AFFC039A95275B99DBF36983F9929EC6760A14D3956595`；`dist/zmusic-android-dilink.apk` 大小 `24,253,804` 字节、MD5 `B9DBDA0F8F0089B8CC961973B752B25F`、SHA256 `627F6D89F6E6623B49572C8377A90F585F03A8A4F741029D2EC11567BA6725E7`。

- 2026-07-24：按用户收窄后的需求完成缓存清理、固定标题和登录输入保留，并生成三端测试包，版本保持 `1.0.16+21`；明确未加入内外网地址或设置页地址切换。音频缓存不再递归删除整个目录，改为与自动限额清理共用逐缓存文件族的容错删除：清理时由播放器提供当前串流、已完整缓存的当前本地播放文件和下一首预取文件路径，保留正在使用的音频及其 `.part/.mime/.complete` 辅助文件，同时继续删除其他缓存，完成后按实际剩余大小提示；最旧缓存文件被 Windows 占用时也不再中断整轮限额清理。登录成功后把最近账号和密码分别写入普通配置与安全存储，退出登录仍清空活动服务器、播放会话和推荐数据，但登录页自动回填最近账号密码且不会自动登录；原有隐藏自定义 URL 功能保持不变。Windows 原生窗口标题固定为 `Zmusic`，SMTC、任务栏封面和媒体按钮仍继续显示当前歌曲信息。新增活动缓存保护和登录输入保留回归；首次完整测试发现两个更新测试仍把当前 `1.0.16` 当远端新版本、每日推荐测试仍要求旧版严格相邻歌手规则，仅校正测试数据和断言，未改相应业务逻辑。最终 `flutter analyze --no-pub` 无问题，完整 `flutter test --no-pub` 150 项全部通过。Windows 在 Visual Studio x64 环境完成 CMake/Ninja Release 与 NSIS 构建，安装器包含 24 个 payload 文件、payload 大小 `49,512,932` 字节，runner ProductVersion/FileVersion 为 `1.0.16+21`，安装器 ProductVersion/FileVersion 为 `1.0.16` / `1.0.16.21`，runner 与 payload 的 `app.so` SHA256 均为 `044021DF7B2D125EE86D6F23215FF0B9CC167036DBF199AFD0D1AB28B343A98C`，临时 `nsis-payload` 已清理；检测到用户安装版 `D:\Programs\Zmusic\zmusic.exe`（PID 44756）正在播放，为避免中断用户程序未做独立 runner 存活烟测。Android 通用版和 DiLink 均构建成功，两个 APK 均为 `versionName=1.0.16`、`versionCode=21`、最低 API 24、仅含 `arm64-v8a` / `armeabi-v7a`、389 个 ZIP 条目和压缩 JNI 原生库，不含 x86/x86_64 或 `just_audio`；通用版 target 36/V2，DiLink target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。最终测试包：`dist/zmusic-windows-x64.exe` 大小 `16,572,287` 字节、MD5 `58CF23D8F738B4746CD37AC4E3156E69`、SHA256 `8E38CD61CE6F4B2FF9E42DA9F251A2B75730F8BBFCF2DA860D8E68C645B58259`；`dist/zmusic-android.apk` 大小 `23,713,408` 字节、MD5 `A73FF304FC050ABE2BE7AE08B2566D63`、SHA256 `555CA882AA745DA66F84FE117F5D2FBF6D8B806CB8E01A0DD989002CF2E6303A`；`dist/zmusic-android-dilink.apk` 大小 `24,253,788` 字节、MD5 `64C6FFD63D936466DDCB9889D0574D7D`、SHA256 `AEC12C83424B1D32B64537D32617F767FFB3C83160B6A43C80C0D8F4FE43CC6C`。`dist/update.json` 保持大小 `2,126` 字节、修改时间 `2026-07-24 18:39:10 +08:00`、MD5 `2BC5C1672B5004C82D9454CFB04425D5`、SHA256 `380FBB8F8FCB4E97B1406C8645885E1F97D6E7D2BC5EF1D4DAE970929557B21A` 不变。

- 2026-07-24：正式发布 `1.0.16+21`。版本从 `1.0.15+20` 升级，同步更新 `pubspec.yaml`、测试默认 `PackageInfo`、检查更新测试清单、MusicBrainz User-Agent、Android/Windows 生成配置及 GitHub Actions 五端发布名称与 iOS 构建号校验；按正式“打包”规则未运行 `flutter analyze` 或 `flutter test`。跨平台更新包含每日推荐动态收藏配额、3/5 天冷却和 7 天历史去重；Windows 额外包含 SMTC 蓝牙恢复播放修复、任务栏封面及上一首/播放/下一首按钮、托盘播放/音量/退出面板，并移除托盘收藏和播放模式按钮。Windows 在 Visual Studio x64 环境完成 CMake/Ninja Release 与 NSIS 构建，安装器包含 24 个 payload 文件、payload 大小 `49,496,548` 字节，runner ProductVersion/FileVersion 为 `1.0.16+21`，安装器 ProductVersion/FileVersion 为 `1.0.16` / `1.0.16.21`，runner 与 payload 的 `app.so` SHA256 均为 `06D0BE782B32D64EAD7D6157F3B2966DE359BED55C3F83E827C5B7A6E4A67DC7`，临时 `nsis-payload` 已清理；检测到已安装 Zmusic（PID 45364）正在运行且正常响应，为避免中断用户程序未做独立 runner 烟测。Android 通用版通过 Flutter Release 构建，DiLink 使用 Android Studio JBR 和显式 `-PzmusicDiLinkCompatibility=true` 的独立 Gradle 进程构建；两个 APK 均为 `versionName=1.0.16`、`versionCode=21`、最低 API 24、仅含 `arm64-v8a` / `armeabi-v7a`、389 个 ZIP 条目并通过 16KB zipalign，两份压缩 `libmpv.so` 合计均为 `5,610,999` 字节，不含 x86/x86_64 或 `just_audio`；通用版 target 36/V2，DiLink target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。重新生成 UTF-8 `dist/update.json`，分平台更新说明及三个节点的文件名、大小、MD5、SHA256 均与最终包匹配；清单大小 `2,126` 字节、MD5 `2BC5C1672B5004C82D9454CFB04425D5`、SHA256 `380FBB8F8FCB4E97B1406C8645885E1F97D6E7D2BC5EF1D4DAE970929557B21A`。最终正式包：`dist/zmusic-windows-x64.exe` 大小 `16,571,602` 字节、MD5 `187D8CC4097A33E87B304FE9B2E02798`、SHA256 `5A6EAB2280CF84E5A48B5BAFC3DEFA0289F71D559358985440A412483742C9B5`；`dist/zmusic-android.apk` 大小 `23,704,405` 字节、MD5 `1F7D057890EC30EA079619993BDE59A8`、SHA256 `44A13F14E1C55A2937675541AAD4B31EC9CE5827FE693D2460E5DDE9430A2BF2`；`dist/zmusic-android-dilink.apk` 大小 `24,244,783` 字节、MD5 `F418D254BB7C833D3D21BE6350AEC4BF`、SHA256 `60B97318B2D717F21F188CFBF50D03864897AAA819A82229B41295DB29004D96`。

- 2026-07-24：优化全平台每日推荐的重复控制，版本保持 `1.0.15+20`。直接收藏配额改为按收藏数量动态取 `1/2/4` 首，收藏歌手相关保持 8 首、最近/最多播放相关保持 9 首，随机探索自动占用剩余名额；直接收藏从相关、习惯和随机池中剔除，避免同一收藏歌曲绕过配额。每日推荐缓存新增最近 7 天历史，普通推荐冷却 3 天、直接收藏冷却 5 天；候选不足时优先按随机、相关、习惯、收藏补齐，仍不足才从最早历史开始逐日解除冷却。同一天“换一批”的结果会合并进当天历史；旧缓存可读取并迁移为普通历史，退出登录、切换账号或关闭入口继续清除全部推荐历史。按普通功能修改规则仅执行必要格式化、引用检查和差异检查，未运行测试、未运行 `flutter analyze`、未构建或打包。

- 2026-07-24：按截图要求移除 Windows 托盘播放面板歌名两侧的收藏和随机/顺序按钮，版本保持 `1.0.15+20`。歌名区域扩展到原两侧按钮占用的整行；同时删除仅服务于这两个托盘按钮的 Dart 状态监听、平台通道字段和命令，以及 Windows 原生状态字段、点击区域和绘制逻辑。主界面的收藏、播放模式和系统媒体键功能不受影响。按普通功能修改规则仅执行必要格式化、引用检查和差异检查，未运行测试、未运行 `flutter analyze`、未构建或打包。

- 2026-07-24：按“打测试包”规则重新生成三端测试包，版本保持 `1.0.15+20`。静态分析首次发现新增系统媒体控制构造参数可使用初始化形参，以及测试播放器记录字段错误覆盖非空 `volume` getter；仅将 5 个依赖改为初始化形参并把测试字段重命名为 `assignedVolume`，不改变运行逻辑。修正后 `flutter analyze --no-pub` 无问题，完整 `flutter test --no-pub` 149 项全部通过。Windows 在 Visual Studio x64 环境完成 CMake/Ninja Release，新增托盘播放面板和任务栏媒体控制原生代码实际通过 MSVC 编译与链接；runner 与 payload 的 `app.so` SHA256 均为 `2C258AE52D60343006911C8DEA2F8EA0BFE4603C1AD2998C43E48C23E17C110E`，NSIS 安装器包含 24 个 payload 文件、payload 大小 `49,481,700` 字节，runner ProductVersion/FileVersion 为 `1.0.15+20`，安装器 ProductVersion/FileVersion 为 `1.0.15` / `1.0.15.20`，临时 `nsis-payload` 已清理；使用独立临时数据目录启动 Release runner，进程存活 7 秒后正常结束烟测。Android 通用版通过 Flutter Release 构建，DiLink 使用 Android Studio JBR 和显式 `-PzmusicDiLinkCompatibility=true` 的独立 Gradle 进程构建；两个 APK 均为 `versionName=1.0.15`、`versionCode=20`、最低 API 24、仅含 `arm64-v8a` / `armeabi-v7a`、压缩 JNI 原生库并通过 16KB zipalign，不含 x86/x86_64 或 `just_audio`；通用版 target 36/V2，DiLink target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。本机未连接 Android 设备，未做真机安装。最终测试包：`dist/zmusic-windows-x64.exe` 大小 `16,565,859` 字节、MD5 `20FEF158823866EFB52A99AF3F3EE4CC`、SHA256 `E3C37A084C953E08069B6C3FC692FE3A3AE28FC49CAE23269E16B119331A1C95`；`dist/zmusic-android.apk` 大小 `23,689,182` 字节、MD5 `2FA76A708203FD07A66F83C0A8F43FBD`、SHA256 `29BD291D1E343A22812C2FDD2B10CE37101D695D2C9FF5FD5EB58AD4AE17C4BE`；`dist/zmusic-android-dilink.apk` 大小 `24,229,552` 字节、MD5 `B4AC721E071F2466CB174EBD02EC3544`、SHA256 `B86836891AFCD6959557503EED1B63182ADE9F50B956A3C832C2654886D31D5A`。`dist/update.json` 保持大小 `2,107` 字节、修改时间 `2026-07-22 14:42:51 +08:00`、MD5 `83AF299D47C2D07B4BCA2AF55AA16563`、SHA256 `B1C299D10F868C2D797826CE98A0D28B46AA9CE6C6880026C1E99366FDBD8E33` 不变。

- 2026-07-24：将 Windows 托盘右键普通文字菜单替换为原生自绘播放控制面板，版本保持 `1.0.15+20`。移除 Windows 右键菜单中的“显示窗口”和文字“播放/暂停”，新增独立 `WindowsTrayPlayerPopup`，按横向布局提供上一首、播放/暂停、下一首、收藏、当前歌名、随机/顺序切换、静音、音量滑块和退出；面板按系统明暗主题绘制、靠近托盘光标弹出并在失焦时隐藏。扩展现有系统媒体通道同步收藏、随机模式和音量状态，收藏复用 Navidrome 收藏接口，音量拖动同步播放器及主界面；非 Windows 平台继续使用原托盘菜单。按普通功能修改规则仅执行必要格式化、差异与引用检查，未运行测试、未运行 `flutter analyze`、未构建或打包。

- 2026-07-24：增强 Windows 任务栏悬浮播放控制，版本保持 `1.0.15+20`。复用现有系统媒体通道，在封面变化时通过 `ArtworkCacheManager` 解析本地缓存路径并传给 Windows Runner；原生端使用 DWM 自定义当前歌曲封面缩略图，通过 `ITaskbarList3` 增加上一首、播放/暂停、下一首按钮，按钮图标与启用状态随播放器实时更新，任务栏/窗口标题显示当前歌名，无歌曲时恢复 `Zmusic` 和应用图标。为避免动态标题破坏单实例唤醒，现有实例改为优先按固定窗口类查找；新增 WIC、COM 链接依赖。按普通功能修改规则仅执行 Dart 必要格式化、差异与引用检查，未运行测试、未运行 `flutter analyze`、未构建或打包。

- 2026-07-23：修复 Windows 蓝牙耳机线控能暂停但无法恢复播放的 SMTC 状态声明问题。`SystemMediaControls::Update` 不再在有歌曲时同时开放播放和暂停指令，改为播放中仅启用暂停、暂停中仅启用播放，使 Windows 与蓝牙耳机按当前状态正确派发恢复播放命令。核对 macOS 与 iOS 已使用同样的播放/暂停互斥启用逻辑；Android 通过独立 `MediaSession PlaybackState` 发布 `PLAYING/PAUSED` 并分开处理 `onPlay/onPause`，不存在该 Windows SMTC 问题，因此未改动其他平台。按普通功能修改规则未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.15+20`。

- 2026-07-22：正式发布 `1.0.15+20`。版本从 `1.0.14+19` 升级，同步更新 `pubspec.yaml`、测试默认 `PackageInfo`、检查更新测试的远端下一版本、MusicBrainz User-Agent、Windows 生成配置及 GitHub Actions 五端发布名称/版本验证；按正式“打包”规则未运行 `flutter analyze` 或 `flutter test`。本次跨平台更新包含搜索结果页保留搜索框与关键词并可直接二次搜索、专辑详情优先按音频元数据曲目号排序，以及移除用户可见的歌曲文件下载入口。Windows 在 Visual Studio x64 环境完成 CMake/Ninja Release 与 NSIS 构建，安装器包含 24 个 payload 文件、payload 大小 `49,459,172` 字节，runner ProductVersion/FileVersion 为 `1.0.15+20`，安装器 ProductVersion/FileVersion 为 `1.0.15` / `1.0.15.20`，runner 与 payload 的 `app.so` SHA256 均为 `85DE5BCFF4D1EBF4520C9F472EC427478451E87B87E17855FE227B02FA6136FB`，临时 `nsis-payload` 已清理。Android 通用版通过 Flutter Release 构建；DiLink 首次使用项目 `.gradle-home` 时因离线镜像缺失 Kotlin `2.2.0` API JAR 失败，改用通用版已验证的默认 Gradle 缓存、Android Studio JBR 21、`--no-daemon` 和显式 `-PzmusicDiLinkCompatibility=true` 后成功。两个 APK 均为 `versionName=1.0.15`、`versionCode=20`、最低 API 24、仅含 `arm64-v8a` / `armeabi-v7a`、389 个 ZIP 条目并通过 16KB zipalign，两份压缩 `libmpv.so` 合计均为 `5,610,999` 字节，不含 x86/x86_64 或 `just_audio`；通用版 target 36/V2，DiLink target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。本机未连接 Android 设备，未做真机安装。重新生成 `dist/update.json`，三个平台节点的文件名、大小、MD5、SHA256 均与最终包匹配；清单大小 `2,335` 字节、MD5 `BA50BE994DD9374FE402BECE8C0062F2`、SHA256 `4C9912BF1D10ECE9259DE2238D006E683221DC7C82C67377B153E1EAF71A4535`。最终正式包：`dist/zmusic-windows-x64.exe` 大小 `16,552,227` 字节、MD5 `3AE8768749CF2A2C57312B3BB81DF0E1`、SHA256 `E0B1989BA0AE3C55741FD9A8166F84C33191B25B5A3CD36C8887F9DE095A68AE`；`dist/zmusic-android.apk` 大小 `23,693,616` 字节、MD5 `378D53336A0DBB3D6AA3306B7555ADF7`、SHA256 `38305ECB9DCB6ED5913FFEFF2B52E03F10A37232BD39E756A0D163F2C54F1C92`；`dist/zmusic-android-dilink.apk` 大小 `24,233,988` 字节、MD5 `B012851E5356AAA48EDF6BF21E660EF5`、SHA256 `7441289BB7A2F4E9AF70B7CE5B501336BAF468D7DE124EEC24CFD0DC5F94882C`。

- 2026-07-22：搜索结果页复用音乐首页的搜索框、关键词控制器、分类选择、远程联想和提交逻辑，进入结果页后保留上次搜索关键词，可直接修改并再次搜索，版本保持 `1.0.14+19`；本次测试包同时包含此前尚未重新打包的歌曲下载功能移除和专辑曲目号排序。将原有“结果页不显示搜索框”测试改为验证关键词保留和二次搜索；`flutter analyze --no-pub` 无问题，完整测试首轮因搜索分类与结果页签会同时显示“歌曲/歌手/专辑”，旧断言仍要求文字只出现一次而失败，修正为存在性断言后第二轮 `flutter test --no-pub` 149 项全部通过。Windows 复用 `build/windows/x64-ninja`，在 Visual Studio x64 环境完成 CMake/Ninja Release 与 NSIS 构建，安装器包含 24 个 payload 文件、payload 大小 `49,459,172` 字节，runner ProductVersion/FileVersion 为 `1.0.14+19`，安装器 ProductVersion/FileVersion 为 `1.0.14` / `1.0.14.19`，runner 与 payload 的 `app.so` SHA256 均为 `D8227E9EEEA1B7A58FAA57805C1771128F1FE7A2D78BE408D720DCF959160503`，临时 `nsis-payload` 已清理；检测到已安装 Zmusic（PID 2076）正在运行且正常响应，新 runner 按单实例设计转交后以退出码 `0` 结束，为避免中断用户程序未做独立新进程存活烟测。Android 通用版通过 Flutter Release 构建，DiLink 使用 Android Studio JBR 21、显式 `-PzmusicDiLinkCompatibility=true` 和独立 Gradle 进程构建；两个 APK 均为 `versionName=1.0.14`、`versionCode=19`、最低 API 24、仅含 `arm64-v8a` / `armeabi-v7a`、389 个 ZIP 条目并通过 16KB zipalign，两份压缩 `libmpv.so` 合计均为 `5,610,999` 字节且不含 x86/x86_64 或 `just_audio`；通用版 target 36/V2，DiLink target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。ADB 未检测到连接设备，未做 Android 真机安装。最终测试包：`dist/zmusic-windows-x64.exe` 大小 `16,555,731` 字节、MD5 `989B38C0AB1B7D00CA0DF20C3EFBEE1D`、SHA256 `5FE8B9B8CE3F87B6DCA25A8CCAA48C58D642BCCB39B84A2EFD971486A2148CE5`；`dist/zmusic-android.apk` 大小 `23,693,660` 字节、MD5 `13293A58652C2224FCAE854D380D04B6`、SHA256 `EBC46740D294D8249B858DEE42E4E34EE02E33ABA0ABD74501A8B3538E6338D4`；`dist/zmusic-android-dilink.apk` 大小 `24,234,047` 字节、MD5 `480E0010B76C45F72668770AE232E0B4`、SHA256 `C0B036FCD58A8C78C22F44C7D18047B83610533AF27AA326CEE618BB2902BCCC`。`dist/update.json` 保持大小 `5,085` 字节、修改时间 `2026-07-20 19:21:25`、MD5 `0B5874DC7D82AFFC0E069E805442F412`、SHA256 `97E6A5FF728575637FE7A3B08A81EB8B4741771B20786BE740263C2AF6C982AD` 不变。

- 2026-07-22：全平台移除歌曲文件下载功能，版本保持 `1.0.14+19`。删除歌曲长按菜单中的“下载”入口、保存位置选择与错误提示流程，删除 `AppController.downloadTrack` 的本地复制/网络下载实现，并清理仅用于下载的音频 `XTypeGroup`、建议文件名、文件名清理和扩展名推断代码；Windows、Android 手机/HD、iOS 与 macOS 共用歌曲列表组件，因此所有平台同步移除。保留检查更新的安装包下载以及边播边缓存，两者不是用户可见的歌曲文件下载。更新现有长按菜单回归断言为不显示“下载”或“分享”；按普通功能修改规则仅执行必要格式化、无效引用/符号搜索和差异检查，未运行测试、未运行 `flutter analyze`、未打包；现有 `dist` 测试包为本次修改前产物，不包含此移除。

- 2026-07-22：专辑详情歌曲支持按元数据曲目号排序，版本保持 `1.0.14+19`。统一 `Track` 模型新增可序列化的可选 `trackNumber`，Navidrome/Subsonic 歌曲映射读取 `track` 字段，本地音频映射读取标签曲目号并兼容 `1/12` 格式；打开专辑详情后，存在有效曲目号时按数值升序排列，未编号歌曲排在后面并保持原有相对顺序，全部没有曲目号时完全保持服务端/本地原顺序；搜索、歌单和现有播放队列不参与该排序。按“打测试包”规则运行 `flutter analyze --no-pub` 无问题，完整 `flutter test --no-pub` 149 项全部通过；未新增测试。Windows 复用 `build/windows/x64-ninja`，首次命令因 CMake 未在 `PATH` 中未启动，第二次定位 VS CMake/Ninja 后因未注入 VC/Windows SDK 环境而缺失标准头文件，通过 `VsDevCmd.bat -arch=x64` 注入开发者环境后 CMake/Ninja Release 成功；同步 `app.so` 到 runner 后 SHA256 均为 `7EA96DEDBD7F7FBB5C5E72605437DA9ADC376A65AC41AA11B5A7A352BF6C1B41`，NSIS 安装器包含 24 个 payload 文件、payload 大小 `49,459,172` 字节，runner ProductVersion/FileVersion 为 `1.0.14+19`，安装器 ProductVersion/FileVersion 为 `1.0.14` / `1.0.14.19`，临时 `nsis-payload` 已清理。检测到用户安装目录 `D:\Programs\Zmusic\zmusic.exe`（PID 2076）正在运行，为避免中断用户程序未停止该实例，新 runner 启动后按单实例设计转交窗口并以退出码 `0` 结束，未做独立新进程存活烟测。Android 通用版首轮 Gradle Release 耗时约 657 秒后成功；首次 DiLink 尝试的 Gradle 环境变量被已有 daemon 忽略，包体复核发现仍为 target 36/V2 后未交付，改用 Android Studio JBR 和显式 `gradlew assembleRelease -PzmusicDiLinkCompatibility=true` 重建为 target 29/V1-only。两个 APK 均为 `versionName=1.0.14`、`versionCode=19`、最低 API 24、仅含 `arm64-v8a` / `armeabi-v7a`、389 个 ZIP 条目，通过 16KB zipalign，两份压缩 `libmpv.so` 合计 `5,610,999` 字节，不含 x86/x86_64 或 `just_audio`；通用版 target 36/V2，DiLink target 29/V1-only，签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。ADB 未检测到连接设备，未做 Android 真机安装。最终测试包：`dist/zmusic-windows-x64.exe` 大小 `16,563,318` 字节、MD5 `0A9D648A295356AE386E19D6A9B39718`、SHA256 `F0D597E50791F0F7B082832429A1818ED41D5733E96A912FCC9D846FAE0E0BB9`；`dist/zmusic-android.apk` 大小 `23,699,032` 字节、MD5 `1DF2F8A1574D734A6C14E18D90368E26`、SHA256 `7092EA2F2E442758D953D017DA501BD411E97DC241C3B94CB855992159CC482C`；`dist/zmusic-android-dilink.apk` 大小 `24,239,412` 字节、MD5 `CEA0F51A1CA121D8D214710B1ED3A669`、SHA256 `545242D5CE823F25E0ABE59B2770D9AAE9E04069D7B941B503743472F2BC9CEB`。`dist/update.json` 保持大小 `5,085` 字节、修改时间 `2026-07-20 19:21:25`、MD5 `0B5874DC7D82AFFC0E069E805442F412`、SHA256 `97E6A5FF728575637FE7A3B08A81EB8B4741771B20786BE740263C2AF6C982AD` 不变。

- 2026-07-21：修复 macOS 登录、启动恢复与桌面播放交互，版本保持 `1.0.14+19`。针对 GitHub Actions 临时签名直发 DMG，macOS 安全存储禁用 Data Protection Keychain 并继续使用标准 Keychain，避免密码写入/读取失败导致重启需要重新登录；登录流程改为先完成账号与密码持久化，再提交内存登录状态，修复凭据保存失败时“看似已登录但不拉取首页数据”的半登录状态；桌面退出前等待播放会话写入完成，保证下次启动恢复队列和当前歌曲。Windows 与 macOS 统一使用 `1088×680` 的 16:10 最小窗口尺寸，只限制最小宽高而不锁定拖动比例；Windows 原生 `WM_GETMINMAXINFO`、macOS `contentMinSize` 和 `window_manager` 三处与各自平台同步约束。桌面端纵向音量浮层从 `126px` 加长到 `196px`。全平台带时间轴歌词增加居中选中，支持点击歌词行、触摸/鼠标拖动和桌面鼠标滚轮结束后跳转到对应播放进度，手动滚动期间暂停自动跟随。新增凭据持久化失败、16:10 最小窗口、桌面音量浮层和歌词三种交互的回归测试；`flutter analyze --no-pub` 无问题，完整 `flutter test --no-pub` 149 项全部通过。已删除旧 GitHub Release 及旧 `v1.0.14` 标签，新标签指向功能提交 `62e49e6ec483bb504aedbe6e232d9989c412d955`；GitHub Actions 运行 `29838286278` 的 Windows、Android、Android DiLink、macOS、iOS 和 Release 六个作业全部成功，新 Release 5 个资产均返回 HTTP 200 并已下载回本机 `dist`。最终包：`zmusic-windows-x64.exe` 大小 `16,559,931` 字节、SHA256 `E12079FACA03BBF40A3194FB79687649022F56313FA95C2CE28A92BFEA2FB34F`；`zmusic-android.apk` 大小 `23,688,587` 字节、SHA256 `FBD52A0ECAFC7C6A2105ECC6827D632EF6C674FB5EB11864C9B32F70EEAF0C1F`；`zmusic-android-dilink.apk` 大小 `23,680,378` 字节、SHA256 `580FCCC16E1C8C7ECFFAF1A260E4BC398355D74A09577ED65A844BE9C76A3D75`；`zmusic-macos.dmg` 大小 `32,600,247` 字节、SHA256 `C6832708D130C4690285C5147007A6BFE61837ECDD95C9CD56C938B0E4B606F0`；`zmusic-ios-unsigned.ipa` 大小 `10,871,851` 字节、SHA256 `9913F0F068820F376043138EFD64F53361F283EEED5949457794C1CFF077B1FA`。IPA 复核为 `1.0.14+19`、Bundle ID `com.zmusic.app`、后台模式含 `audio`、主 App 不含签名或 Provisioning Profile；Windows 安装器 ProductVersion/FileVersion 为 `1.0.14` / `1.0.14.19`。

- 2026-07-21：补齐 iOS 手机端播放与发布基础并发布 `1.0.14+19`。新增 `media_kit_libs_ios_audio 1.1.4`、标准 iOS Podfile 及 Pods xcconfig 引用，避免仅提供 podspec 的原生音频库在干净 Runner 中缺失；iOS 原生端使用 `AVAudioSession` playback 类别和后台 audio mode，并通过现有 `com.zmusic.app/media_session` 通道接入锁屏、耳机及控制中心的播放/暂停/上一首/下一首、正在播放元数据、音频中断恢复和耳机拔出暂停；Dart 系统媒体控制和更新平台映射增加 iOS。GitHub Actions 扩展为五端，macOS Runner 实编 iPhoneOS Release 后移除主 App 签名内容并封装 `zmusic-ios-unsigned.ipa`，校验版本 `1.0.14+19`、IPA 结构后上传 Artifact 与 Release；该包必须二次自签，不能直接安装。新增 iOS 平台映射、系统媒体支持和 iPhone 窄屏设置回归测试；`flutter pub get` 通过，首次分析发现未跟踪的旧 `build/macos-scaffold-20260721` 模板测试引用无效 `MyApp`，确认其位于项目生成目录且无跟踪文件后移出工作区，最终 `flutter analyze --no-pub` 无问题；完整 `flutter test --no-pub` 首次发现新增 iPhone widget 测试未在 invariant 检查前恢复平台覆盖值，修正清理顺序后 145 项全部通过。GitHub Actions 运行 `29807127059` 的 Windows、Android、Android DiLink、macOS、iOS 和 Release 六个作业全部成功，iOS 依赖校验、Swift/Xcode 编译、Flutter iPhoneOS AOT、unsigned IPA 封装及 Artifact 上传均通过；Release 下载地址返回 HTTP 200，`zmusic-ios-unsigned.ipa` 大小 `10,867,534` 字节、SHA256 `EA85B8971489FF54B53FD97978A6B0194195B207AFA1B51AD54211FC49EBA3CD`。下载回本机复核 128 个 ZIP 条目，包含 `Payload/Runner.app/Runner`、`Frameworks/App.framework/App` 和 `Info.plist`，Bundle ID 为 `com.zmusic.app`、后台模式含 `audio`，主 App 不含 `_CodeSignature` 或 `embedded.mobileprovision`。

- 2026-07-21：完成 GitHub Actions 四端正式发布链路并发布 `1.0.14+19`。`.github/workflows/release.yml` 固定使用 Flutter `3.44.4`，由 Windows、Ubuntu、macOS Runner 并行生成 Windows NSIS 安装器、Android 通用 APK、Android DiLink APK 和临时签名但未公证的 macOS DMG；补交 Android Gradle Wrapper，设置仓库补入 Flutter 官方引擎 Maven 地址，Windows Runner 增加 MSVC 14.51 实验性协程兼容宏，macOS 移除两个拖放协议方法上无效的 `override`，失败日志会输出关键 Checks 注解，最终由固定提交的 `softprops/action-gh-release v2.5.0` 更新 GitHub Release。GitHub Actions 第六轮运行 `29803968198` 五个作业全部成功，`v1.0.14` 指向提交 `4e35aed7794610478664373a7ee68b7581177031`；Release 四个资产均返回 HTTP 200：`zmusic-windows-x64.exe` 大小 `16,553,138` 字节、SHA256 `C1082C67885DF2A2217A3633861B0EB18F30A74BD5E40B876266AA3AE39D22D2`，`zmusic-android.apk` 大小 `23,686,302` 字节、SHA256 `CF933B7130906C6839CB534C33D5C724CB4F3FC50961AB466E6EF1C7FB7DE605`，`zmusic-android-dilink.apk` 大小 `23,678,092` 字节、SHA256 `2B3678EB688811333873834E3F472DB1D92631B1FE78874323F53D66C54D6ACA`，`zmusic-macos.dmg` 大小 `32,579,955` 字节、SHA256 `1640C381A32B7F1560BCE3DE110BB1F4DBC5B750D0538D241767CA27C985E86E`。未在本地运行测试或 `flutter analyze`；四端编译、平台封装、Artifact 上传和 Release Assets 发布均由 GitHub Actions 实际验证。

- 2026-07-21：准备首次推送 GitHub 源码仓库，扩展根目录 `.gitignore`，排除项目内 Flutter SDK、Gradle Home/临时运行目录、Android 构建报告、烟测与工具运行数据、LocalLow 本地状态及 `dist` 发布产物；保留源码、四个平台工程、测试、文档和必要资源进入版本控制。发布二进制继续通过 GitHub Release/Actions Artifacts 管理，避免把大文件固化进 Git 历史。仅进行 Git 暂存边界与敏感信息检查，未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.14+19`。

- 2026-07-21：补齐 macOS 端工程与核心平台代码，版本保持 `1.0.14+19`。基于当前 Flutter SDK 生成 `macos/` Runner，最低系统版本 10.15，应用名 `Zmusic`、Bundle ID `com.zmusic.app`；新增 `media_kit_libs_macos_audio 1.1.4`、标准 CocoaPods Podfile 和插件注册，加入与 Windows 一致的 AppIcon 及明暗模式模板菜单栏图标。原生 Runner 通过现有 `com.zmusic.app/media_session` 通道接入 `MPNowPlayingInfoCenter` / `MPRemoteCommandCenter`，支持播放、暂停、上一首、下一首及系统正在播放元数据；通过 `com.zmusic.app/file_drop` 接入 Finder 拖放、音频文件“打开方式”和启动早期文件暂存。设置页向 macOS 开放本地标签管理，更新检查可读取 `macos` 清单节点；Runner 声明受支持音频扩展、HTTP 自建服务访问和 Keychain Sharing。为兼容持久本地路径及递归标签管理，官网直接分发配置不启用 App Sandbox，并在文档中明确未来 Mac App Store 需改安全作用域书签。执行 `flutter pub get`、Dart 格式化、plist/entitlements/JSON 结构解析、插件注册与图标尺寸检查；按普通功能修改规则未运行 `flutter analyze`、未运行测试、未打包。当前 Windows 主机没有 Xcode/Swift/macOS SDK，无法实际编译或运行 macOS Runner，必须在 Mac 上完成首次 `flutter build macos --release`、签名和真机烟测。

- 2026-07-20：根目录 `.gitignore` 新增 `/.gradle-home/caches/`，忽略项目本地 Gradle Home 的全部缓存内容；检查确认该目录当前没有已被 Git 跟踪的文件，因此未执行索引移除，也没有删除本地缓存。按普通配置修改规则未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.14+19`。

- 2026-07-20：发布 `1.0.14+19`。版本从 `1.0.13+18` 升级，并同步更新 `pubspec.yaml`、测试默认 `PackageInfo`、检查更新测试的远端下一版本、Windows 生成配置及 MusicBrainz User-Agent；按正式“打包”规则未运行 `flutter analyze` 或 `flutter test`。本次跨平台更新包含缓存达到 95% 后后台按 LRU 清理至 90%、顺序队列结束后可选随机续播，以及设置页尾部开关和操作按钮统一 16px 右侧基线；Windows 额外包含开机启动、本地音频拖入和系统打开方式、单实例文件转发、本地标签管理及 MusicBrainz 搜索、纯格式标签与内嵌封面、默认及最小窗口宽度修复。Windows 在 Visual Studio x64 环境完成 CMake/Ninja Release 与 NSIS 构建，安装器包含 24 个 payload 文件、payload 大小 `49,459,172` 字节，runner ProductVersion/FileVersion 为 `1.0.14+19`，安装器 ProductVersion 为 `1.0.14`、FileVersion 为 `1.0.14.19`，runner 与 payload 的 `app.so` SHA256 均为 `D4500158CAFE19AFEAAE02FCC3E996142F6F09ACA470ED0D2738ED9CB22318CD`，NSIS 使用 `windows/installer/zmusic-installer.ico`。检测到 Zmusic（PID 21836）正在运行，为避免中断用户程序未做覆盖安装或新 runner 启动烟测。Android 两包均为 `versionName=1.0.14`、`versionCode=19`、最低 API 24、仅含 `arm64-v8a` / `armeabi-v7a`、388 个 ZIP 条目并通过 16KB zipalign，压缩后的两份 `libmpv.so` 合计 `5,610,999` 字节且不含 x86/x86_64 或 `just_audio`；通用版 target 36/V2，DiLink 使用 Android Studio JBR 21 和显式 Gradle `-PzmusicDiLinkCompatibility=true` 构建为 target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。本机无连接的 Android 设备，未做真机安装。重新生成 `dist/update.json`，按 `windows / android / android-dilink` 分平台提供 `1.0.14` 下载地址和更新说明，三个节点的大小、MD5、SHA256 均与最终包匹配；清单大小 `5,085` 字节、MD5 `0B5874DC7D82AFFC0E069E805442F412`、SHA256 `97E6A5FF728575637FE7A3B08A81EB8B4741771B20786BE740263C2AF6C982AD`。最终正式包：`dist/zmusic-windows-x64.exe` 大小 `16,561,015` 字节、MD5 `FFBDBF1EBDF8248FF4FDA8469CFABAA1`、SHA256 `ED33EC2C12EC7394D6AD51C5CBA94380B059B0B1DF20CAEC8954098292C49669`；`dist/zmusic-android.apk` 大小 `23,686,975` 字节、MD5 `BF755B4B2DB0B376E4CCDAB652055DC8`、SHA256 `6A350689A23F73DF0E94B188EBD8265375A11A3C9FC0D9B9F319C52511BD2217`；`dist/zmusic-android-dilink.apk` 大小 `24,227,353` 字节、MD5 `8553FCAC7DFB5A60D0BD3BF6D9115D66`、SHA256 `E2C5763CA5006D6C44FD829035C0ADC1032CEDC897CFAA3AC910F625183CDF5B`。

- 2026-07-20：统一设置页所有尾部按钮和开关的右侧间距。根因是 Material 3 `ListTile` 默认尾部内边距为 `24px`，首页布局自定义分组使用 `8px`，同时 80% 开关以中心缩放造成额外视觉内缩。`CompactSwitch` 改为以右侧中心为缩放锚点，`CompactSwitchListTile` 默认水平内边距固定为 `16px`；首页布局三个分组、桌面/手机 `_SettingsActionTile` 和主题设置行的右侧内容边界也统一为 `16px`。因此播放、窗口、开机启动、启动时检查更新、上传、选择、清除、检查更新和账号操作等尾部控件共用同一条右侧基线；显式使用零内边距的歌单表单仍保持原有容器内对齐。按普通功能修改规则仅执行必要格式化和结构检查，未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.13+18`。

- 2026-07-20：修复 Windows 最小窗口宽度仍可被无限缩小的问题。根因是 `window_manager 0.5.1` 的顶层窗口过程先于 `Win32Window::MessageHandler` 截获并返回 `WM_GETMINMAXINFO`，导致原生基类中的 `1088px` 约束无法执行；`DesktopIntegration.initialize` 现在在 Windows 上通过插件自身的 `setMinimumSize(Size(1088, 0))` 设置最小宽度，C++ 约束继续保留为插件初始化前的兜底，高度仍不限制。按普通功能修改规则仅执行必要格式化和结构检查，未运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.13+18`。

- 2026-07-20：修复本地歌曲格式标签和封面。`localMetadataToTrack` 不再把完整“文件名.扩展名”交给格式解析器，而是只读取最后扩展名，因此播放栏及歌曲列表仅显示 `MP3 / APE / FLAC` 等纯格式且保留原颜色；恢复旧本地播放队列时也按文件路径重新归一化格式，避免必须重新拖入歌曲。`readLocalAudioMetadata` 开启内嵌图片读取并优先选择 `coverFront`，图片通过 `ArtworkCacheManager` 写入现有 artwork 缓存；Track 保存带原音频路径的 `zmusic-local-artwork` 引用，缓存存在时直接复用，清除后可从 MP3/FLAC/APE 等音频重新提取，同一源文件无封面时在当前进程内避免重复解析，旧队列缺少封面引用时会根据本地文件地址自动补齐。`LocalAudioMetadata` 增加可选 `coverUrl` 并传递到本地 Track。执行 `dart format lib/src/artwork_cache.dart lib/src/models.dart lib/src/local_library.dart lib/src/app_controller.dart` 和 `git diff --check`；按普通功能修改规则未新增或运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.13+18`。

- 2026-07-20：按“打测试包”规则保持版本 `1.0.13+18`，完成 Windows 安装版、Android 通用版和 Android DiLink 三端重打包，未生成或覆盖 `dist/update.json`。静态分析首次发现 Windows 文件转发通道错误等待 `setMethodCallHandler` 的 `void` 返回值以及缓存错误回调的冗余下划线参数，分别修正 `lib/main.dart` 和 `lib/src/audio_cache.dart`；完整测试首次因新增 Windows 设置通道未注册 mock 而挂起，并发现检查更新用例仍把当前 `1.0.13` 当作远端新版本，补充 `com.zmusic.app/windows_settings` mock、将测试远端版本改为 `1.0.14` 后，最终 `flutter analyze --no-pub` 无问题，`flutter test --no-pub` 142 项全部通过。Windows 在 Visual Studio x64 环境完成 CMake/Ninja Release 与 NSIS 构建，安装器包含 24 个 payload 文件、payload 大小 `49,442,788` 字节，runner ProductVersion/FileVersion 为 `1.0.13+18`，安装器 ProductVersion 为 `1.0.13`、FileVersion 为 `1.0.13.18`，runner 与 payload 的 `app.so` SHA256 均为 `B18D5B12EA08A4AF26AA3DF203FBE1CA81BDA1C65556924C06435DA13F61DCC2`；检测到 Zmusic（PID 19672）正在运行，为避免中断用户程序未做 Windows 启动烟测。Android 两包均为 `versionName=1.0.13`、`versionCode=18`、最低 API 24、仅含 `arm64-v8a` / `armeabi-v7a`、388 个 ZIP 条目并通过 16KB zipalign，压缩后的两份 `libmpv.so` 合计 `5,610,999` 字节且不含 x86/x86_64 或 `just_audio`；通用版 target 36/V2，DiLink 使用 Android Studio JBR 21 和显式 Gradle `-PzmusicDiLinkCompatibility=true` 构建为 target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。本机无连接的 Android 设备，未做真机安装。`dist/update.json` 保持大小 `4,393` 字节、修改时间 `2026-07-17 19:17:54`、SHA256 `685FF45A6637E02EA7739A50741D4FBCE7C46C404A760ADC52B20381827A9C42` 不变。最终测试包：`dist/zmusic-windows-x64.exe` 大小 `16,552,755` 字节、MD5 `4071485EF0BEAA6B91E7637235B90404`、SHA256 `3222B7BC9C34296AF7A6AC4AAA3B5D76EB80A31F04A0B9750456651F4B3988B9`；`dist/zmusic-android.apk` 大小 `23,669,348` 字节、MD5 `358D2C425D9129188C0D93A8E44889DA`、SHA256 `F585203F87D04E32DEDEFA0FB4509088DECF08B91B0ABAC590973E23566448AC`；`dist/zmusic-android-dilink.apk` 大小 `24,209,729` 字节、MD5 `B7E8E56F62FA3F89DB7AE7C07A44FD15`、SHA256 `3087A967301378C0ECE385EA4C66FAD98133C7EA7B86194FC625B1C122E658F5`。

- 2026-07-20：新增两个默认关闭的持久化设置。Windows“窗口”分组增加“开机启动”，通过新的 `com.zmusic.app/windows_settings` 原生通道写入或删除当前用户 `Run` 注册表值，Runner 增加 `advapi32.lib` 链接，卸载器同步清理启动项；不需要管理员权限。全平台新增“播放 → 随机播放”，说明为“顺序播放的队列结束后，自动获取随机歌曲并继续播放”；仅在顺序模式正常到达队尾且不是电台时调用当前服务器 `getRandomSongs` 获取 30 首并替换队列，失败或空结果保持原停止行为，等待接口期间若用户切歌、改模式、切换账号或关闭设置则丢弃迟到结果，随机队列播放完后可继续获取下一批。执行 `dart format lib/src/settings_models.dart lib/src/app.dart lib/src/player_controller.dart lib/src/app_controller.dart lib/src/desktop_integration.dart`；按普通功能修改规则未新增或运行测试、未运行 `flutter analyze`、未构建 Windows Runner/安装器、未打包，版本保持 `1.0.13+18`。

- 2026-07-20：新增 Windows 专属本地音频标签管理页。设置页在“缓存”和“窗口”之间增加“工具 → 标签管理”，打开独立左右内容页并继续保留底部播放器；左侧支持手工输入路径、选择单个文件或递归扫描文件夹，文件列表只收集路径，选中时才读取标签，右侧按 key-value 形式查看和编辑标题、歌手、专辑、流派、年份、曲目号和歌词，支持格式仍复用现有 `audio_metadata_reader` 写入边界，APE 等格式显示为只读。新增轻量 `MusicBrainzMetadataService`，使用官方录音搜索接口和带应用信息的 User-Agent，按平均每秒一次请求做 1.1 秒节流；结果可选择后“一键全部填入”或勾选固定字段填入，填入只修改表单，点击“保存元数据”后才写文件，MusicBrainz 不提供歌词。执行 `dart format lib/src/app.dart lib/src/metadata_admin.dart lib/src/musicbrainz_metadata.dart`；按普通功能修改规则未新增或运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.13+18`。

- 2026-07-20：补齐 Windows 音频文件“打开方式”注册及单实例文件转发。NSIS 为 MP3、FLAC、APE、M4A、OGG、Opus、WAV、AIF/AIFF/AIFC 写入当前用户级 `Zmusic.Audio` ProgID、应用能力和 OpenWith 候选，不修改用户默认播放器，安装/卸载后通知 Explorer 刷新且卸载只移除 Zmusic 自身注册。Runner 首次由文件关联启动时在首帧后把命令行路径发送到现有 `file_drop` 通道；Zmusic 已运行时，第二实例通过 `WM_COPYDATA` 将 UTF-8 路径转交现有窗口后激活并退出，避免单实例锁吞掉文件。按普通功能修改规则未运行测试、未运行 `flutter analyze`、未构建安装器或打包，版本保持 `1.0.13+18`。

- 2026-07-20：修复 Windows 将本地 MP3 等音频拖入 Zmusic 后无法打开播放的问题。Runner 使用 `WM_DROPFILES` 接收资源管理器拖放，通过独立 `com.zmusic.app/file_drop` MethodChannel 发送 UTF-8 文件路径；Flutter 端过滤、去重支持的本地音频，读取元数据后按拖入顺序建立播放队列并立即播放第一首，标签损坏时以文件名兜底，多文件拖入一次处理。新增 `shell32.lib` 链接但未增加第三方依赖；未登录时保持登录约束，不后台播放。执行 `dart format lib/main.dart lib/src/app_controller.dart`，文件无需改写；按普通功能修改规则未运行测试、未运行 `flutter analyze`、未构建或打包，版本保持 `1.0.13+18`。

- 2026-07-20：固定 Windows 默认及最小窗口宽度。Runner 初始窗口由 `1280×720` 调整为 `1088×720`，约对应 `1072px` 客户区；`Win32Window` 新增按当前显示器 DPI 缩放的 `WM_GETMINMAXINFO` 最小宽度约束，窗口可继续放大但不能缩窄到使底部播放栏进入两行布局。仅修改 `windows/runner/main.cpp` 和 `windows/runner/win32_window.cpp`；按普通功能修改规则未运行测试、未运行 `flutter analyze`、未构建或打包，版本保持 `1.0.13+18`。

- 2026-07-20：按确认规则调整音频缓存淘汰水位。缓存总占用达到设置上限的 `95%` 时开始后台清理，并按 LRU 删除完整旧缓存直到占用降至 `90%`，形成 5 个百分点的触发回差；正在写入的 `.part` 文件仍受保护，不会清空全部缓存。执行 `dart format lib/src/audio_cache.dart`，文件无需再次改写；按普通功能修改规则未新增或运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.13+18`。

- 2026-07-20：修复音频缓存达到设置上限后，新歌曲会长时间停在加载中的问题。根因是未缓存歌曲在创建边播代理前同步等待全目录缓存扫描、排序和淘汰；达到上限后每次播放都会进入删除流程，播放与下一首预缓存还可能并发清理同一文件。`AudioCacheManager` 现在先返回边播缓存文件并在后台执行淘汰，所有淘汰任务串行化且同一时刻的自动任务会合并；缓存统计与界面统一计入主文件及辅助文件，带 `.part` 标记的当前在写缓存不参与删除；超过上限后按 LRU 一次清理到上限的约 95%（至少预留 64MB），避免每首歌都重复触发淘汰。只执行 `dart format lib/src/audio_cache.dart`；按普通功能修改规则未新增或运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.13+18`。

- 2026-07-17：发布 `1.0.13+18`。版本从 `1.0.12+17` 升级并同步更新 `pubspec.yaml`、测试默认 `PackageInfo` 和 Windows 生成配置；本次正式更新汇总播放自动切歌持续缓冲与加载中快速切歌修复、下一首后台预缓冲、搜索联想下拉及歌手/专辑直达、歌单详情短时缓存、独立刷新状态、同步未匹配报告、网易云 `163cn.tv` 短链接和清除系统封面缓存。按“打包”规则未运行 `flutter analyze` 或 `flutter test`。首次 Windows kernel snapshot 编译发现 Dart 未对 `PlaybackEngine` 与可中断接口做交集类型提升，改为检查后显式保存接口引用并执行 `dart format lib/src/player_controller.dart`，随后 Windows 在 Visual Studio x64 环境完成 CMake/Ninja Release 与 NSIS 构建；安装器包含 24 个 payload 文件，runner ProductVersion/FileVersion 为 `1.0.13+18`，安装器 ProductVersion 为 `1.0.13`、FileVersion 为 `1.0.13.18`，runner 与 payload 的 `app.so` SHA256 均为 `B7185BD2E5A38ACD759523B23ECE2EEEF1CAB93CF00CBC789A0011399116008A`，NSIS 使用 `windows/installer/zmusic-installer.ico`。检测到 `D:\Programs\Zmusic\zmusic.exe`（PID 9584）正在运行，为避免中断用户程序未做覆盖安装烟测。Android 两包均为 `versionName=1.0.13`、`versionCode=18`、最低 API 24、仅含 `arm64-v8a` / `armeabi-v7a`、388 个 ZIP 条目并通过 16KB zipalign，压缩后的两份 `libmpv.so` 合计 `5,610,999` 字节且不含 x86/x86_64 或 `just_audio`；通用版 target 36/V2，DiLink 通过显式 Gradle `-PzmusicDiLinkCompatibility=true` 构建为 target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。本机无连接的 Android 设备，未做真机安装。重新生成 `dist/update.json`，按 `windows / android / android-dilink` 提供 `1.0.13` 下载地址、平台更新说明和最终哈希，清单大小 `4,393` 字节、SHA256 `685FF45A6637E02EA7739A50741D4FBCE7C46C404A760ADC52B20381827A9C42`；三节点的文件大小、MD5、SHA256 复核均匹配。最终正式包：`dist/zmusic-windows-x64.exe` 大小 `16,522,604` 字节、MD5 `92BD3ED4CE9F681EC60A102432821B22`、SHA256 `9366A9E8B54B63E523EAB0D353ADC35334083524C21996E8D9961ECBD7D3BFF5`；`dist/zmusic-android.apk` 大小 `23,608,354` 字节、MD5 `CA72ACE117C74D96C69A9ED61A24D4EF`、SHA256 `8B6A48200984A06DFB7042DAE83E5AF6C857247946BB22A5C0F618C114213E2C`；`dist/zmusic-android-dilink.apk` 大小 `24,149,101` 字节、MD5 `72EF6F0D1FD9AA3DB9EEE12D82D16E32`、SHA256 `310BA07200757C9D21F13927E2C9FEB9C0E196E01425305FD4F9783DC8D48985`。

- 2026-07-17：统一设置页系统缓存操作文案。缓存分组标题由“系统缓存清理”改为“清除系统缓存”，右侧按钮由“清理”改为“清除”；确认弹窗标题、说明、确认按钮和完成提示同步统一使用“清除”，实际仍只删除本地封面图片缓存。执行 `dart format lib/src/app.dart lib/src/app_controller.dart`；按普通功能修改规则未新增或运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.12+17`。

- 2026-07-17：修复歌曲 A 尚在加载时立即切换歌曲 B，B 缓存完成后仍无法播放且暂停/播放无效的问题。根因是 Zmusic 的 `_audioOperation` 与 `media_kit NativePlayer.open` 两层串行锁叠加，旧曲目 `open` 未结束时新请求只能排队，同时提前关闭旧缓存代理可能让旧 `open` 更难退出。现在检测到新请求取代仍在打开的旧请求时，通过原生播放器 `stop(open: true, synchronized: false)`中断旧打开流程，再由既有串行队列安全执行最新请求；旧请求的迟到异常不再回传为当前播放失败。缓存代理清理改为先原子取得旧订阅与代理再异步关闭，避免快速连续切歌时旧清理误取消新代理；`open(play: true)` 返回但播放器仍未进入播放态时继续保留“需要重新打开”状态，使播放按钮能够真正重开当前歌曲。Windows、Android 和 iOS 的原生 `media_kit` 路径共用该修复，非原生平台保持原停止回退。执行 `dart format lib/src/player_controller.dart`；按普通功能修改规则未新增或运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.12+17`。

- 2026-07-17：修复首页快捷入口刷新错误占用全局忙碌状态的问题。“我喜欢的 / 我的歌单 / 公开歌单 / 电台”刷新改用不修改 `isBusy` 的独立请求包装，刷新期间不再连带置灰搜索类型下拉、设置页“重新加载曲库”或其他无关操作；首页每个快捷卡片各自维护刷新等待状态，只禁用并旋转实际点击的刷新按钮，“我的歌单”和“公开歌单”即使复用同一歌单接口也不会互相显示加载。每日推荐继续使用既有 `isLoadingRecommendations`，整库刷新继续使用 `isRefreshingLibrary`。执行 `dart format lib/src/app.dart lib/src/app_controller.dart`；按普通功能修改规则未新增或运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.12+17`。

- 2026-07-17：按“打测试包”规则保持版本 `1.0.12+17`，完成 Windows 安装版、Android 通用版和 Android DiLink 三端重打包，未生成或覆盖 `dist/update.json`。完整测试首次发现联想点击用例仍按旧逻辑要求歌手进入搜索结果页；将回归用例更新为校验联想下拉与完整搜索框左右对齐、歌手和专辑直接进入各自歌曲详情且不显示搜索结果页，单项复测通过。最终两次 `flutter analyze --no-pub` 均无问题，`flutter test --no-pub` 142 项全部通过。Windows 在 Visual Studio x64 开发环境完成 CMake/Ninja Release 与 NSIS 构建，安装器包含 24 个 payload 文件，runner ProductVersion/FileVersion 为 `1.0.12+17`，安装器 ProductVersion 为 `1.0.12`、FileVersion 为 `1.0.12.17`，runner 与 payload 的 `app.so` SHA256 均为 `6585ADC6A5E6F3527F1AEBF0BFC58D1EE14CBCDF0708028FBE6990F73DFF4281`；检测到 `D:\Programs\Zmusic\zmusic.exe`（PID 4252）正在运行，为避免中断用户程序未做覆盖安装烟测。Android 两包均为 `versionName=1.0.12`、`versionCode=17`、最低 API 24、仅含 `arm64-v8a` / `armeabi-v7a`、388 个 ZIP 条目并通过 16KB zipalign，压缩后的两份 `libmpv.so` 合计 `5,610,999` 字节且不含 x86/x86_64 或 `just_audio`；通用版 target 36/V2，DiLink 通过显式 Gradle `-PzmusicDiLinkCompatibility=true` 构建为 target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。本机无连接的 Android 设备，未做真机安装。`dist/update.json` 保持大小 `3,761` 字节、SHA256 `5B29955745747356C758EC63D80991574C0720940749191F178181573CE2F814` 不变。最终测试包：`dist/zmusic-windows-x64.exe` 大小 `16,520,926` 字节、MD5 `26C5CF9E2BE55A46D3AD0A57B5B54816`、SHA256 `147FC184A2E7A67CE2DE2DA4D18FF28ECA86F2086F245337A2635D41C5DDEA1B`；`dist/zmusic-android.apk` 大小 `23,606,184` 字节、MD5 `404CA78953E8A5DDC14BF391D3E7B8D7`、SHA256 `1E3ADA5D946AC0393BED26B05B7F9FF60B3DF2759E5D73C5011BF29403591A62`；`dist/zmusic-android-dilink.apk` 大小 `24,146,937` 字节、MD5 `F5807B481D170539FDDD90F57979ED21`、SHA256 `B0E1918D32717CA1E6B842AE3725B1EC54621B94A4E28A055398C8CC7B9E6E02`。

- 2026-07-17：调整首页远程联想和歌单详情加载。联想下拉层现在按完整搜索组件的左边缘与总宽度对齐，不再只从关键词输入框起始位置展开；歌曲联想继续进入歌曲搜索结果，歌手和专辑联想保留 `search3` 返回的真实条目 ID，点击后直接请求并打开对应歌手/专辑歌曲详情，不再先闪过搜索结果页。排查确认歌单详情只调用一次 Subsonic/Navidrome `getPlaylist`，该接口会一次返回完整歌单而不支持客户端分页；控制器新增 5 分钟歌单歌曲内存缓存和同歌单并发请求合并，重复进入详情时直接复用，登录、登出、重新加载及歌单增删改后清除或精确失效，主动刷新仍强制拉取最新数据。执行 `dart format lib/src/app.dart lib/src/app_controller.dart`；按普通功能修改规则未新增或运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.12+17`。

- 2026-07-17：按反馈扩展缓存、首页模块恢复和播放预缓冲。设置页“缓存”分组新增“系统缓存清理”，确认后清除本地封面图片缓存且不影响音频缓存大小统计；首页快捷入口或发现模块从隐藏恢复显示后，已登录状态会自动补拉一次首页数据，避免“最新专辑”等模块打开后仍为空；播放器在当前曲目音频缓存完成且仍在播放时，只按实际播放模式预缓冲下一首会播放的歌曲，支持“下一首播放”、顺序、列表循环和随机模式的真实下一首，切歌时可接管正在预缓冲的缓存代理。执行 `dart format lib/src/artwork_cache.dart lib/src/app_controller.dart lib/src/app.dart lib/src/streaming_audio_cache_source.dart lib/src/player_controller.dart`；按普通功能修改规则未新增或运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.12+17`。

- 2026-07-17：按“打测试包”规则保持版本 `1.0.12+17`，完成 Windows 安装版、Android 通用版和 Android DiLink 三端重打包，未生成或覆盖 `dist/update.json`。首次完整测试发现 4 项问题：联想下拉的 `ListView` 在 Android 环境继承页面级 `PrimaryScrollController`，与菜单滚动条形成多位置冲突；一个 Android 设置测试未在断言结束前复原平台覆盖值；音乐页测试仍按旧布局查找已移到设置页的曲库刷新按钮。联想列表改为 `primary: false`，并同步修正两项过期测试。最终 `flutter analyze --no-pub` 无问题，`flutter test --no-pub` 142 项全部通过。Windows 在 Visual Studio x64 环境完成 CMake/Ninja Release 与 NSIS 构建，安装器包含 24 个 payload 文件，runner ProductVersion/FileVersion 为 `1.0.12+17`，安装器 ProductVersion 为 `1.0.12`、FileVersion 为 `1.0.12.17`，runner 与 payload 的 `app.so` SHA256 均为 `40836539ED00E118C9880CD7E2E491ED090702DD4A6CB5E3495B0393E4DB05B4`；从最终安装器提取确认仍使用绿色 Zmusic 音符图标。检测到 `D:\Programs\Zmusic\zmusic.exe`（PID 27876）正在运行，为避免中断用户程序未做覆盖安装烟测。Android 两包均为 `versionName=1.0.12`、`versionCode=17`、最低 API 24、仅含 `arm64-v8a` / `armeabi-v7a`、388 个 ZIP 条目并通过 16KB zipalign，压缩后的两份 `libmpv.so` 合计 `5,610,999` 字节且不含 x86/x86_64 或 `just_audio`；通用版 target 36/V2，DiLink target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。本机无连接的 Android 设备，未做真机安装。`dist/update.json` 保持 2026-07-16 18:05:10、大小 `3,761` 字节、SHA256 `5B29955745747356C758EC63D80991574C0720940749191F178181573CE2F814` 不变。最终测试包：`dist/zmusic-windows-x64.exe` 大小 `16,519,475` 字节、MD5 `F3C43F3DF830634B3EA9AF018A4F16A5`、SHA256 `67A81888E0821734E96FCD6BE580784D6BAC8900DD08F0DAF6C70A342B9ACA86`；`dist/zmusic-android.apk` 大小 `23,588,363` 字节、MD5 `210F39937B89B952F81898534C29310A`、SHA256 `ABD43FA49357FBC4A980A4EFA6F6C051318A23B34A9E31D67A0CA0A4AED9289A`；`dist/zmusic-android-dilink.apk` 大小 `24,129,255` 字节、MD5 `394F28E04377F5D7EE7CE93F4C0EB30F`、SHA256 `155CDE91D4072E4CAEFB6970D0C461AFC3B91BBB4AC670A275A2F12BFFF0B152`。

- 2026-07-17：修复首页远程联想搜索达到“1 个中文或 3 个字母/数字”阈值后没有任何可见反馈的问题。搜索栏不再把异步请求状态完全交给 `RawAutocomplete`，改为显式管理 300ms 防抖、最新请求编号和锚定下拉层：请求开始显示“正在搜索”，成功展示最多 5 条歌曲/歌手/专辑混合结果，空结果和异常分别显示“未找到相关内容 / 联想搜索失败”，旧关键词的迟到响应不会覆盖新结果；点击结果仍直接进入对应默认页签的搜索结果页。`SubsonicApiClient` 默认改为复用单个进程级 `http.Client`，避免每次联想和正式搜索创建但不关闭新连接造成连接积累与后续搜索变慢；测试注入的独立 Client 保持不变。联想和正式搜索仍各自只调用一次 Subsonic/Navidrome `search3`，该单一接口通过 `songCount / artistCount / albumCount` 同时返回歌曲、歌手、专辑三组数据，并非三个接口。执行 `dart format lib/src/app.dart lib/src/subsonic_api.dart`；按普通功能修改规则未新增或运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.12+17`。

- 2026-07-17：排查并修复 Windows 长时间播放后，上一首自然结束自动切到下一首时可能永久缓冲、手动上一首/下一首后又恢复的问题，APE 曲目更易触发。主因是自动换歌先把界面状态切到下一首，再同步等待旧歌曲缓存代理关闭；代理关闭包含大文件写盘 `flush()` 和下载任务收尾，高品质 APE 更容易长时间占住第一次请求，而手动再切一次时旧代理引用已经清空，所以第二次请求能立即继续。现在普通换歌会立即取消旧代理并在后台完成关闭，不再阻塞下一首解析和打开，停止播放、队列结束等需要完整释放资源的路径仍会等待关闭。另有两处放大问题一并修正：Zmusic 原先在 `media_kit 1.2.6` 的 `Player.open()` 内部原子 `stop()` 之前额外执行两次 `stop()`，现已移除重复调用；启动恢复计时器原先会被上一首迟到的末尾位置事件提前取消，且只识别“正在播放且缓冲”，现在改为 12 秒到期时结合当前请求、打开状态、播放位置、缓冲和播放状态判断，覆盖打开未返回、未播放缓冲及静默停滞。顺序播放结束等仍需显式停止的路径最多等待 3 秒。格式、原始 APE/FLAC、单路边播边缓存和完整缓存复用逻辑均未改变。执行 `dart format lib/src/player_controller.dart`；按普通功能修改规则未新增或运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.12+17`。

- 2026-07-17：将全曲库刷新入口从音乐页搜索框右侧移到设置页“账号”区域，并放在“登出”按钮左侧。Windows 和 Android 平板使用无边框“刷新图标 + 重新加载曲库”按钮，Android 手机（小于 510dp）仅显示带“重新加载曲库”提示的刷新图标；刷新期间显示圆形进度并继续复用现有分接口加载、状态回填和防重复点击逻辑。音乐页搜索框恢复独占原有最大宽度。执行 `dart format lib/src/app.dart`；按普通功能修改规则未新增或运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.12+17`。

- 2026-07-17：同步歌单完成报告新增未匹配歌曲明细。`PlaylistSyncResult` 现在携带按外部歌单原顺序保留的 `ExternalPlaylistTrack` 列表，未匹配数量直接由该列表计算；同步页面在原有来源、匹配、新增、已有和未匹配计数下方列出全部未匹配歌曲的编号、歌曲名和歌手，无未匹配项时不显示明细。匹配策略和 Navidrome 写入流程未改变。执行 `dart format lib/src/playlist_sync.dart lib/src/app_controller.dart lib/src/playlist_tools_page.dart`；按普通功能修改规则未新增或运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.12+17`。

- 2026-07-17：修复网易云音乐 `163cn.tv` 分享短链接无法同步歌单。`PlaylistSyncService` 在网易云入口识别 `163cn.tv` 及其子域名后，以禁用自动跳转的单次 GET 请求读取 `3xx Location`，将最终网易云地址交回既有歌单 ID 解析、曲目读取和匹配流程；普通数字 ID、完整网易云地址和 QQ 音乐地址保持原逻辑。实测用户截图中的短链接返回 `302` 且最终地址包含有效歌单 ID。执行 `dart format lib/src/playlist_sync.dart`；按普通功能修改规则未新增或运行测试、未运行 `flutter analyze`、未打包，版本保持 `1.0.12+17`。

- 2026-07-16：发布 `1.0.12+17`。Android 手机设置页的账号登出、应用 Logo 上传、背景图上传、缓存目录选择和检查更新统一改为“信息在左、操作在右”的单行布局；设置页“登出 / 上传 / 选择”在 Windows、Android 手机和平板上统一使用无边框文本按钮。`AppSettings.checkUpdatesOnStartup` 的全新配置默认值及旧配置缺失字段回退值均改为 `true`，显式保存的关闭状态继续保留；启动检查增加控制器初始化守卫，确保读取完本地设置后再请求更新。同步补充默认开启、手机尾部按钮位置和无边框样式回归用例，并将测试包信息更新为 `1.0.12+17`；执行 `dart format`，按“打包”规则未运行 `flutter analyze` 或 `flutter test`。Windows 在 Visual Studio x64 环境完成 CMake/Ninja Release 和 NSIS 构建，安装器含 24 个 payload 文件，ProductVersion 为 `1.0.12`、FileVersion 为 `1.0.12.17`，runner 与 payload 的 `app.so` SHA256 均为 `CD23B4620B379E736049AC5E4ED892E3FA2C235062264ADD5850CD77AD532483`，提取确认安装器仍使用绿色 Zmusic 音符图标；检测到 `D:\Programs\Zmusic\zmusic.exe`（PID 21012）正在运行，为避免中断用户程序未做覆盖安装烟测。Android 两包均为 `versionName=1.0.12`、`versionCode=17`、最低 API 24、仅含 `arm64-v8a` / `armeabi-v7a`、388 个 ZIP 条目并通过 16KB zipalign，压缩后的两份 `libmpv.so` 合计 `5,610,999` 字节且不含 x86/x86_64 或 `just_audio`；通用版 target 36/V2，DiLink target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。本机无连接的 Android 设备，未做真机安装。重新生成 `dist/update.json`，三个平台的文件大小和哈希均与最终包匹配，清单大小 `3,761` 字节、SHA256 `5B29955745747356C758EC63D80991574C0720940749191F178181573CE2F814`。最终正式包：`dist/zmusic-windows-x64.exe` 大小 `16,517,725` 字节、MD5 `E61F73C368E5D92D04F92AA4EBA6F3EB`、SHA256 `9A0C4F0F7717766BA5F2D4FA430414B04637D471F615EA2BB72DD87465847C1B`；`dist/zmusic-android.apk` 大小 `23,608,996` 字节、MD5 `CBB9C2937C259911A4E5EEA19F3A5F03`、SHA256 `98CE880D581C5B75ADC59640A7500E1B835393C300854FB1E5CB2B89308433A2`；`dist/zmusic-android-dilink.apk` 大小 `24,149,889` 字节、MD5 `439ED24DCE42A53293882799D134A00B`、SHA256 `47B3AFF2E05136B99971424CBF1CA087D2C092A45DD399EA1279A2D65B1EB221`。

- 2026-07-16：按“打测试包”规则保持版本 `1.0.11+16` 重打 Windows 安装版、Android 通用版和 Android DiLink，未生成或覆盖 `dist/update.json`。首次完整测试发现新增快捷入口播放按钮后，4 个旧用例点击整张“我喜欢的”卡片时命中新播放按钮而未进入详情；测试改为明确点击卡片标题，并新增“我喜欢的 / 每日推荐”播放按钮会替换队列并开始播放的回归用例。最终两次 `flutter analyze --no-pub` 均无问题，`flutter test --no-pub` 141 项全部通过。Windows 在 Visual Studio x64 环境完成 CMake/Ninja Release 和 NSIS 构建，安装器含 24 个 payload 文件，版本为 `1.0.11.16`，runner 与 payload 的 `app.so` SHA256 均为 `0F9F10BA583F45444D455A03A8D849B2B2F96FA70C627F4FF55317563CD93225`，提取确认安装器使用绿色 Zmusic 音符图标；检测到 `D:\Programs\Zmusic\zmusic.exe`（PID 47852）正在运行，为避免中断播放未执行覆盖安装烟测。Android 两包均为 `versionName=1.0.11`、`versionCode=16`、最低 API 24、仅含 `arm64-v8a` / `armeabi-v7a`、通过 16KB zipalign，压缩后的两份 `libmpv.so` 合计 `5,610,999` 字节且不含 x86/x86_64 或 `just_audio`；通用版 target 36/V2，DiLink target 29/V1-only，两包签名证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。本机无连接的 Android 设备，未做真机安装。`dist/update.json` 保持 2026-07-16 14:17:27、大小 `5,381` 字节、SHA256 `BA12217734E3A3F62A2FD70C56DBB27D49DA16539A11FA6331B1214FB3D8A37E` 不变。最终测试包：`dist/zmusic-windows-x64.exe` 大小 `16,523,382` 字节、MD5 `6FB542C06BD6DC2B7A23C8BEA45ED0C2`、SHA256 `F58C106EF9F0F9AF951788F310C53E4456E1E8A36FF4B7633A2F060C3BB57D5F`；`dist/zmusic-android.apk` 大小 `23,608,812` 字节、MD5 `5E974DEFFEF24B8D9A6E6CC9F6CBCD1E`、SHA256 `F9DF89EF527D64EA8F5425C3017E8BB39DAE62C5007D9BB0A3AE64A02CBDD422`；`dist/zmusic-android-dilink.apk` 大小 `24,149,690` 字节、MD5 `0FBFAB11384F6D4A7CD073D70174FF07`、SHA256 `F6075170DE5975B4F13C156E15DFC50EE71437611323BA7EF1C63C66B94DBDC2`。

- 2026-07-16：仅调整手机版设置页“关于”区域，在紧凑宽度下把“检查更新”的“检查”按钮从版本信息下方左侧移到右侧；Windows、平板及其他设置操作项保持原布局。`dart format lib/src/app.dart` 通过；按普通功能修改规则未运行 `flutter analyze`、`flutter test` 或打包，版本保持 `1.0.11+16`。

- 2026-07-16：按反馈将歌单卡片和加入歌单选择列表里的公开歌单小标从“公共”改为“公开”，内部 `public` 命名不变。本轮仅执行必要格式化，未运行 `flutter analyze`、`flutter test` 或打包。

- 2026-07-16：调整全平台音乐页快捷入口和全量刷新反馈。“每日推荐”与“我喜欢的”在刷新按钮左侧新增播放按钮，有歌曲时会直接用对应曲目替换当前播放队列并从第一首开始播放；用户可见的“公共电台”统一简化为“电台”，内部 `publicRadio` 配置枚举保持不变。全量刷新继续并行加载首页模块，但精确歌曲总数的全专辑分页统计和首次每日推荐改为延后任务：可见首页数据完成后立即停止全量刷新菊花，延后结果仍会自动回填且 `loadLibraryOverview()` 会等待全部任务收尾，避免重复刷新并发；全量刷新成功不再生成右上角“已加载……歌曲数……”提示，接口失败信息继续保留。同步更新既有电台文案断言；`dart format lib/src/app.dart lib/src/app_controller.dart test/widget_test.dart` 通过。按普通功能修改规则未运行 `flutter analyze`、`flutter test` 或打包，版本保持 `1.0.11+16`。

- 2026-07-16：按“打测试包”规则保持版本 `1.0.11+16` 重打 Windows 安装版、Android 通用版和 Android DiLink，未生成或覆盖 `dist/update.json`。完整测试首次发现每日推荐测试桩未实现缓存接口、隐藏模块用例未关闭推荐入口及快捷入口总开关断言遗漏推荐项；补齐 `_MemoryLibraryStore` 的每日推荐缓存实现、修正隐藏配置和总开关断言，并新增 30 首上限、排除当前队列、同曲高品质去重及尽量错开相邻歌手的推荐算法回归测试。`dart format test/widget_test.dart` 通过，`flutter analyze --no-pub` 无问题，`flutter test --no-pub` 140 项全部通过。Windows 在 Visual Studio x64 环境完成 CMake/Ninja Release 和 NSIS 构建，安装器含 24 个 payload 文件，版本为 `1.0.11.16`，提取确认使用绿色 Zmusic 音符图标；检测到 `D:\Programs\Zmusic\zmusic.exe`（PID 47852）正在运行，为避免中断播放未执行覆盖安装烟测。Android 两包均为 `versionName=1.0.11`、`versionCode=16`、最低 API 24，仅含 `arm64-v8a` / `armeabi-v7a` 并通过 16KB zipalign；通用版 target 36/V2，DiLink target 29/V1-only，两包签名证书一致且不含 x86/x86_64 或 `just_audio`。本机无连接的 Android 设备，未做真机安装。`dist/update.json` 保持 2026-07-16 14:17:27、SHA256 `BA12217734E3A3F62A2FD70C56DBB27D49DA16539A11FA6331B1214FB3D8A37E` 不变。最终测试包：`dist/zmusic-windows-x64.exe` 大小 `16,523,604` 字节、MD5 `FB69B669BA3770FCF8DBBC80E02CC3ED`、SHA256 `F123DAD2EBB3C44A4A4D20FDA0569E6AF34C18E7036380DD3A14100D45322D86`；`dist/zmusic-android.apk` 大小 `23,609,613` 字节、MD5 `F563DB086D09F6595EB7398906FEB058`、SHA256 `A0FDE0AD3B1A9CE987583A91E68B39C2667A528C936B4E79AE19139405B0D75D`；`dist/zmusic-android-dilink.apk` 大小 `24,150,504` 字节、MD5 `EF3DA4C70B63C93427ADF6DDA4E00301`、SHA256 `DC30CAFF3A63E0E6D3E384DE4566E33F8F48B0AB9EEC362B2A660EECCF099462`。

- 2026-07-16：新增全平台“每日推荐”。控制器通过收藏歌曲、收藏歌手的曲库搜索、最近/最多播放专辑曲目和随机歌曲四类来源生成最多 30 首，按 4/8/9/9 配额抽取，接口单路失败时由其余来源补足；生成前按歌手+歌名统一选择 FLAC > APE > MP3 的最高品质版本，排除当前播放队列并尽量错开相邻歌手。同一天结果持久化复用，播放队列冲突、账号切换、退出登录或关闭入口时清除，详情页“换一批”使用新随机种子。首页手机端使用全宽推荐入口加原有 2×2 快捷入口，宽屏最多五项同排；推荐详情支持播放全部、刷新和保存到已有个人歌单或按日期创建新歌单。设置页新增“每日推荐”独立开关并纳入快捷入口总开关，隐藏后启动、登录及全量刷新均不请求推荐依赖接口。执行 `dart format` 解析并格式化 5 个相关 Dart 文件；按当前普通功能修改规则，未运行 `flutter analyze`、`flutter test` 或打包，版本保持 `1.0.11+16`。

- 2026-07-16：按本轮反馈将用户可见的“公共歌单”统一改为“公开歌单”，内部 `publicPlaylists` 等业务命名不变。设置页“关于”区域在“检查更新”上方新增“启动时检查更新”开关，设置持久化到 `AppSettings.checkUpdatesOnStartup`；开启后应用每次启动首帧后自动检查一次更新，发现新版本时复用手动检查的更新弹窗和下载逻辑，无新版本或失败时不打扰。更新相关回归测试覆盖设置持久化、开关保存、启动自动检查弹窗和“公开歌单”文案；补齐 `_MemoryLibraryStore` 播放会话方法，避免全量测试触发真实 `shared_preferences` 通道。验证：`dart format lib/src/settings_models.dart lib/src/app.dart lib/src/app_controller.dart test/widget_test.dart` 通过，`flutter analyze --no-pub` 无问题，`flutter test --no-pub` 139 项全部通过。本轮未打包。

- 2026-07-16：新增播放会话本地恢复。`LibraryStore` 仅保存播放队列、当前歌曲索引和播放模式，不增加音源地址或账号字段；`PlayerController` 可恢复为已选中但未打开媒体的暂停状态，首次点击播放才重新解析当前账号的 Subsonic 流地址并从 `00:00` 开始，结构变化采用专用回调避免播放进度和缓冲通知高频写盘，一次性的“下一首播放”不会进入持久化队列。`AppController` 串行保存会话，普通重启恢复，登录账号切换和退出登录都会等待旧写入完成后清除内存及本地播放会话，音频和图片文件缓存保持独立。同步固定新执行规则：普通功能修改不进入测试逻辑，只有明确“打测试包”才运行分析、测试和三端测试打包；正式“打包”仍按发布规则执行但不自动运行测试。本轮仅执行 `dart format`，按要求未运行 `flutter analyze`、`flutter test` 或打包，版本保持 `1.0.11+16`。

- 2026-07-16：固定后续发布口令。“打测试包”表示三端打包但可见版本和内部 build/versionCode 都不变，且不生成或覆盖 `update.json`；“打包”表示可见补丁版本和内部 build/versionCode 均递增、生成三端正式包并根据最终产物生成 `update.json`。更新清单的说明改为按 `windows / android / android-dilink` 分平台维护，平台独有功能不得写入无关平台节点，哈希必须使用最终包重新计算。本轮仅更新项目协作规则，未修改代码、未运行测试、未打包。

- 2026-07-16：发布 `1.0.11+16`。全项目开关统一使用 `CompactSwitch` / `CompactSwitchListTile` 并按 80% 缩放视觉尺寸；设置页“首页布局”的“快捷入口 / 专辑与播放 / 歌单”新增分组总开关，总开关关闭会批量关闭并折叠子项，重新开启会恢复全部子项，“歌单板块”重命名为“歌单”。更新清单地址由旧 `/zmusic/update.json` 直接迁移为 `https://file.zuitimes.com/zmusic/0/update.json`，不做旧地址兼容；版本从 `1.0.10+15` 升为 `1.0.11+16`，同步更新测试默认包信息和 Windows 生成配置。新增总开关批量状态/折叠、新清单地址和紧凑开关回归断言；`dart format` 通过，`flutter analyze --no-pub` 无问题，`flutter test --no-pub` 137 项全部通过。按用户要求由三个独立子代理分别构建 Windows、Android 通用版和 Android DiLink，主任务复核两份 APK 均为最低 API 24、仅 `arm64-v8a`/`armeabi-v7a` 且通过 16KB zipalign；通用版 target 36/V2，DiLink target 29/V1-only。未连接 Android 真机，未做真机安装；Windows 正式安装未执行，避免关闭用户正在运行的 Zmusic。新增 `dist/update.json`，包含三个平台的 `1.0.10 → 1.0.11` 更新说明、下载地址、文件大小、MD5 和 SHA256，部署时应上传到 `/zmusic/0/update.json`。最终产物：`dist/zmusic-windows-x64.exe` 大小 `16,504,535` 字节、MD5 `16E8352D970067D7E51D33E9EA3E82FA`、SHA256 `78F11BD6361E6C6AC9466D4D7DAB22997786972F2DC83F850CA343688B2C3275`；`dist/zmusic-android.apk` 大小 `23,563,576` 字节、MD5 `869C87E0BA93180DBC07B4F816F6E68D`、SHA256 `E723DCC4E0F880BE1978F7494732AB3F6CC5158063920CB2627A690636E0298F`；`dist/zmusic-android-dilink.apk` 大小 `24,104,744` 字节、MD5 `E615169E097302B0A70C7F6C025116A9`、SHA256 `DC66BFAC3B887960CCC2044A5E5E638298D6339F408C68F7DB9AC1FC0CDAB44F`。

- 2026-07-16：按本轮截图继续统一全平台登录、搜索、歌单工具和歌单详情。登录账号/密码改为图标加行内提示且不再使用浮动标签；Android 手机合并歌单移除来源/目标互换按钮，合并与批量添加的歌单选择移除重复字段标题；删除歌单确认文案去掉“同步到当前 Navidrome 服务”。修正 Android 音乐页搜索栏与内容左边距；播放详情不再显示来源。我的歌单详情把“歌单 · N 首”移到封面下方、简介移到标题信息区并支持 50 字截断后点击展开，新增批量移除模式、选中计数、二次确认及倒序索引删除，取消确认保留选择而离开详情会清空；公共歌单使用相同只读头部布局。验证：`flutter analyze --no-pub` 无问题，`flutter test --no-pub` 136 项全部通过。本轮按用户要求由三个独立子代理分别构建 Windows、Android 通用版和 Android DiLink，版本保持 `1.0.10+15`；主任务复核 `dist` 恰好只含三个固定文件，两份 APK 均最低 API 24、仅 `arm64-v8a`/`armeabi-v7a` 且通过 16KB zipalign，通用版 target 36/V2，DiLink target 29/V1-only。未连接 Android 真机，未做真机安装；检测到已安装的 Zmusic 正在运行，未中断用户当前程序。最终产物：`dist/zmusic-windows-x64.exe` 大小 `16,504,317` 字节、MD5 `4C388D5E9BC7962B8D3DC5FEA0C176D7`、SHA256 `74E7D2A74D170D2CE1A1C386905C923931E28DF319A4AE60CF453CF934B3F95F`；`dist/zmusic-android.apk` 大小 `23,569,659` 字节、MD5 `534C661948497CCBF8B4FEFA7C78D82E`、SHA256 `92AFB35C551580D25FC882F5E30690CE7568F7C7441B5F49A063343A2DF4AC47`；`dist/zmusic-android-dilink.apk` 大小 `24,110,844` 字节、MD5 `6BC7D0597953F1206FB922861C404E72`、SHA256 `EA05D402F5C87D2959D3E2E8C4C8C1A85148BEDB77C4D73B415B2518DEBD92B0`。

- 2026-07-16：按本轮反馈调整 Windows 安装器和更新流程。NSIS 安装器启用安装进度完成后自动跳转到自定义完成页，不再要求用户手动点击“下一步”；完成页按钮改为左侧“打开”、右侧“完成”，并同步调整完成页说明文案。Windows 检查到更新后点击“下载更新”不再跳浏览器，而是在应用内弹窗展示下载进度，流式下载更新安装器到系统临时目录后自动启动安装器；非 Windows 平台继续沿用外部浏览器/系统打开方式。新增 `AppUpdateService.downloadUpdate` 下载进度模型、文件名安全化和失败清理逻辑，更新设置页弹窗状态和回归测试；顺手修复安装器构建脚本在清理临时 payload 后再统计文件数导致测试编译退出码为 1 的问题。验证：`dart format lib/src/app_update.dart lib/src/app.dart test/widget_test.dart` 通过且无新格式化变化，`flutter analyze --no-pub` 无问题，`flutter test --no-pub` 136 项全部通过，`windows/installer/build-installer.ps1 -OutputFile build/package/test-zmusic-installer.exe -TestMode` 使用 NSIS 3.12 编译通过，测试安装器 24 个 payload 文件、大小 `16,500,815` 字节。本轮未正式打包，`dist` 未覆盖。

- 2026-07-16：按本轮截图统一精简首页、详情页和歌单工具。首页新建歌单、全曲库刷新，我的歌单页同步/合并/批量添加，合并页互换/加入以及批量曲库搜索等指定图标按钮改为透明背景，保留原图标、提示和禁用状态；专辑、发现、我喜欢的及远程歌单详情移除头部已包含数量后仍重复出现的“歌曲 N”区标题，搜索结果页签改为仅显示“歌曲 / 歌手 / 专辑”，不再追加数量。同步歌单的地址和目标名称、合并歌单的来源/目标选择、批量添加的目标选择均改为固定外部标题加紧凑输入控件，焦点变化不会再把文字浮到边框左上角。Android 手机小于 510dp 时，合并歌单和批量添加改为“先选歌曲 → 再选目标歌单 → 完成”的单面板分步流程，已选歌曲跨步骤保留，可返回修改；Windows 和 Android 平板继续使用双栏。新增透明按钮、无浮动标签、无重复数量、无页签计数及 Android 390dp 合并/批量分步回归测试；`dart format` 通过，`flutter analyze --no-pub` 无问题，`flutter test --no-pub` 134 项全部通过。按要求可见版本和内部构建号均保持 `1.0.10+15`。Windows 在 Visual Studio x64 环境下完成 CMake/Ninja Release 和 NSIS 正式安装器构建；测试安装器在项目隔离目录静默安装退出码 0，24 个 payload 文件中的 `data/app.so` 与本次 AOT SHA256 一致，安装后生成 `zmusic.exe`、`uninstall.exe`、`zmusic.installed` 和 `userdata`，程序版本为 `1.0.10+15`，正式安装器版本为 `1.0.10.15` 且可提取 32x32 Zmusic 图标。检测到 `D:\Programs\Zmusic\zmusic.exe` 正在运行，为避免中断用户播放未强制关闭做新 runner 启动烟测。Android 通用版与 DiLink 均为 `versionName=1.0.10`、`versionCode=15`、最低 API 24、仅 `arm64-v8a`/`armeabi-v7a`、388 个 ZIP 条目、16KB zipalign、JNI Deflate 压缩且不含 x86/x86_64 或 `just_audio`；通用版 target 36、V2，DiLink target 29、V1-only，两包证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。本机 `adb devices` 无连接设备，未做 Android/DiLink 真机安装。最终 `dist` 只保留三个固定文件：`dist/zmusic-windows-x64.exe` 大小 `16,496,493` 字节、MD5 `E4AAC2EDEDCF9BF001FE436D44EFFD65`、SHA256 `1890E97CABE6C0C91DF8AF2B3F424F9A66C8DF93E0373BD7A3B54049B1A5DBCB`；`dist/zmusic-android.apk` 大小 `23,549,793` 字节、MD5 `9F740D87C7FC4DD0203D4AA6D4B2A56D`、SHA256 `C730FD343F49559E71A5706E69B0C50FB5E36FD64B84C413CC4E6CD340F87F2C`；`dist/zmusic-android-dilink.apk` 大小 `24,091,080` 字节、MD5 `3BFEDAB66D4CB83B14192F351EFC92E8`、SHA256 `27283424C9522886A96B1CEF76FAB7826A3E6F1E2C3B16F1911B7FA6EDCBD935`。

- 2026-07-15：按本轮反馈修正全平台搜索类型语义和 Windows 安装完成页，并保持版本 `1.0.10+15` 重打三端。搜索下拉移除“所有”，只保留“歌曲 / 歌手 / 专辑”，菜单项改为中文在前、状态图标在后；该选择现在只控制搜索结果页的默认页签，正式搜索、歌曲翻页、远程输入联想以及播放详情歌手/专辑跳转均固定按 `LibrarySearchScope.all` 获取三类数据。默认和返回首页后的选择改为“歌曲”。移除 `RawAutocomplete` 随类型变化重建的 `ValueKey`，菜单关闭后的下一帧恢复同一 `FocusNode`，修复切换类型后输入框需要多次点击才显示焦点的问题。Windows NSIS 完成页恢复两个独立标准按钮：“完成”在左侧，直接退出且不启动应用；“打开”在右侧，触发启动后退出安装器。实现复用 NSIS 原生 Next/Back 控件并动态移动到右下相邻按钮位，避免把 Cancel 改名后仍触发取消确认；测试宏会写一次性回调标记以验证“打开”路径。新增搜索菜单无“所有”、文字/图标顺序、切换后焦点、选择歌手仍请求全部类型且默认打开歌手页签的回归测试，并更新播放详情和联想搜索断言；`dart format` 通过，`flutter analyze --no-pub` 无问题，`flutter test --no-pub` 134 项全部通过。NSIS 3.12 测试安装器实际走完两次安装流程：确认按钮位置为完成 `x=1355`、打开 `x=1441`，取消按钮隐藏；“完成”关闭安装器且无启动标记，“打开”写入启动标记并关闭安装器。Windows CMake/Ninja Release 在 Visual Studio x64 环境构建成功，正式安装器 payload 24 个文件，`data/app.so` SHA256 与最新 AOT 文件一致，运行程序 ProductVersion/FileVersion 均为 `1.0.10+15`，包含 `libmpv-2.dll`、`app_icon.ico`，不含 `userdata`、安装标记、卸载器或 `just_audio`；从最终安装 EXE 提取图标确认是绿色 Zmusic 音符。检测到 `D:\Programs\Zmusic\zmusic.exe` 正在运行，为避免中断用户播放未强制关闭做新 runner 启动或正式覆盖安装。Android 通用版与 DiLink 均为 `versionName=1.0.10`、`versionCode=15`、最低 API 24、仅 `arm64-v8a`/`armeabi-v7a`、388 个 ZIP 条目、16KB zipalign、JNI Deflate 压缩且不含 x86/x86_64 或 `just_audio`；通用版 target 36、V2，DiLink target 29、V1-only，两包证书 SHA256 均为 `577bfbc4804601171fb48d191145b2f0d746a22c852124707ccd400dd835c9e3`。本机 `adb devices` 无连接设备，未做 Android/DiLink 真机安装。最终 `dist` 只保留三个固定文件：`dist/zmusic-windows-x64.exe` 大小 `16,497,879` 字节、MD5 `586865CF30DFB55A896E083FB21E3B88`、SHA256 `CCC437073E28776E82F9B5EB3DF48535EE8D25AB2449D6ADC7223F9912B64968`；`dist/zmusic-android.apk` 大小 `23,556,281` 字节、MD5 `24E42BC79A909C2C56D44685C422CD0D`、SHA256 `8B9E4F57BF5D11203722530C6F30A6088D8BEAF165CD94534746BB726BF07162`；`dist/zmusic-android-dilink.apk` 大小 `24,097,583` 字节、MD5 `EA4E91191CE2E90EBBBC1268864C5CF2`、SHA256 `47D8E4F2273F3C3D0F5008719341D96856DA998D341C7622117333B697991D8F`。

- 2026-07-15：按本轮反馈修正 Windows 安装完成页、搜索、详情页布局和播放队列交互。NSIS 自定义完成页移除内容区独立“打开 Zmusic”按钮，把向导右下角主按钮改为“完成并打开”，点击后退出安装器并启动应用；继续只使用 `MUI_ICON` / `MUI_UNICON` 注入安装器专用 BMP-ICO，测试编译后的 EXE 提取图标确认是绿色 Zmusic 音符。正式搜索改为歌曲 60 首/页：远端每页多取 1 首判断下一页并使用 `songOffset`，本地搜索同步分页；歌手和专辑结果在歌曲翻页时继续保留，从专辑/歌手详情或搜索结果返回首页会恢复“所有”分类，避免后续搜索只请求歌曲。最新专辑、随机专辑、最近播放、最多播放详情页增加对应页面标题且歌曲行隐藏封面；我的歌单、公共歌单详情页增加明确标题。搜索、分类、歌单、专辑、收藏和歌单工具页的返回按钮统一到页面最左侧并去除底色；专辑/歌单封面统一放大，标题字号增加 1，播放全部改为封面上的无底色图标，歌单编辑操作放在“歌单 · N 首”下方，歌曲区通过分割线与头部隔开并统一内容宽度。歌曲行播放、底部列表、设置页清除缓存/检查更新按钮去除底色，底部队列悬停提示统一为“列表”。歌曲右键/长按菜单新增“下一首播放”：曲目作为一次性临时项，不出现在可见队列，当前曲目完成后播放一次，再按原播放模式回到原队列；重新选队或停止会清理临时安排。新增远程搜索两页与歌手/专辑保留、临时下一首队列恢复、搜索分类复位、发现详情标题/无歌曲封面及新版布局断言；`dart format` 通过，`flutter analyze --no-pub` 无问题，`flutter test --no-pub` 133 项全部通过。NSIS 3.12 测试安装器编译成功，24 个 payload 文件，提取的 EXE 图标正确；测试安装器及隔离安装目录已清理。本轮未正式打包，版本保持 `1.0.10+15`，`dist` 未覆盖。

- 2026-07-15：新增全平台首页定制并持久化到 `AppSettings`。设置页新增“首页布局”：快捷入口“我喜欢的 / 我的歌单 / 公共歌单 / 公共电台”和发现模块“最新专辑 / 随机专辑 / 最近播放 / 最多播放”均可显示/隐藏、显示当前位置 1-4 并用上下按钮排序；底部“我的歌单 / 公共歌单”板块可独立隐藏。Windows、Android 手机和 Android 平板共用配置，旧配置默认维持原顺序且全部显示，损坏或缺项的排序会去重并自动补齐。音乐页按保存顺序动态组对发现模块，奇数个模块不保留空白半栏，全部快捷入口隐藏时不留下顶部间距。启动、登录和全量刷新统一按可见性裁剪远端请求：隐藏收藏、电台、最新/随机/最近/最多模块后不请求其接口；个人与公共歌单共用 `getPlaylists`，仅当两个歌单快捷入口和两个底部歌单板块全部隐藏时不请求；账号区所需歌曲总数继续加载。调整设置保存逻辑，仅缓存目录或上限变化时执行缓存整理，首页布局变化不再触发无关磁盘扫描。新增设置 JSON 往返/纠错、隐藏模块接口裁剪、Android 390px 设置交互和桌面首页排序/隐藏测试；`dart format` 通过，`flutter analyze --no-pub` 无问题，`flutter test --no-pub` 131 项全部通过。本轮未打包，版本保持 `1.0.10+15`，`dist` 未覆盖。

- 2026-07-15：完成歌单工具、首页与详情页的本轮交互调整并按要求保持 `1.0.10+15` 打包三端。合并歌单页新增来源/目标互换；加入前必须二次确认，默认保留源歌单歌曲，取消勾选后只在目标写入成功后从来源按倒序索引删除所选曲目。批量添加的曲库远程搜索增加 60 首/页分页，通过 Subsonic `songOffset` 获取后续结果，不再只停在第一页。同步歌单的“高品质优先”默认改为关闭，开始按钮居中；同步过程继续展示进度条和当前阶段，完成报告保留来源数、匹配数、新增数、已有数、未匹配数和重复跳过数。首页四个发现模块标题移除数字；“我的歌单 / 公共歌单 / 公共电台”分页显示明确标题；搜索栏旁新增全曲库刷新按钮，复用已有的歌手、专辑、歌单、数量、发现列表、收藏和电台并行分接口增量回填。专辑/歌单详情的返回、封面、标题信息和操作按钮改为顶部同行对齐，操作统一为圆形 tonal 图标按钮。Windows 搜索分类鼠标移入自动展开、移出延时收起，Android 保持点击交互。新增/更新回归测试覆盖互换、确认及源删除、`songOffset=60`、窄屏布局、模块标题、三类列表标题、详情顶部对齐和桌面悬停菜单；`dart format` 通过，`flutter analyze --no-pub` 无问题，`flutter test --no-pub` 127 项全部通过。Windows CMake/Ninja Release 和 NSIS 安装器构建成功，payload 24 个文件，运行程序 ProductVersion/FileVersion 为 `1.0.10+15`；检测到 `D:\Programs\Zmusic\zmusic.exe` 正在运行，为避免中断播放未强制关闭做安装烟测。Android 通用版和 DiLink 均为 `versionName=1.0.10`、`versionCode=15`、最低 API 24、双 ARM ABI、16KB zipalign、压缩 JNI，ZIP 388 项完整读取通过，包含媒体会话前台服务且不含 x86/x86_64 或 `just_audio`；通用版 target 36、V2，DiLink target 29、V1-only，两包签名证书一致。本机无 Android/DiLink 真机，未做真机安装验证。最终发布文件：`dist/zmusic-windows-x64.exe` 大小 `16,484,779` 字节、MD5 `DF2D7FBAC39ACF5DD6E4B52005A819C0`、SHA256 `60EEB8D06BAE9EDF201420B57ED686F6B3EA9357568A8592A5099E4A0CF73DCD`；`dist/zmusic-android.apk` 大小 `23,520,230` 字节、MD5 `CABADB77A7D9E72BB9A0C3E9E904B79B`、SHA256 `E66E08DBB793B1CD44E4C80629D68935DA422B54D1CBB21E885558CE4F9FD2F3`；`dist/zmusic-android-dilink.apk` 大小 `24,062,900` 字节、MD5 `399677D066692E30BF37AE7D95DA3444`、SHA256 `8BB43D7A66C4A527A11E60C8B089C4903D4CACCCB500FF5CCE26383EFE934E81`。

- 2026-07-15：将“同步歌单 / 合并歌单 / 批量添加歌曲”从小弹窗改为播放器内容区内的独立页面，保留底部播放栏。同步页新增自定义目标歌单名称、“允许匹配不同歌手的同名歌曲”和“高品质优先”选项；同曲多格式在高品质模式按 APE/FLAC/MP3 选择，关闭时按 MP3/FLAC/APE 兼容顺序选择。合并页改为左侧来源歌单歌曲、右侧目标歌单，且仅提交左侧勾选歌曲；批量添加页改为左侧曲库搜索多选、右侧目标歌单已有歌曲。双栏在 760px 以上左右排列，窄屏自动上下排列。登录页移除 Logo，`Zmusic` 标题字号增加 2 并接管 5 连击显示音源地址；地址新增隐藏按钮，隐藏后输入框和按钮同时消失。所有歌曲列表副信息只保留歌手，不再显示专辑和音源。测试时发现歌单行的 Ink 水波纹被 `DecoratedBox` 遮挡，改用带同等玻璃色、边框和圆角的 `Material` 表面。新增同步匹配策略、页面导航、桌面勾选传输和 390dp 窄屏布局测试；`dart format` 通过，`flutter analyze --no-pub` 无问题，`flutter test --no-pub` 126 项全部通过。按用户明确要求仅重打 Windows，版本保持 `1.0.10+15`，Android 两包未覆盖。Visual Studio x64 环境下 CMake/Ninja Release 构建成功；NSIS 隔离测试安装退出码 0，生成 `zmusic.exe`、`uninstall.exe`、`zmusic.installed`、`userdata` 和最新 `data/app.so`，程序 ProductVersion/FileVersion 均为 `1.0.10+15`，安装器可提取 32x32 应用图标；静默卸载后程序文件删除且 `userdata` 保留。检测到用户已有 `zmusic.exe`（PID 43540）运行，为避免打断播放未另启 Release 做运行烟测。最终 `dist/zmusic-windows-x64.exe` 大小 `16,461,270` 字节、MD5 `E560A944583CE68AB4FEACCC0BC6163D`、SHA256 `45FBBAFA6B27154B1DDBD274E1A822DFA909CC433085180C721233006038505A`。

- 2026-07-15：修复 Windows 安装器图标和运行中覆盖安装。原 `windows/runner/resources/app_icon.ico` 的帧均为 PNG 压缩格式，且普通 `Icon` 指令会被 Modern UI 页面宏覆盖，导致 EXE 文件和向导标题栏仍显示 NSIS 默认图标；新增仅供安装器使用的多尺寸 BMP-ICO `windows/installer/zmusic-installer.ico`，构建脚本改用该文件，NSIS 改为 `MUI_ICON` / `MUI_UNICON`，从最终 PE 资源提取并截图确认绿色音符 Logo 已用于安装器、标题栏和卸载器。覆盖安装现在强制关闭旧进程后额外等待 1 秒；清理旧 EXE、DLL、图标和 data 时每 500ms 自动重试，最长 10 秒，仍占用时提供“重试/取消”，释放后会自动继续。安装过程中不再提前删除 `zmusic.installed`，即使后续复制中断也保留受管目录身份；安全检查同时识别 `zmusic.exe`、卸载器、核心 Flutter DLL、`libmpv-2.dll`、文件选择插件和 `data/app.so`，因此此前失败后缺少 marker/data 的半安装目录可直接覆盖修复。隔离测试使用独占锁占用 `file_selector_windows_plugin.dll` 4 秒，安装器等待约 7.12 秒后自动完成覆盖；另行删除 marker、卸载器、data 和 EXE 模拟用户现有残留状态，修复安装成功且 `userdata` 全程保留。`flutter analyze --no-pub` 无问题，`flutter test --no-pub` 122 项全部通过。仅重打 Windows，版本保持 `1.0.10+15`，Android 两包未覆盖；最终 `dist/zmusic-windows-x64.exe` 大小 `16,455,539` 字节、MD5 `9E97E8CC62D7F9FCD6257D3F49226E9B`、SHA256 `21D34277F507C50703E1C0608BF5835A97CB520E09B7237FFAB5125CE785011A`。

- 2026-07-15：完成登录式单账号首页与音乐信息架构调整。应用首次启动显示 Logo、账号、密码和登录按钮，默认隐藏 `https://fnnav.zuitimes.com`，3 秒内连续点击 Logo 5 次可显示并修改服务地址；登录通过 `ping` 验证，成功后只保存当前远程账号并异步加载曲库，退出登录会停止播放、清除账号/服务器凭据并返回登录页。移除当前 UI 的多音源/本地音源入口和音乐页音源摘要，设置页改为账号、歌曲数、退出登录且不再显示搜索框；顶部合并为音乐/设置两页签，音乐页按“搜索与四个快捷入口 → 最新/随机专辑 → 最近/最多播放 → 我的歌单 → 公共歌单”排列，去掉旧歌手/专辑板块。新增原生 Dart 外部歌单同步，支持网易云和 QQ 公开歌单，按标题+歌手匹配当前 Navidrome、优先 MP3/FLAC/APE、并发搜索、去重和每 100 首批量写入；我的歌单列表同时新增歌单合并和批量添加，公共歌单继续只读。Windows NSIS 欢迎页和页眉新增品牌 Logo 位图。`dart format` 通过，`flutter analyze --no-pub` 无问题，`flutter test --no-pub` 122 项全部通过；Windows CMake/Ninja Release 构建成功。隔离 NSIS 测试安装验证首次安装、覆盖安装及卸载流程全部通过，三者均正确保留 `userdata`；实际打开正式安装器并截图确认品牌图和标准欢迎页。由于已安装的 `D:\Programs\Zmusic\zmusic.exe` 正在运行且客户端使用全局单实例锁，为避免中断播放未强制关闭做新 runner 原生启动烟测，登录页桌面/手机/`sw510dp` 布局由 widget 测试覆盖。本轮按用户明确要求只重打 Windows，版本保持 `1.0.10+15`，Android 两包未覆盖；产物 `dist/zmusic-windows-x64.exe` 大小 `16,098,320` 字节、MD5 `A90EA48600B5EB79D86CD7BFE34C088A`、SHA256 `9B24534C65CBF4638C9908A9281C35D00BD5DDC205A15565DA50EDF0679E3C2F`。

- 2026-07-15：按用户要求把 Windows 安装器从自定义 C# WinForms 自解压程序迁移为标准 NSIS 3 Modern UI，并删除已被替代的 `build/package/ZmusicSetup.cs`。新增 `windows/installer/ZmusicSetup.nsi` 和 `windows/installer/build-installer.ps1`，安装向导现在使用标准欢迎、目录、组件、真实解压进度和完成页面，进度页带“显示详情”，完成页同时提供“打开 Zmusic”和“完成”；桌面快捷方式默认勾选。继续支持记住上次安装目录、安装前关闭正在运行的 `zmusic.exe`、开始菜单/卸载注册项和 `uninstall.exe`，覆盖安装与卸载均保留 `userdata`。安装目录安全策略继续生效：磁盘根目录及 `D:\.` 等价形式自动转为 `D:\Zmusic`，非空且非受管目录返回退出码 2 且不修改原文件。已安装官方 NSIS 3.12 到 `D:\Program Files\NSIS`；PowerShell 构建脚本解析通过，测试版和正式宏分支均编译成功，24 个 payload 文件压缩后的 NSIS EXE 约 15.3 MiB且不含 `just_audio`。项目内映射盘隔离回归验证非空目录拒绝、首次安装、覆盖安装清除旧 DLL、卸载、根目录规范化和 `userdata` 保留全部通过；实际打开向导并截图确认组件页、真实解压进度条及完成页布局。`flutter analyze --no-pub` 无问题，`flutter test --no-pub` 118 项全部通过。本轮未正式打包，版本保持 `1.0.10+15`，`dist` 三个既有发布包 SHA256 均未变化。

- 2026-07-15：将 Windows 单文件 EXE 安装器改为常见的三阶段安装向导。初始页保留安装目录、浏览和桌面图标选项；点击“安装”后不再关闭窗口或卡住 UI，安装任务转到后台线程，页面切换为安装位置、状态文字和进度条，安装期间阻止误关窗口。进度不是假延时：payload 解压阶段按实际已写入字节在 15%~85% 之间推进，其余进度对应目录检查、旧版清理、卸载器/快捷方式创建和卸载信息注册。到达 100% 后才显示“完成”和“打开 Zmusic”两个按钮，取消原有的安装前“安装完成后打开”勾选和完成消息框。使用隔离假 payload 完成 C# DLL/WinExe 编译，验证字节进度单调并到达 85% 解压里程碑，反射验证“选项 → 进度 → 完成操作”控件状态，并对三种页面做了截图检查；上一轮的磁盘根目录规范化、非受管目录拒绝和 `userdata` 保留策略回归通过。`flutter analyze --no-pub` 无问题，`flutter test --no-pub` 118 项全部通过。本轮未正式打包，版本保持 `1.0.10+15`，`dist` 未覆盖。

- 2026-07-15：修复 Windows 首次安装时把磁盘根目录当作应用目录，进而尝试清理 `D:\$RECYCLE.BIN` 等系统目录的高风险问题。根因是 `build/package/ZmusicSetup.cs` 无条件枚举并删除用户选择目录的内容；现在选择或输入 `D:\` 这类磁盘根目录时会自动规范为 `D:\Zmusic`，新安装仅允许不存在、空目录或仅保留 `userdata` 的目录，非空且非 Zmusic 受管目录会明确拒绝且不删除任何文件。覆盖安装/卸载的删除入口增加二次校验：必须同时存在 `zmusic.installed` 和 `zmusic.exe`/`uninstall.exe`，并且绝不能是磁盘根目录；单独存在 `app_icon.ico` 不再被识别为安装目录。使用 C# 反射策略测试验证根目录自动追加 `Zmusic`、空目录/`userdata` 重装可用、普通非空目录拒绝且文件保留、伪图标目录不受信任，以及受管目录清理后仅保留 `userdata`；安装器 C# 源码编译通过，`flutter analyze --no-pub` 通过，`flutter test --no-pub` 118 项全部通过。本轮未打包，版本保持 `1.0.10+15`，`dist` 未覆盖。

- 2026-07-14：按用户明确要求将当前三个发布包的可见版本升级为 `1.0.10`，内部 build/versionCode 升为 15，即 `1.0.10+15`；同步更新 `pubspec.yaml`、Windows/Android 构建配置、设置页版本测试和后续默认打包版本规则。首次全量测试时，更新检查测试夹具的远端版本仍为 `1.0.10`，与新客户端同版本而按正确逻辑不再弹更新；将测试用远端版本调整为 `1.0.11` 后，`dart format` 无额外改动，`flutter analyze --no-pub` 无问题，`flutter test --no-pub` 118 项全部通过。Windows CMake/Ninja Release 构建通过，安装器内 `zmusic.exe` ProductVersion/FileVersion 均为 `1.0.10+15`；payload 共 24 个运行文件，内嵌 SHA256 与源 zip 完全一致，包含 `data/app.so`、`libmpv-2.dll` 和 `app_icon.ico`，不含 `userdata`、安装标记或 `just_audio`。已安装的 `D:\Programs\Zmusic\zmusic.exe` 仍在运行，为避免中断播放未强制关闭做新 runner 启动烟测。Android 通用版与 DiLink 均为 `versionName=1.0.10`、`versionCode=15`、最低 API 24、双 ARM ABI、16KB zipalign、压缩 JNI，均包含媒体播放前台服务和权限，388 个 ZIP 条目完整读取通过且不含 x86/x86_64 或 `just_audio`；通用版 target 36、V1+V2，DiLink target 29、V1-only，两包签名证书一致。本机无 Android/DiLink 真机，未做真机安装和蓝牙耳机验证。临时载荷已清理，`dist` 最终只保留：`dist/zmusic-windows-x64.exe` 大小 `20,978,688` 字节、MD5 `BA846B109637376ED4AB8F30E8CFE581`、SHA256 `8E9E8DAC7A375F55EAB409BC766688826F91A3EF0F72805713E9326C8915ECFC`；`dist/zmusic-android.apk` 大小 `23,430,916` 字节、MD5 `87B30C305805CF2060DD60E7A9544446`、SHA256 `357DE6B4BAAEB3DC9169457D04F8ED87222EB26874584A2AF2267CC4676478A1`；`dist/zmusic-android-dilink.apk` 大小 `23,973,873` 字节、MD5 `009F46F30744B270D2E20DFAEF17C4EC`、SHA256 `860513A8590B9745307F7A2294B3AFC2182BBB25ADABF1DB93C416F90A011C69`。

- 2026-07-14：按默认发布规则为系统媒体控制/蓝牙耳机线控版本完成三端打包。可见版本保持 `1.0.9`，内部 build/versionCode 从 13 升到 14，并同步 `pubspec.yaml`、测试默认包信息、Windows 生成配置和 Android 本地构建配置。`dart format` 无额外改动，`flutter analyze --no-pub` 无问题，`flutter test --no-pub` 118 项全部通过。Windows 使用 CMake/Ninja 重新生成并编译 Release runner，`zmusic.exe` ProductVersion/FileVersion 均为 `1.0.9+14`；安装器 payload 共 24 个运行文件，包含 `data/app.so`、`libmpv-2.dll`、`app_icon.ico`，不含 `userdata`、安装标记、嵌套发布包或 `just_audio`，安装器内嵌 payload SHA256 与源 zip 完全一致。检测到已安装的 `D:\Programs\Zmusic\zmusic.exe` 正在运行，为避免中断当前播放未强制关闭做新 runner 启动烟测。Android 通用版和 DiLink 均为最低 API 24、版本码 14、仅 `arm64-v8a`/`armeabi-v7a`、16KB zipalign、压缩 JNI，均包含 `ZmusicMediaSessionService` 和媒体播放前台权限，388 个 ZIP 条目完整读取通过且不含 x86/x86_64 或 `just_audio`；通用版 target 36、V1+V2 签名，DiLink target 29、V1-only，两包证书一致。本机仍无 Android/DiLink 真机，未做蓝牙耳机及真机安装验证。已清理临时 payload/重签名文件，`dist` 最终只保留三个固定文件：`dist/zmusic-windows-x64.exe` 大小 `20,978,688` 字节、SHA256 `2C85A28182F96B82F0063433FB4567E0A5F4AF1299521D79D5FC93E087100C4C`；`dist/zmusic-android.apk` 大小 `23,430,922` 字节、SHA256 `42C6F8EB442CEDDB2D6F73360E062A444370A87C73DDBB64DDF44FA3A4F4B100`；`dist/zmusic-android-dilink.apk` 大小 `23,973,874` 字节、SHA256 `A7C920203EB7F201888A361168929C406AF7332098B431DD050E3F4834312A9D`。

- 2026-07-14：为 Windows 和 Android 补齐系统媒体控制及蓝牙/有线耳机线控。Flutter 新增统一 `SystemMediaControls` 平台通道，向原生端同步当前歌曲、歌手、专辑、播放状态、位置、时长和可切歌状态，并把播放、暂停、播放/暂停切换、上一首、下一首指令路由回 `PlayerController`；播放器新增幂等的显式 `play()` / `pause()`，避免原生重复暂停事件反而恢复播放。Windows runner 接入 C++/WinRT SMTC，并以 `WM_APPCOMMAND` 作为系统不支持 SMTC 时的媒体键兜底；Android 新增 `MediaSession` 媒体播放前台服务和 MediaStyle 通知，支持应用在后台播放时由蓝牙耳机、锁屏及通知栏控制。新增平台通道状态同步和五类媒体指令回归测试。验证：`dart format` 通过，`flutter analyze --no-pub` 无问题，`flutter test --no-pub` 118 项全部通过；Windows CMake/Ninja Release runner 编译链接通过；Android Release APK 编译通过，APK Manifest 确认最低 API 24、目标 API 36，并包含媒体播放前台权限与服务声明。当前环境没有可连接的实体蓝牙耳机，原生硬件按键仍需设备实测；本轮未打包、未覆盖 `dist`，版本保持 `1.0.9+13`。

- 2026-07-14：排查并修复播放期间偶发界面掉帧及可能伴随音频短促卡顿的高频更新链路。根因是 `media_kit` 的播放位置、缓冲位置以及本地流式缓存的每个网络分块都会经 `PlayerController -> AppController` 扩散为整棵应用界面刷新；Windows 桌面集成还会在每次无关状态变化时同步检查托盘图标文件，而本地音频缓存代理与 Flutter UI 共用 Dart isolate，密集重建可能延迟代理向 libmpv 供数。现在播放位置和普通缓冲位置只通过各自 StreamBuilder 更新进度条/歌词，不再触发全局 ChangeNotifier；缓存进度默认最多每 100ms 更新一次且完成值强制送达，音频字节流和边播边缓存逻辑不变；托盘图标仅在配置路径变化时重新检查。新增测试覆盖位置/缓冲 tick 不触发全局通知、缓冲状态仍触发通知，以及缓存进度节流且必定发出 100%。`dart format` 通过，`flutter analyze --no-pub` 无问题，`flutter test --no-pub` 117 项全部通过；本轮未打包，版本保持 `1.0.9+13`。
- 2026-07-14：按要求调整后续 Android 通用版打包规则。`android/app/build.gradle.kts` 的 `jniLibs.useLegacyPackaging` 改为所有构建固定启用，通用版和 DiLink 都会压缩 `libflutter.so`、`libapp.so`、`libmpv.so` 等 JNI 原生库并在安装时解压；通用版仍保留 `targetSdk 36` 和 V1+V2 签名，DiLink 仍保留 `targetSdk 29` 与 V1-only 签名，双 ARM ABI 规则不变。预计后续通用 APK 将从约 47 MiB 降至约 23～25 MiB，安装后存储占用不一定降低。Android `gradlew help --no-daemon` 配置解析成功，仅有项目既有 Kotlin/AGP 弃用警告；`flutter analyze --no-pub` 通过，`flutter test --no-pub` 115 项全部通过。本轮只固定构建规则，未打包，版本保持 `1.0.9+13`，现有 `dist` 未覆盖。
- 2026-07-14：完善电台播放状态、收藏反馈、默认音量和外观设置。点击任一电台后会把当前音源全部具有播放地址的频道组成队列并选中点击项，M3U/PLS 只在对应频道实际播放时解析；电台播放统一切到顺序模式，播放栏和移动播放详情中的播放模式、收藏、拖动进度均禁用，左右时间固定为 `00:00`，队列入口改为“电台频道”并展示全部频道，上一台/下一台仍可用。收藏/取消收藏不再复用全局 `isBusy`，只保留心形按钮自身的 pending 动画，因此搜索或详情页顶部不再出现线性进度条。应用启动时播放器和桌面音量控件统一初始化为 55%。`AppSettings` 新增独立 `backgroundPath`，设置页将“应用 Logo / 背景图”拆为“应用 Logo”和“背景图”两个上传入口；背景全屏填充、Logo 为独立淡化品牌层，旧设置仅在 JSON 缺少新字段时沿用原图片作为背景，明确留空不会被错误迁移。新增电台三频道队列与禁用控件、收藏无全局 busy、55% 音量、双图片路径持久化/旧配置迁移及双入口测试。`dart format` 通过，`flutter analyze --no-pub` 通过，`flutter test --no-pub` 115 项全部通过；本轮未打包，版本保持 `1.0.9+13`，现有 `dist` 未覆盖。
- 2026-07-14：修复网络歌曲偶发首次打开后一直缓冲、手动切到下一首再返回却可正常播放的问题。根因是播放器或本地缓存代理的单次启动请求可能既不报错也不推进播放位置，原逻辑没有超时恢复；`PlayerController` 现在仅在同一播放请求仍有效、位置未超过 1 秒且打开操作仍未返回或播放器持续处于播放中缓冲时，等待 12 秒后自动停止并重开当前歌曲一次。恢复重试不改变队列、不切歌、不创建新的播放会话，避免重复上报 Navidrome“正在播放”；播放位置正常推进、用户主动暂停、切歌或请求已过期都会取消恢复。播放引擎接口增加测试注入能力，新增回归测试覆盖卡死后只重试一次以及暂停后不重试。`dart format` 通过，`flutter analyze --no-pub` 通过，`flutter test --no-pub` 112 项全部通过；本轮未打包，版本保持 `1.0.9+13`，现有 `dist` 未覆盖。

- 2026-07-14：修复播放详情元数据布局与导航，并新增手动更新检查后按新规则完成三端打包。桌面播放详情的专辑、歌手和来源改为同一行弹性布局，移动端歌手/专辑也保持单行；专辑和歌手恢复主题绿色与加粗可点击样式，过长时各自省略。点击歌手/专辑后立即显示对应分类的“搜索结果”页并在该页等待远程结果，不再先闪到搜索首页。新增 `AppUpdateService`，设置页“关于”区域提供“检查更新”：固定读取 `https://file.zuitimes.com/zmusic/update.json`，Windows、Android 通用版、DiLink 分别读取 `windows`、`android`、`android-dilink` 节点；Android 由原生通道根据构建 target SDK 自动识别 DiLink。更新判断只比较 `latestVersion`，避免服务端示例 `versionCode=109` 将同为 `1.0.9` 的客户端误判为更新；发现新版本时展示版本、发布时间、更新内容和“下载更新”，通过 `url_launcher` 交给系统浏览器下载。公网清单实测 HTTP 200、`application/octet-stream`，且当前文件包含尾随逗号，解析器已兼容 UTF-8 二进制响应和尾随逗号。新增清单解析、语义版本比较、设置弹窗、元数据单行/颜色和慢搜索直接结果页测试；`dart format`、`flutter analyze --no-pub` 均通过，`flutter test --no-pub` 110 项全部通过。可见版本保持 `1.0.9`，内部 build/versionCode 从 12 升至 13；Windows CMake/Ninja Release、Android 通用 Release 和 DiLink Gradle Release 均构建成功。Windows payload 共 24 个文件，包含 `url_launcher_windows_plugin.dll`，不含 `just_audio`、用户数据或安装标记；内嵌 payload SHA256 与临时源 zip 完全一致，内部 `zmusic.exe` ProductVersion/FileVersion 均为 `1.0.9+13`。Android 两包均为最低 API 24、仅 `arm64-v8a`/`armeabi-v7a`、16KB zipalign 且不含 x86/x86_64 或 `just_audio`；通用包 target 36、V1+V2、`libmpv.so` 未压缩，DiLink 包 target 29、V1-only、`libmpv.so` 压缩。按新固定命名清理旧 `dist` 和临时绿色载荷，最终只保留：`dist/zmusic-windows-x64.exe` 大小 `20,131,840` 字节、SHA256 `06F55F6F4C883AC89DEF441F840AE0A73A34F60776E29B5D0C51D53F5DEF005B`；`dist/zmusic-android.apk` 大小 `49,276,178` 字节、SHA256 `166C2E2AB62D0E42DD2208227CDF45F6D7C630633350247CA1E8AA6C9605A7EE`；`dist/zmusic-android-dilink.apk` 大小 `23,957,350` 字节、SHA256 `986F2CB045309ADFE4B8A01FF53E7AFA537736363DDBA6B24F9E88E40CF7390C`。检测到已安装的 `D:\Programs\Zmusic\zmusic.exe` 正在运行，为避免中断用户播放未关闭它做新 runner 启动烟测；本机无 Android/DiLink 真机，未做真机安装验证。
- 2026-07-14：按用户明确要求将当前 Windows、Android 通用版和 Android DiLink 版的可见版本统一为 `1.0.9`；为保证 Android 可以覆盖已有 `versionCode=11` 的安装，内部版本码使用 `12`，即项目版本为 `1.0.9+12`，同步更新测试默认包信息和 Windows 生成配置。`dart format` 通过，`flutter analyze --no-pub` 通过，`flutter test --no-pub` 107 个测试全部通过。Windows CMake/Ninja Release 构建通过，`zmusic.exe` ProductVersion/FileVersion 均为 `1.0.9+12`；重建干净绿色目录和 zip，压缩包共 23 个运行文件，不含用户数据、安装标记、构建目录、嵌套包或 `just_audio`，安装器内嵌 payload 与绿色 zip 的 SHA256 一致，隔离数据目录启动烟测存活 5 秒。Android 通用 APK 为最低 API 24、目标 API 36、双 ARM ABI、未压缩 `libmpv.so`，使用同一 debug 证书完成 V1+V2 双签名；DiLink APK 为最低 API 24、目标 API 29、双 ARM ABI、压缩 `libmpv.so`、V1-only，同一签名证书；两个 APK 的 16KB zipalign、签名、版本、ABI、ZIP 完整性均通过且不含 x86/x86_64 或 `just_audio`。首次使用项目隔离 Gradle 缓存构建 DiLink 因缺少 Kotlin 2.2.0 artifact 失败，改用 Flutter 通用构建已使用的用户 Gradle 缓存后成功，未修改依赖。已清理 `dist` 中旧版本安装器和旧 DiLink 包；最终产物：`dist/zmusic-windows-x64.zip` 大小 `20,857,886` 字节、SHA256 `662A5E86FE2D0A356007FB2E2FD43FA739F4FD93A4E0823A01A3D1B6379FED23`；`dist/zmusic-windows-x64-setup_1_0_9.exe` 大小 `20,874,752` 字节、SHA256 `34C48975EE3AD5ADB7BF128F09D4AADE1AFAAA6E0A0351F3919EDF82844C9A50`；`dist/zmusic-android-release.apk` 大小 `49,189,685` 字节、SHA256 `5E4CEEF31E072B970B8E24347A5E0CB04B3F94AE87006D85C2FACE9E3F49528C`；`dist/zmusic-android-dilink-release_1_0_9.apk` 大小 `23,926,267` 字节、SHA256 `4E2C5EA4C4AAAFE2382737354D9AA8205903DCB699A9AB2C29589410A6FEAC3E`。
- 2026-07-13：修复 Windows 托盘右键菜单持续闪烁及不易关闭的问题。根因是 `DesktopIntegration` 监听整个 `AppController`，播放器位置等高频状态通知会反复调用 `trayManager.setContextMenu`，原生菜单显示期间其菜单项被持续移除并重建。现在托盘菜单只在桌面集成初始化时创建，控制器变化只检查图标，托盘提示也只设置一次；右键弹出增加单菜单保护，Windows 使用前台所有者弹出原生菜单，使其在失去焦点或点击外部时正常关闭。新增平台通道回归测试，覆盖播放状态变化不重建菜单、连续右键只弹出一个菜单及 Windows 前台参数。验证：`dart format` 通过，定向托盘测试通过，`flutter analyze --no-pub` 通过，`flutter test --no-pub` 107 个测试全部通过，Windows CMake/Ninja Release 构建通过并同步最新 `app.so` 到 runner；本轮未打包，版本保持 `1.0.10+11`。
- 2026-07-13：为全平台顶部搜索框增加 Navidrome/Subsonic 远程输入联想。输入内容含任意汉字时从 1 个汉字开始请求；纯字母/数字按有效字符计数，满 3 个才请求；使用 300ms 可取消防抖，在输入变化、清空、失焦、分类切换和组件销毁时取消尚未发出的请求，避免无效网络调用和异步结果覆盖。建议查询独立于正式搜索，不设置全局忙碌状态、不覆盖 `searchResults`，本地音源不发远程请求；`SubsonicApiClient.search` 支持自定义歌曲/歌手/专辑数量，“所有”单次配额为歌曲 2、歌手 2、专辑 1，指定分类时该分类取 5 条，输入框下方浮层最终最多显示 5 条。建议行显示歌曲/歌手/专辑图标、名称和分类信息，点击后按对应关键词与分类进入正式搜索结果页。新增测试覆盖 5 条远程请求配额、正式结果不被联想污染、两位英文数字不触发、三位触发、单个汉字触发、5 条浮层位置、歌手建议分类跳转，以及 Android 390dp 窄屏不越界。验证：`dart format` 通过，`flutter analyze --no-pub` 通过，`flutter test --no-pub` 106 个测试全部通过；本轮未打包，版本保持 `1.0.10+11`。
- 2026-07-13：按要求全平台移除歌曲分享功能，并修复播放详情页专辑/歌手点击看似无响应的问题。删除播放详情分享按钮、歌曲右键/长按菜单分享项、`AppController` 分享能力与 `SubsonicApi.createShare`；专辑和歌手元数据链接改为更大的 `InkWell` 点击区域，点击后先同步切换到搜索页并写入关键词/分类，再异步请求搜索结果，避免慢接口阻塞页面跳转，歌手默认进入歌手分类、专辑默认进入专辑分类。新增慢搜索未完成时也立即完成导航的 widget 测试，并更新无分享入口测试。打包校验时发现 Flutter Gradle 插件会覆盖 `ndk.abiFilters`，因此将 `android/app/build.gradle.kts` 的 x86/x86_64 JNI 排除改为所有构建无条件生效，最终通用 APK 确认只含 `armeabi-v7a` 和 `arm64-v8a`，体积从约 76.8 MB 降到 49.1 MB。版本从 `1.0.9+10` 递增到 `1.0.10+11`；`dart format` 通过，`flutter analyze --no-pub` 通过，`flutter test --no-pub` 103 个测试全部通过。Windows CMake/Ninja Release 构建通过，`zmusic.exe` ProductVersion/FileVersion 均为 `1.0.10+11`；绿色 zip 共 28 个条目，包含 `data/app.so`、`libmpv-2.dll` 和图标，不含用户数据、安装标记、嵌套安装包或 `just_audio`，版本化安装器确认内嵌最新 `payload.zip`。Android 通用包为最低 API 24、目标 API 36、V1+V2 签名、双 ARM ABI、未压缩 JNI；DiLink 包为最低 API 24、目标 API 29、V1-only、双 ARM ABI、压缩 JNI；两个 APK 的 16KB zipalign、签名、版本、ABI 和 `libmpv.so` 检查均通过且不含 `just_audio`。检测到安装版 `D:\Programs\Zmusic\zmusic.exe` 正在运行，为避免中断用户播放未强制关闭做绿色版启动烟测；无真实 DiLink 车机环境做安装验证。产物：`dist/zmusic-windows-x64.zip` 大小 `20,840,598` 字节、SHA256 `5EFCF71CE066CDE10B98B29B1982119048FD01EE733041799D5B6399326A4C28`；`dist/zmusic-windows-x64-setup_1_0_10.exe` 大小 `20,877,312` 字节、SHA256 `AE6C197C74A32263398F7E94943EA9464DB2E776EA1245C57F646832950F680B`；`dist/zmusic-android-release.apk` 大小 `49,142,366` 字节、SHA256 `5E8808EEA547194F5078758E1BB97C261C8A73BB566094EDAA1C0E1E3847AF1A`；`dist/zmusic-android-dilink-release_1_0_10.apk` 大小 `23,887,408` 字节、SHA256 `64C2CDF2AD71C607AC3D00E39C386636D899B84E663937982C3EBFABB2D43E75`。
- 2026-07-13：固定 Android 通用包的 ARM-only 规则，但本轮不打包。`android/app/build.gradle.kts` 将 `ndk.abiFilters` 改为所有 Android 构建统一只包含 `armeabi-v7a` 和 `arm64-v8a`，后续通用 APK 不再适配 `x86_64`；通用版仍保持现代 JNI 打包方式，只有 `-PzmusicDiLinkCompatibility=true` 的 DiLink 包使用压缩 JNI并在安装时解压。同步更新默认打包规则说明；版本保持 `1.0.9+10`，现有 `dist` 产物未覆盖。验证：`flutter analyze --no-pub` 通过，`flutter test --no-pub` 104 个测试全部通过，Android `gradlew help` 配置解析通过；未生成任何新安装包。
- 2026-07-13：按要求恢复并固化 DiLink 默认打包。默认“打包”清单现在包含 Windows 绿色 zip、Windows 安装版、Android 通用 APK 和 Android DiLink 兼容 APK；DiLink 文件名固定使用版本后缀 `dist/zmusic-android-dilink-release_<major>_<minor>_<patch>.apk`。确认 DiLink 体积较小不只因为压缩：兼容包仅保留 `arm64-v8a`、`armeabi-v7a`，并用旧式 JNI 打包压缩 `libmpv.so`，由系统安装时解压；通用包继续保留三 ABI 和现代原生库直接加载方式，避免无明确要求时改变安装/启动行为。版本从 `1.0.8+9` 递增到 `1.0.9+10`，`dart format test/widget_test.dart` 无格式变化，`flutter analyze --no-pub` 通过，`flutter test --no-pub` 104 个测试全部通过。Android 通用包为 `versionName=1.0.9`、`versionCode=10`、最低 API 24、目标 API 36、三 ABI、V1+V2 签名；DiLink 包为同版本、最低 API 24、目标 API 29、两种 ARM ABI、V1-only 签名和压缩 JNI。两个 APK 的 16KB zipalign、签名、ZIP 内容和 `libmpv.so` 检查均通过，均不含 `just_audio`；本轮未打包 Windows，且无真实 DiLink 车机环境做安装验证。产物 `dist/zmusic-android-release.apk` 大小 `76,807,637` 字节、SHA256 `ACDED636648344BD6A1BB70E03BE8971CF764DEA9671F1F3D43F27A24402F63C`；`dist/zmusic-android-dilink-release_1_0_9.apk` 大小 `23,890,635` 字节、SHA256 `AAE6FF84F8E667A14CF04BFFF719E08C0BBE75101A230FA4090CE0B63CFC4305`。
- 2026-07-13：音乐页签歌单预览数量从 5 个增加到 9 个；歌单区标题右侧保留“新建歌单”加号，并在其右边新增“更多”，点击后复用音乐页“歌单”入口进入同一个完整歌单列表页。新增 widget 测试覆盖前 9 个预览、创建/更多按钮顺序及第 10 个歌单只在完整列表显示。版本从 `1.0.7+8` 递增到 `1.0.8+9`；`flutter analyze --no-pub` 通过，`flutter test --no-pub` 104 个测试全部通过。Windows CMake/Ninja Release 构建、安装组件同步和隔离数据目录启动烟测通过，绿色 zip 共 28 个条目且不包含 `userdata`、安装标记或 `just_audio`；版本化安装器确认嵌入最新 `payload.zip`。Android 通用 release APK 为 `versionName=1.0.8`、`versionCode=9`、最低 API 24、目标 API 36，16KB zipalign、V1/V2 签名验证通过，包含 `armeabi-v7a`、`arm64-v8a`、`x86_64` 三个 ABI 的 `libmpv.so` 且不含 `just_audio`。已打包 `dist/zmusic-windows-x64.zip`、`dist/zmusic-windows-x64-setup_1_0_8.exe`、`dist/zmusic-android-release.apk`；SHA256 分别为 `1615F4D169AF496F3AF4DF5F46224C25FED7E659463267EA96E4B5CB8EDAA89A`、`4451F26F77AF91C0D02DBE3F0F1313D2A87CDEBE4437904979D46B1B6995C935`、`94A8CE0D5252925671F57DF423F025376FFFFB87FB159D49831E438E9BA68BBA`。
- 2026-07-13：新增 Navidrome/Subsonic 歌曲公开分享与播放详情元数据搜索导航。`SubsonicApiClient` 新增 `createShare` 调用，支持服务端返回绝对或相对分享地址；`AppController` 按歌曲的 `sourceServerId` 选择正确音源账号生成分享，避免同 URL 多账号时串用凭据。本地歌曲不提供分享；远端歌曲可从桌面右键菜单、Android 长按菜单和播放详情分享按钮生成公开链接，成功后直接复制到剪贴板并提示。播放详情中的歌手、专辑名称改为可点击文本：歌手按歌手名执行 `artists` 范围搜索并默认打开歌手页签，专辑按专辑名执行 `albums` 范围搜索并默认打开专辑页签，同时顶部切换到搜索页。新增测试覆盖 `createShare` 参数/相对 URL、多账号音源选择、分享菜单复制反馈和歌手/专辑页签导航。验证：`dart format` 通过，`flutter analyze --no-pub` 通过，`flutter test --no-pub` 103 个测试全部通过；本轮未打包，版本保持 `1.0.7+8`。
- 2026-07-13：优化收藏/取消收藏反馈。`AppController` 新增按单曲维度的收藏切换 pending 状态和重复点击保护，收藏接口请求未完成时同一首歌不会重复发送 `star/unstar`；歌曲列表、底部播放栏、移动迷你播放条和播放队列共用新的心形填充动画，收藏时实心心从空心中逐步填满，取消收藏时逐步变回空心，并在请求期间显示“正在收藏 / 正在取消收藏”提示。更新 widget 测试覆盖取消收藏加载态、收藏加载态、重复点击只发一次请求，并为播放详情歌手/专辑链接测试补一帧等待稳定搜索结果返回按钮。验证：`dart format lib/src/app_controller.dart lib/src/app.dart test/widget_test.dart` 通过，定向收藏相关测试通过，`flutter analyze --no-pub` 通过，`flutter test --no-pub` 103 个测试全部通过；本轮未打包。
- 2026-07-11：针对比亚迪汉 DM-i DiLink 在 `1.0.6` 双签名 APK 上仍提示“解析软件包出现问题”增加独立兼容构建，同时保留普通 Android 包。先确认旧包 ZIP/16KB 对齐、Manifest、ARM ABI 及 V1/V2 签名本身均有效，本机无 Android 模拟器或已连接设备可复现真实安装。`android/app/build.gradle.kts` 新增 `-PzmusicDiLinkCompatibility=true` 构建开关：兼容包使用 Android 10 对应的 `targetSdk 29`、V1-only 签名、`extractNativeLibs=true` 的旧式 JNI 打包，并排除 x86/x86_64，只保留 `armeabi-v7a` 和 `arm64-v8a`；普通包继续使用 `targetSdk 36`、V1+V2 和三 ABI，不受兼容配置影响。版本从 `1.0.6+7` 递增到 `1.0.7+8`，同步更新测试默认版本和 Windows 生成配置。验证：`dart format test/widget_test.dart` 无格式变化，最终 `flutter analyze --no-pub` 通过，`flutter test --no-pub` 100 个测试全部通过；普通 `flutter build apk --release --no-pub` 通过；使用 Android Studio JBR 17 执行 `android/gradlew.bat app:assembleRelease -PzmusicDiLinkCompatibility=true --no-daemon` 生成兼容包，Gradle 构建、7-Zip 完整性、zipalign 和 apksigner 均通过。普通包 `dist/zmusic-android-release.apk` 大小 `76,774,774` 字节，SHA256 `2EC987036C19439006CB08D9E776A9C307E74C45EA0C2D41BE1DA97EB24CCD89`；DiLink 包 `dist/zmusic-android-dilink-release_1_0_7.apk` 大小 `23,883,477` 字节，SHA256 `AD59EF64A7BB0843A9541AE68C5BD96DBDE20B11093F7534929DAB1EBDE8F5A6`。本轮未打包 Windows；无真实比亚迪车机环境，兼容包安装结果需车机实测。
- 2026-07-10：为比亚迪 DiLink 等定制 Android 安装器补充 APK 签名兼容，并按用户明确要求只重新打包一个通用 Android 版本。`android/app/build.gradle.kts` 对现有 debug/release 签名配置显式启用 V1 JAR 签名和 V2 APK 签名；最低系统仍为 Android 7.0/API 24，Target SDK 仍为 36，ABI 仍包含 `armeabi-v7a`、`arm64-v8a`、`x86_64`。版本号从 `1.0.5+6` 递增到 `1.0.6+7`，同步更新测试默认版本和 Windows 生成配置。旧 `1.0.5` APK 强制按 API 23 验证时报 `Missing META-INF/MANIFEST.MF`，新 APK 按同一方式验证 V1、V2 均为 `true`，确认不是只修改配置而未生效。验证：`dart format test/widget_test.dart` 无格式变化，`flutter analyze --no-pub` 通过，`flutter test --no-pub` 100 个测试全部通过，`flutter build apk --release --no-pub` 通过；7-Zip 完整性检查 392 个条目全部正常，包含 3 个 ABI 的 `libmpv.so` 且不含 `just_audio`。已覆盖 `dist/zmusic-android-release.apk`，大小 `76,774,780` 字节，SHA256 `B1A0B125F30A98D35D32CC68E9F359A99AAB19601D6745BC89DA68AA3554412C`；本轮未打包 Windows。
- 2026-07-10：按反馈把设置页音源操作改为全平台紧凑标题行，并完成默认打包。“音源 / 导入 / 添加”现在处于同一水平中心线；导入、远程添加和 Windows 本地添加按钮统一使用 18px 图标、紧凑视觉密度、4px 垂直/8px 水平内边距和收缩点击尺寸，按钮实际高度不超过 36px。Android 手机明确只显示“导入 / 添加”，Android `390px` 手机、`sw510dp` 平板和 Windows 均新增/更新布局断言；先确认旧布局标题与按钮中心线相差 40px，再完成修复。版本号从 `1.0.4+5` 递增到 `1.0.5+6`；`dart format lib/src/app.dart test/widget_test.dart` 通过，`flutter analyze --no-pub` 通过，`flutter test --no-pub` 100 个测试全部通过。使用 Visual Studio Build Tools `vcvars64` + CMake/Ninja 构建并安装 Windows Release，确认 `zmusic.exe` ProductVersion/FileVersion 均为 `1.0.5+6`；Windows zip 共 28 个条目，包含 `data/app.so` 和 `libmpv-2.dll`，不包含用户数据、安装标记、嵌套包或 `just_audio`；绿色版使用项目内隔离数据目录启动并存活 5 秒，烟测通过。安装器命名规则改为追加语义版本下划线后缀，本轮只生成 `dist/zmusic-windows-x64-setup_1_0_5.exe`，旧无版本安装包已移除，安装器确认嵌入最新 `payload.zip`。Android release APK 为 `versionName=1.0.5`、`versionCode=6`，包含 3 个 ABI 的 `libmpv.so` 且不含 `just_audio`；手机和平板继续使用同一通用 APK。已打包 `dist/zmusic-windows-x64.zip`、`dist/zmusic-windows-x64-setup_1_0_5.exe`、`dist/zmusic-android-release.apk`。产物 SHA256：Windows zip `1F26758B22DB2FDFB8034DCC9074C6D00857686C451983741BA78DC4EA4AA6DF`，Windows 安装包 `5930F12CA8181D5F054237F24557D71076E8F7A3AFCF2894F162251E9878D50F`，Android APK `C99AA60F35B24D57C28C975019322BE94D9E2E603B4F906DD66AB69A5DAA3854`。
- 2026-07-10：按反馈精简音源与移动播放界面并完成默认打包。设置页音源区删除重复的“音源设置”列表项，音源行删除右侧“当前音源”圆形对钩，当前选择只由左侧单选框表示；音乐页音源摘要删除账号文案，只显示音源名称和歌曲数；移动端播放详情删除与页签重叠的外层返回按钮，只保留页签行内返回按钮，使其与“歌曲 / 歌词”处于同一水平线。修复设置页版本长期显示 `1.0.0+1`：根因是 `_appVersion` 写死，新增轻量 `package_info_plus 10.2.0` 读取 Windows/Android/iOS 实际包版本，设置页只显示语义版本（本包为 `1.0.4`），不显示内部 build number。新增/更新 widget 测试覆盖上述删除项、音源摘要、移动端单返回按钮及中心线对齐、平台版本读取且隐藏 build number；先确认旧实现对应测试失败，再完成修复。版本号从 `1.0.3+4` 递增到 `1.0.4+5`；`dart format lib/src/app.dart test/widget_test.dart` 通过，`flutter analyze --no-pub` 通过，`flutter test --no-pub` 100 个测试全部通过。使用 Visual Studio Build Tools `vcvars64` + CMake/Ninja 构建并安装 Windows Release，确认 `zmusic.exe` ProductVersion/FileVersion 均为 `1.0.4+5`；Windows zip 共 28 个条目，包含 `data/app.so`、`libmpv-2.dll`、`media_kit_libs_windows_audio_plugin.dll`，不包含 `userdata`、安装标记、嵌套包或 `just_audio`；安装器重新嵌入最新 `payload.zip`。Android release APK 为 `versionName=1.0.4`、`versionCode=5`，包含 3 个 ABI 的 `libmpv.so` 且不含 `just_audio`；手机和平板继续使用同一通用 APK。检测到安装版 `D:\Programs\Zmusic\zmusic.exe` 正在运行，未强制关闭做绿色版启动烟测。已打包 `dist/zmusic-windows-x64.zip`、`dist/zmusic-windows-x64-setup.exe`、`dist/zmusic-android-release.apk`。产物 SHA256：Windows zip `BD77C0595E04F9BB70AB5476E828C8F7491DD1BD6BB0DDFB64B649303BE07CF5`，Windows 安装包 `DAC2553BD7F89E27DF5ECD34F423CA83C23FB2AD60F980102F6F78C0EFBAE402`，Android APK `E724107A2C08BB56F7EBC4DF3529C78667907A7BCAB7627EEA151E5E16F3FF77`。
- 2026-07-10：按默认“打包”规则为音源管理布局和 `openAndPlay` 播放启动修正版重新打包。版本号从 `1.0.2+3` 递增到 `1.0.3+4`；`flutter analyze --no-pub` 通过，`flutter test --no-pub` 全部通过。使用 Visual Studio Build Tools `vcvars64` 环境和 CMake/Ninja 成功构建并安装 Windows Release，确认 `zmusic.exe` ProductVersion/FileVersion 均为 `1.0.3+4`；重建 `dist/zmusic-windows-x64` 和 zip，zip 共 28 个条目，包含 `data/app.so`、`libmpv-2.dll`、`media_kit_libs_windows_audio_plugin.dll`，不包含 `userdata`、`zmusic.installed`、嵌套包或 `just_audio`；Windows 安装器重新嵌入最新 `payload.zip` 并编译通过。Android release 构建通过，APK 为 `versionName=1.0.3`、`versionCode=4`，包含 3 个 ABI 的 `libmpv.so` 且不包含 `just_audio`；手机和平板继续使用同一个通用 APK，不生成重复 HD 包。检测到安装版 `D:\Programs\Zmusic\zmusic.exe` 正在运行，为避免中断用户当前播放未强制关闭做绿色版启动烟测。已打包 `dist/zmusic-windows-x64.zip`、`dist/zmusic-windows-x64-setup.exe`、`dist/zmusic-android-release.apk`。产物 SHA256：Windows zip `5F962DF95EC6CCFAD5DB930404447D3BD071BF5A122E8200664A5E3538683821`，Windows 安装包 `2EF45D23C202F08C14AA74FA1B7501FD48F9FD6FED0D63C4D9A359AAEBCDD6FD`，Android APK `3AEB806505E4C615A27DA042CF9D205D90FDF971BD4B38DF007BEF4980F90848`。
- 2026-07-10：按反馈调整设置页音源管理布局，并修正新曲目启动方式。设置页“音源设置”条目去掉“当前：xxx”副标题，当前音源只由列表单选状态表示；“导入”和“添加”操作移动到音源列表上方，桌面端显示两个同名“添加”按钮并通过 `add` / `folder_open` 图标区分添加远程音源和添加本地音源，Android 仍隐藏添加本地；“导出”从顶部操作区移到每个音源行右侧，点击后只导出对应音源的受保护配置文本，并保留复制成功 3 秒对钩反馈。新增 `AppController.exportSingleServerConfigText` 复用既有 `ZHANGLONG` 加密包装规则。上一轮播放链路修正一并保留：新曲目打开时改用 `openAndPlay`，`media_kit` 以 `play: true` 直接打开本地代理/文件/远端 URL，避免 `open(play:false)` 预读过多导致体感像“缓存满才播放”；暂停恢复仍走独立 `play()`。验证：`dart format lib/src/app.dart lib/src/app_controller.dart lib/src/player_controller.dart test/widget_test.dart` 通过，`flutter test --no-pub` 98 个测试通过，`flutter analyze --no-pub` 通过；本轮未打包。
- 2026-07-10：按默认“打包”规则为设置页内联音源管理版本重新打包。版本号从 `1.0.1+2` 递增到 `1.0.2+3`；`flutter analyze --no-pub` 通过，`flutter test --no-pub` 98 个测试通过。使用 VS BuildTools + CMake/Ninja 重新配置并构建 Windows `zmusic` target，修正 `windows/flutter/ephemeral/generated_config.cmake` 后确认 Windows `zmusic.exe` VersionInfo 为 `1.0.2+3`；同步最新 `app.so`，重建 `dist/zmusic-windows-x64` 和 `dist/zmusic-windows-x64.zip`，zip 内容检查 25 个条目且未包含 `userdata`、`zmusic.installed`、嵌套 zip、安装包或 `just_audio`，包含 `libmpv-2.dll`、`media_kit_libs_windows_audio_plugin.dll` 和 `data/app.so`；Windows 安装包重新嵌入 `payload.zip` 并编译通过。`flutter build apk --release --no-pub` 通过，已复制为 `dist/zmusic-android-release.apk`，APK 检查包含 `libmpv.so` 且无 `just_audio`；本轮不生成重复 HD APK。绿色版启动烟测因当前已有安装版 `D:\Programs\Zmusic\zmusic.exe` 运行触发单实例退出，未强制关闭用户正在运行的客户端。已打包 `dist/zmusic-windows-x64.zip`、`dist/zmusic-windows-x64-setup.exe`、`dist/zmusic-android-release.apk`。产物 SHA256：Windows zip `3292CBB27C07622199C54D492FDCAC2DAC922938AED266285019B57B7ADD1BC6`，Windows 安装包 `1AC8344EF50D95363177CE1052186B12CED78B807E7C91F1680EE2CAE4BBF679`，Android APK `E9CD2C70CE24E99C7DC1393908EB920FA495766B1CD617480C2C5C5514E25C09`。
- 2026-07-10：按反馈把音源列表直接嵌入设置页“音源设置”下面，不再点击“音源设置”打开音源管理弹窗；原有音源列表、主音源选择、测试连接、编辑、删除、导入、导出、添加音源、桌面端添加本地等操作改为设置页内联展示，新增/编辑音源仍使用各自单层表单弹窗，避免“音源设置弹窗 + 新增弹窗”的多弹窗叠加。Android 继续隐藏“添加本地”。更新 widget 测试覆盖设置页内联音源管理、导出复制反馈、添加音源/添加本地只出现单个表单弹窗、手机宽度和 Android sw510dp 布局。验证：`dart format lib/src/app.dart test/widget_test.dart` 通过，`flutter test --no-pub test/widget_test.dart` 98 个测试通过，`flutter analyze --no-pub` 通过，`flutter test --no-pub` 98 个测试通过；本轮未打包。
- 2026-07-10：按“安装 Windows 测试播放可信性”做安装器与播放链路验收。安装器实测时发现嵌入 zip 资源名大小写不一致会导致安装后只剩 `userdata`、程序文件未释放；修复 `build/package/ZmusicSetup.cs` 的 payload 资源查找逻辑，兼容 `payload.zip` / `Payload.zip` 和完整 manifest 名称，并重新编译 `dist/zmusic-windows-x64-setup.exe`。使用安装向导真实覆盖安装到 `D:\Programs\Zmusic`，确认安装器会关闭旧进程，安装后 `zmusic.exe` 自动启动，`uninstall.exe`、`zmusic.installed`、`userdata`、`libmpv-2.dll`、`media_kit_libs_windows_audio_plugin.dll` 和当前用户卸载注册项均存在，`just_audio_windows_plugin.dll` 不再存在；运行中的进程已加载 `libmpv-2.dll` 和 `media_kit_libs_windows_audio_plugin.dll`。使用安装目录现有音源配置做非 UI 音频首包验证，MP3 首包约 1.1 秒、FLAC 首包约 1.1 秒、APE 首包约 0.9 秒，均在 20 秒内返回对应音频 MIME；CLI 直连 media_kit 的有声播放探针受当前非交互环境无音频设备限制，不能作为扬声器输出结论。验证：`flutter analyze --no-pub` 通过，`flutter test --no-pub` 98 个测试通过；本轮仅重建 Windows 安装包，Windows zip 和 Android APK 未重新打包。产物 SHA256：Windows zip `B439395A3EC317C2C062FB165E40D734A8CAB9DEE8C363C51CE67FBDFCD1937C`，Windows 安装包 `C537743517C85957E2438A331B4E433E17A304868465244C49EC9B3DD398A4C2`，Android APK `884D5C147BA7F9D657C362480F4E42D600E8F591721E4ECB14859F7E911217AC`。
- 2026-07-10：按 APE/FLAC/MP3 全格式“边加载边播放、缓存完成后复用本地缓存”的目标切换播放后端。移除 `just_audio` / `just_audio_windows` 依赖，新增 `media_kit`、`media_kit_libs_windows_audio`、`media_kit_libs_android_audio`，运行时使用 libmpv/FFmpeg 音频解码，Windows 与 Android 都支持 MP3、FLAC、APE；测试环境使用内存播放引擎避免依赖本机 native DLL。移除 Windows APE/FLAC 绕过缓存代理逻辑，所有普通网络音频重新统一进入 `StreamingAudioCacheProxy`；代理新增远端 Range 旁路响应，解码器请求文件尾部/索引时可快速返回，同时主下载继续顺序写完整缓存，缓存完成后仍通过 `.complete` 标记复用本地文件。Subsonic `stream.view` 不再为 APE 请求 `format=mp3` 转码，保留原格式流。Android 音源设置隐藏“添加本地”入口，桌面端保留；版本号从 `1.0.0+1` 升到 `1.0.1+2`；重新生成插件注册并清理 Windows 绿色版打包内容，确认 zip 不包含 `just_audio`、`userdata`、嵌套 zip 或安装包，且包含 `libmpv-2.dll` 与 `media_kit_libs_windows_audio_plugin.dll`；Android APK 确认包含 `libmpv.so` 且无 `just_audio`。验证：`dart format lib/main.dart lib/src/player_controller.dart lib/src/audio_cache.dart lib/src/streaming_audio_cache_source.dart lib/src/subsonic_api.dart lib/src/app.dart test/widget_test.dart` 通过；`flutter test --no-pub` 98 个测试通过；`flutter analyze --no-pub` 通过；Windows CMake/Ninja Release 构建通过并手动执行 media_kit libmpv 解包 target；Android `flutter build apk --release --no-pub` 通过。已打包 `dist/zmusic-windows-x64.zip`、`dist/zmusic-windows-x64-setup.exe`、`dist/zmusic-android-release.apk`；本轮不再生成重复 HD APK。产物 SHA256：Windows zip `B439395A3EC317C2C062FB165E40D734A8CAB9DEE8C363C51CE67FBDFCD1937C`，Windows 安装包 `FA28419D8AC4D69EC239415343533544237A460C1A61097DA4B7176C166EC3C6`，Android APK `884D5C147BA7F9D657C362480F4E42D600E8F591721E4ECB14859F7E911217AC`。
- 2026-07-10：去掉首页搜索框下方的全局线性加载进度条。由于远端库信息已按歌手、专辑、歌单、歌曲数等接口并行增量加载，首页不再用 `controller.isBusy` 展示整页进度条，只保留音源卡片刷新按钮的局部加载反馈。修复 Windows 原始 APE/FLAC 经过单路顺序缓存代理后可能“已加载但不播放”的问题：Windows 上未转码的 APE/FLAC 现在绕过本地缓存代理，直接交给播放器使用远端原始流；明确 `format=mp3` 的服务端转码流和普通 MP3 仍继续走边播边缓存。验证：`dart format lib/src/app.dart lib/src/audio_cache.dart test/widget_test.dart` 通过，`flutter test --no-pub` 97 个测试通过，`flutter analyze --no-pub` 通过；本轮未打包。
- 2026-07-10：修复 APE/FLAC 播放链路回退问题并完成打包。`stream.view` 现在使用专用音频流 URL，不再携带 `f=json`；Windows 端 APE 恢复原始流播放路径，不再强制请求 MP3 转码，避免破坏之前可播放的 APE；FLAC 全平台走原始 FLAC 流，不再被 Windows 强制转码；非 Windows 的 APE 仍保留 MP3 转码尝试以兼顾 Android 原生解码限制；本地缓存代理在远端返回 XML/JSON 错误响应时返回 502 并停止缓存，避免把错误文本当音频文件缓存。继续保留本轮其它修复：Android/桌面切歌前更主动停止旧播放器连接、音源弹窗底部 4 个按钮单行显示、远端库信息按歌手/专辑/歌单/歌曲数/发现数据分接口并行加载、Windows 安装器覆盖安装前主动关闭同安装目录运行中的 `zmusic.exe`，安装完成后可选择直接打开应用。验证：`flutter test --no-pub` 95 个测试通过，`flutter analyze --no-pub` 通过；最终安装包覆盖安装烟测通过，确认旧进程关闭、`uninstall.exe`、`zmusic.installed` 和安装后启动均正常；使用安装目录现有音源配置做非 UI 流首包验证，随机 APE 返回 `audio/x-monkeys-audio` 且 4096 字节首包，随机 FLAC 返回 `audio/flac` 且 328 字节首包，均在 60 秒内成功。已打包 `dist/zmusic-windows-x64.zip`、`dist/zmusic-windows-x64-setup.exe` 和通用 `dist/zmusic-android-release.apk`；Android HD 没有差异化 flavor，本轮不再生成重复 HD 副本。产物 SHA256：Windows zip `D09E7A69BC0E4F53036A3F9E24B5E642192BBF68DB43F49F62FBBFCB7F95C017`，Windows 安装包 `71658FEC8A1BFD4454348EE8AB0EEEC25CED91EF98A6C5EE18C73CB67DD71536`，Android APK `D7ADF247D9DF07B9D8B5671C616A1E06E38DF78DC5A42DFCCED25E0A2B1612D1`。
- 2026-07-09：按默认“打包”规则重新打包 Windows 绿色版、Windows 安装版和 Android release APK，本轮无业务代码改动。验证：`flutter analyze --no-pub` 通过，`flutter test --no-pub` 92 个测试通过；使用 VS BuildTools 环境和 CMake/Ninja 成功构建 Windows `zmusic` target，并构建 `jni` target 确认无待构建内容；同步最新 `build/windows/app.so` 到 Windows runner，覆盖 `dist/zmusic-windows-x64` 并重新生成 `dist/zmusic-windows-x64.zip`，zip 内容检查 24 个条目且未包含 `userdata`、`zmusic.installed`、`CMakeFiles`、嵌套 zip 或安装包；重新编译 `dist/zmusic-windows-x64-setup.exe`；`flutter build apk --release --no-pub` 通过，并同步 `dist/zmusic-android-release.apk` 与 `dist/zmusic-android-hd-release.apk`。Windows 绿色版启动烟测因当前已有安装版 `D:\Programs\Zmusic\zmusic.exe` 运行触发单实例退出，未强制关闭用户正在运行的客户端。产物 SHA256：Windows zip `CC0DAB9541DE7240041C783E449C91FA60A77AAAC10E9C9897DD95E1675F4C5C`，Windows 安装包 `095BA038E914144C76BAEC488D1D384AE8C9DAE6B53BEC70544BD8E413AA64D0`，Android APK/HD APK `6C92D1F58193C3D3F5AE3E126B9A00B09961E754413A0D061DA93E2C3732B234`。
- 2026-07-09：修复顶部“搜索 / 音乐 / 设置”三页签切换时搜索框保留旧关键词的问题。`_selectHomeTab` 现在在切换页签时调用 `_clearSearchText()`，同时关闭搜索结果、详情页和播放详情，确保跨页签切换后顶部搜索输入框为空；新增 widget 测试覆盖在音乐页输入关键词后切到设置页、搜索页都会清空输入框。验证：`dart format lib/src/app.dart test/widget_test.dart` 通过，`flutter test --no-pub` 91 个测试通过，`flutter analyze --no-pub` 通过；本轮未打包。
- 2026-07-09：按单路代理方案重做网络音频边播边缓存链路。未完成缓存不再把远端 URL 直接交给播放器或额外启动后台下载，而是创建本地 `StreamingAudioCacheProxy`，播放器只读取 `127.0.0.1` 本地代理；代理内部对 Navidrome/Subsonic 只发起一条远端 GET，一边写缓存文件一边从本地缓存响应播放器请求，本地 Range 读取命中已缓存数据时直接返回，未缓存位置等待同一条下载流推进。移除 Subsonic APE/FLAC 转 MP3 流绕过缓存的逻辑，转码流也可以边播边缓存且不再产生双实时转码连接；切歌/停止时继续取消本地代理和远端下载。更新测试覆盖转码流进入缓存代理、本地代理多次读取只产生一次远端请求、取消代理会中断远端下载。验证：`dart format lib/src/audio_cache.dart lib/src/player_controller.dart lib/src/streaming_audio_cache_source.dart test/widget_test.dart` 通过，`flutter test --no-pub test/widget_test.dart` 92 个测试通过，`flutter analyze --no-pub` 通过，`flutter test --no-pub` 92 个测试通过；本轮未打包。
- 2026-07-09：优化音源设置导入/导出按钮和 Windows 启动速度。音源设置底部“文本导入 / 文本导出”文案改为“导入 / 导出”，文本导入弹窗标题也改为“导入”；点击“导出”后仍直接复制到剪贴板，同时导出按钮图标切换为对钩并在 3 秒后恢复复制图标，保留“已复制到剪贴板。”提示。Windows/桌面启动入口调整为先 `runApp` 显示主界面，再后台执行 `AppController.load()` 和 `DesktopIntegration.initialize()`，避免启动阶段等待本地存储、远端音乐库刷新和托盘/窗口图标初始化导致窗口迟迟不显示；后台加载异常会写入 debug 日志，不阻塞 UI。更新 widget 测试覆盖“导入/导出”新文案、导出后对钩反馈与 3 秒恢复。验证：`dart format lib/main.dart lib/src/app.dart test/widget_test.dart` 通过，`flutter test --no-pub` 90 个测试通过，`flutter analyze --no-pub` 通过；本轮未打包。
- 2026-07-09：调整音源配置“文本导出”交互。点击“文本导出”后不再弹出导出内容预览窗口，而是直接把加密后的音源配置文本写入系统剪贴板，并在当前界面提示“已复制到剪贴板。”；删除不再使用的 `_TextExportDialog`，更新 widget 测试覆盖直接复制、不出现“导出音源配置”二级弹窗且仍停留在音源设置弹窗。验证：`dart format lib/src/app.dart test/widget_test.dart` 通过，`flutter test --no-pub` 90 个测试通过，`flutter analyze --no-pub` 通过；本轮未打包。
- 2026-07-09：修复远端 FLAC/APE 播放兼容和 Android 切歌竞态问题。Subsonic 播放地址生成和播放前 URL 兜底统一使用 `shouldRequestSubsonicMp3Transcode`：Windows 端远端流继续请求 MP3 转码，APE 全平台请求 MP3 转码，旧数据里的 FLAC/APE 播放前也会重新生成可播放 URL；缓存层对“原始非 MP3 + 服务端 MP3 转码”的未完成流不再启动第二条后台缓存请求，避免实时转码双连接导致一直缓冲，已有完整缓存仍优先复用。`PlayerController` 在新播放请求进入后先停止旧音源并取消旧缓存，再解析/加载新曲目，降低 Android 上点搜索结果新歌时详情页已切换但声音仍是旧歌的竞态。底部播放栏和移动端迷你播放条新增收藏按钮；播放队列行新增收藏按钮，并在队列歌名旁显示 MP3/APE/FLAC 标签。验证：`dart format lib/src/subsonic_api.dart lib/src/app_controller.dart lib/src/audio_cache.dart lib/src/player_controller.dart lib/src/app.dart test/widget_test.dart` 通过，`flutter test --no-pub` 90 个测试通过，`flutter analyze --no-pub` 通过；本轮未打包。
- 2026-07-09：修复远端 APE 曲目加载容易一直缓冲的问题。Navidrome/Subsonic 曲目如果识别为 APE，现在全平台都会生成 `format=mp3&maxBitRate=320` 的 MP3 转码播放 URL；播放历史或旧数据里仍是旧 URL 的 APE 曲目，在播放前会根据音源和歌曲 ID 重新生成可播放的转码 URL。音频缓存层对“APE 原始格式 + MP3 转码 URL”的未完成缓存不再启动第二条后台缓存请求，避免服务器对同一 APE 同时启动两路实时转码导致卡住；如果此前已存在完整缓存仍会复用本地文件。更新测试覆盖 APE 转码参数和 APE 转码流绕过双连接缓存。验证：`dart format lib/src/subsonic_api.dart lib/src/audio_cache.dart lib/src/app_controller.dart test/widget_test.dart` 通过，`flutter test --no-pub` 90 个测试通过，`flutter analyze --no-pub` 通过；本轮未打包。
- 2026-07-09：修复首页歌手/专辑卡片进入详情后返回首页仍残留搜索关键词的问题。搜索结果页返回现在统一回到音乐库首页并清空顶部搜索框；歌手、专辑、歌单等详情页返回也会清空由卡片点击写入的搜索文本，避免返回后首页搜索框继续显示歌手名或专辑名。更新 widget 测试覆盖点击专辑进入歌曲页后返回首页搜索框为空。验证：`dart format lib/src/app.dart test/widget_test.dart` 通过，`flutter test --no-pub` 89 个测试通过，`flutter analyze --no-pub` 通过；本轮未打包。
- 2026-07-09：音乐页音源卡片标题增加账号显示。`_SourceSummaryCard` 现在对 Navidrome/Subsonic 音源显示 `音源：名称 · 账号：用户名`，账号为空或本地音源不显示账号；歌曲数、扫描状态仍保留在第二行。更新 widget 测试覆盖音源摘要卡位置稳定和音乐页快捷入口场景下的账号展示。验证：`dart format lib/src/app.dart test/widget_test.dart` 通过，`flutter test --no-pub` 89 个测试通过，`flutter analyze --no-pub` 通过；本轮未打包。
- 2026-07-09：调整搜索框分类下拉按钮选中后的视觉状态。`_LibrarySearchBar` 左侧 `DropdownButton` 的焦点色改为透明，选择分类后焦点回到搜索输入框，避免“所有/歌曲/歌手/专辑”下拉按钮在选中后残留灰色高亮。更新 widget 测试覆盖分类下拉按钮 `focusColor` 为透明。验证：`dart format lib/src/app.dart test/widget_test.dart` 通过，`flutter test --no-pub` 89 个测试通过，`flutter analyze --no-pub` 通过；本轮未打包。
- 2026-07-09：调整音源唯一标识和播放器界面细节。同 URL 的 Navidrome/Subsonic 音源现在按 `URL|账号` 生成远程音源 ID，同 URL 同账号仍更新原音源，同 URL 不同账号可以同时保留；加载旧版纯 URL 选中值时会映射到对应音源，音源导入去重也改为按完整音源 ID 处理。搜索框输入区改为左对齐，未聚焦时显示“输入关键词”，获取焦点后隐藏占位文字；底部播放进度条右侧播放队列按钮去掉“歌单”文字，只保留图标。新增/更新 widget 测试覆盖同 URL 不同账号并存、搜索框聚焦占位隐藏和队列按钮图标化。验证：`dart format lib/src/models.dart lib/src/app_controller.dart lib/src/source_config_codec.dart lib/src/app.dart test/widget_test.dart` 通过，`flutter test --no-pub` 89 个测试通过，`flutter analyze --no-pub` 通过；本轮未打包。
- 2026-07-09：修复新增音源后当前主音源被自动切换、看起来像被覆盖的问题。`AppController.saveServer` 在新增不同 URL 的音源时只把新音源追加到列表，不再自动替换 `selectedServerId`；只有当前没有有效主音源时才默认选中新音源；编辑已选中音源或同 URL 保存仍按 URL 唯一标识更新原音源。新增 widget 测试覆盖“新增不同 URL 音源后保留当前主音源”。验证：`dart format lib/src/app_controller.dart test/widget_test.dart` 通过，`flutter test --no-pub` 89 个测试通过，`flutter analyze --no-pub` 通过；本轮未打包。
- 2026-07-09：调整音源设置弹窗关闭按钮位置。`_SourceManagerDialog` 的关闭操作统一放到弹窗标题行右上角，使用 `Icons.close` 图标按钮，不再在底部操作区显示“关闭”文字按钮；桌面、手机、Android HD/sw510dp 均保持同一关闭入口，底部只保留文本导入、文本导出、添加音源、添加本地等操作。更新 widget 测试覆盖弹窗内存在关闭图标且不再显示“关闭”文字。验证：`dart format lib/src/app.dart test/widget_test.dart` 通过，`flutter test --no-pub` 88 个测试通过，`flutter analyze --no-pub` 通过；本轮未打包。
- 2026-07-09：调整 Windows 安装器默认安装路径逻辑。安装向导打开时会优先读取当前用户卸载注册表项 `HKCU\Software\Microsoft\Windows\CurrentVersion\Uninstall\Zmusic` 的 `InstallLocation`，自动带出上一次安装目录；读取不到或路径异常时再回退到 `%LOCALAPPDATA%\Programs\Zmusic`。本轮只修改 `build/package/ZmusicSetup.cs` 并重新编译 `dist/zmusic-windows-x64-setup.exe`，未重建 Windows 绿色版 zip 或 Android APK。验证：C# 安装器编译通过，`flutter analyze --no-pub` 通过，`flutter test --no-pub` 88 个测试通过；新安装包 SHA256 为 `D94FBE6C3D017B33514648A2EFE98508BFDCD163D53B62E30EA1DB29FC2B45D5`。
- 2026-07-09：按默认“打包”规则为可取消边播边缓存修复版本重新打包 Windows 绿色版、Windows 安装版和 Android release APK。使用 VS BuildTools + CMake/Ninja 重新构建 `zmusic` target，同步最新 `build/windows/app.so` 到 runner，重建 `dist/zmusic-windows-x64` 和 `dist/zmusic-windows-x64.zip`；zip 内容检查 24 个条目，未包含 `userdata`、`zmusic.installed`、`CMakeFiles`、嵌套 zip 或安装包；C# 安装器重新嵌入最新 zip 生成 `dist/zmusic-windows-x64-setup.exe`；Android `flutter build apk --release --no-pub` 通过，并同步 `dist/zmusic-android-release.apk` 与 `dist/zmusic-android-hd-release.apk`。验证：`flutter analyze --no-pub` 通过，`flutter test --no-pub` 88 个测试通过，绿色版 `dist/zmusic-windows-x64/zmusic.exe` 使用临时 APPDATA/LOCALAPPDATA 启动烟测并存活 5 秒。产物 SHA256：Windows zip `C2BC4A089A1D2E572ED9DE2374582DF4B7DE245BB818ED00728FFA00DC3C9C76`，Windows 安装包 `B4EF0A62255428A0DD81873E0724401FC4D74730D2FCBB8F1942F0BDF12009EE`，Android APK/HD APK 均为 `CF63C7009A7B6E43384485C3D68D0BC330606F0DBBA6DEA410C1CA805F6BFAAB`。
- 2026-07-09：修复 Windows 播放多首网络歌曲后一直缓冲的问题。原因定位为旧的 `LockCachingAudioSource` 在切歌后仍可能继续后台下载整首歌，连续播放/切换多首大文件时会堆积连接和文件句柄，导致后续歌曲无法开始播放；新增可取消的 `StreamingAudioCacheSource`，当前歌曲边播边缓存并支持已下载区间读取，切换新歌、停止到队尾或释放播放器时会取消上一首缓存下载；`PlayerController` 在加载新音源前统一 `stop()` 并清理旧缓存进度/源，避免旧连接残留。新增本地 `HttpServer` 回归测试覆盖缓存下载可取消。验证：`dart format lib/src/player_controller.dart lib/src/streaming_audio_cache_source.dart test/widget_test.dart` 通过，`flutter analyze --no-pub` 通过，`flutter test --no-pub` 88 个测试通过；本轮未打包。
- 2026-07-09：修复 Android 手机端返回行为。`HomePage` 增加统一返回拦截：播放详情、搜索结果、歌单/电台/歌手/专辑等内部页面先返回上一层；已经在根页面时通过 Android 原生 `moveTaskToBack(true)` 把应用退到后台，不再直接关闭 Activity；手机端增加左侧边缘右滑手势并复用同一返回逻辑。`MainActivity` 新增 `com.zmusic.app/task` MethodChannel 处理退后台。新增 widget 测试覆盖 Android 左侧边缘滑动先关闭内部页面、根页面系统返回调用退后台通道。验证：`dart format lib/src/app.dart test/widget_test.dart` 通过，`flutter analyze --no-pub` 通过，`flutter test --no-pub` 87 个测试通过；本轮未打包。
- 2026-07-08：按用户确认取消保留上一版小写技术命名，把项目技术标识统一迁移为 `zmusic`，不做旧命名兼容；覆盖 Flutter package 名、Dart import、Windows CMake project/target、Windows exe 版本信息、安装器 exe/安装标记/payload 根目录/卸载进程名、Android namespace/applicationId/Kotlin package、iOS bundle id、Subsonic client name、品牌资源文件名、IDE `.iml` 文件名和测试用例；Windows 可执行文件改为 `zmusic.exe`，绿色版目录和 zip 改为 `dist/zmusic-windows-x64*`，Android APK 改为 `dist/zmusic-android-*.apk`，安装标记改为 `zmusic.installed`；删除旧命名产物、旧历史打包目录、旧烟测目录和旧临时 obj；安装器覆盖安装前会清理安装目录中除 `userdata` 外的旧运行时文件，避免旧 exe 残留。验证：`flutter pub get` 通过，`dart format lib test` 通过，`flutter analyze --no-pub` 通过，`flutter test --no-pub` 85 个测试通过；清空旧 `build/windows/x64-ninja` 后重新配置 CMake/Ninja，成功构建 `zmusic` target，并补构建 `jni` target 生成 `dartjni.dll`；zip 检查 21 个条目且无旧名、`userdata`、`zmusic.installed`、`CMakeFiles`、嵌套 zip 或安装包；绿色版 `dist/zmusic-windows-x64/zmusic.exe` 启动烟测存活 5 秒；Windows exe VersionInfo 为 `OriginalFilename=zmusic.exe`、`InternalName=zmusic`、`CompanyName=com.zmusic.app`；已打包 `dist/zmusic-windows-x64.zip`、`dist/zmusic-windows-x64-setup.exe`、`dist/zmusic-android-release.apk` 和 `dist/zmusic-android-hd-release.apk`，两个 Android APK SHA256 均为 `657BB4401CDD17948692360AD4A659661C66AF1F9AB4F1BDDA9CE9318DCE85E0`，安装包 SHA256 为 `80D73A21A152801FCF098962FC757152D7423BF184B8D2BF32F9C32B35471129`。
- 2026-07-08：按用户要求统一产品显示名为 `Zmusic`，覆盖 Flutter 应用标题、Dart 应用类名、Windows 窗口标题/资源信息/单实例 Mutex、托盘提示、Android/iOS 显示名、安装器窗口文案、快捷方式名、卸载注册项、默认安装目录 `%LOCALAPPDATA%\Programs\Zmusic`、README、设计文档、测试和项目记忆；安装器源码文件同步改名为 `build/package/ZmusicSetup.cs`；保留既有技术包名、exe 名 `zmusic.exe` 和 `dist/zmusic-*` 产物命名，避免扩大迁移范围。验证：`dart format` 通过；`flutter analyze --no-pub` 通过；`flutter test --no-pub` 85 个测试通过；使用 VS BuildTools `vcvars64.bat` 环境和 CMake/Ninja 成功重建 Windows runner，同步最新 `app.so` 与 `zmusic.exe` 到 `dist/zmusic-windows-x64`；zip 检查 23 个运行时条目且未包含 `userdata`、`zmusic.installed`、`CMakeFiles`、嵌套 zip 或安装包；绿色版启动烟测存活 5 秒；二进制旧品牌名扫描无残留；重新生成 `dist/zmusic-windows-x64.zip` 和 `dist/zmusic-windows-x64-setup.exe`；Android release APK 构建通过并同步 `dist/zmusic-android-release.apk` 和 `dist/zmusic-android-hd-release.apk`，两个 APK SHA256 均为 `638C4004F625BB4D17CE3B8346DC9B0C189EFD26418EDDE3AA6C531EE0A5E846`。
- 2026-07-08：按用户要求把产品显示名从带空格形式统一为 `Zmusic`，覆盖 Flutter 应用标题、Dart 应用类名、Windows 窗口标题/资源信息/单实例 Mutex、托盘提示、Android/iOS 显示名、安装器窗口文案、快捷方式名、卸载注册项、默认安装目录 `%LOCALAPPDATA%\Programs\Zmusic`、README、设计文档、测试和项目记忆；保留技术包名、exe 名和 `dist/zmusic-windows-x64` 等已无空格的小写标识。验证：`dart format` 通过；`flutter analyze --no-pub` 通过；`flutter test --no-pub` 85 个测试通过；使用 VS BuildTools `vcvars64.bat` 环境和 CMake/Ninja 成功重建 Windows runner，同步最新 `app.so` 与 `zmusic.exe` 到 `dist/zmusic-windows-x64`；zip 检查 23 个运行时条目且未包含 `userdata`、`zmusic.installed`、`CMakeFiles`、嵌套 zip 或安装包；绿色版启动烟测存活 5 秒并退出；重新生成 `dist/zmusic-windows-x64.zip` 和 `dist/zmusic-windows-x64-setup.exe`；Android release APK 构建通过并同步 `dist/zmusic-android-release.apk` 和 `dist/zmusic-android-hd-release.apk`，两个 APK SHA256 均为 `EF350847316DFC4E367DDB0671A22C312E0B18A5E135620D0D3EAE7ACA8A2F17`。
- 2026-07-08：补齐 Windows 安装版卸载能力。`build/package/ZmusicSetup.cs` 新增 `/uninstall` 模式；安装时会把当前安装器复制到安装目录 `uninstall.exe`，创建开始菜单“卸载 Zmusic”快捷方式，并写入当前用户 `HKCU\Software\Microsoft\Windows\CurrentVersion\Uninstall\Zmusic` 卸载项；通过开始菜单/系统卸载列表或直接双击 `uninstall.exe` 都会进入卸载流程；卸载时会尝试关闭安装目录内运行中的 `zmusic.exe`，删除程序文件、桌面/开始菜单快捷方式和卸载注册项，同时保留安装目录下 `userdata`，卸载器自身通过临时清理脚本延迟删除。验证：C# 安装器编译通过并重新生成 `dist/zmusic-windows-x64-setup.exe`；`flutter analyze --no-pub` 通过，`flutter test --no-pub` 85 个测试通过；本轮仅重建 Windows 安装包，未重建 Windows zip 或 Android APK。
- 2026-07-08：改进 Windows 安装包交互体验。`build/package/ZmusicSetup.cs` 从静默安装改为 WinForms 安装向导：安装前显示“安装 Zmusic”窗口，用户可查看和修改安装位置，支持“浏览...”选择目录，默认勾选“创建桌面图标”；安装完成提示改为中文并显示实际安装目录；继续创建开始菜单快捷方式，桌面快捷方式指向 `zmusic.exe` 并使用安装目录下 `app_icon.ico`；覆盖安装仍跳过 payload 中的 `userdata` 和 `zmusic.installed`，并保留/创建安装目录下 `userdata`。验证：C# 安装器编译通过并重新生成 `dist/zmusic-windows-x64-setup.exe`；`flutter analyze --no-pub` 通过，`flutter test --no-pub` 85 个测试通过；本轮仅重建 Windows 安装包，未重建 Windows zip 或 Android APK。
- 2026-07-08：按默认“打包”规则为播放 scrobble 上报版本重新打包 Windows 绿色版、Windows 安装版和 Android release APK，本轮无业务代码改动。验证：`flutter analyze --no-pub` 通过，`flutter test --no-pub` 85 个测试通过；Windows Release 使用缓存的 VS CMake/Ninja 构建通过，已同步 `build/windows/app.so` 到 runner，重新生成 `dist/zmusic-windows-x64.zip` 和 `dist/zmusic-windows-x64-setup.exe`；zip 内容检查 35 个条目且未包含 `userdata`、`zmusic.installed`、嵌套 zip 或安装包；绿色版 `dist/zmusic-windows-x64/zmusic.exe` 启动烟测并存活 5 秒；Android `flutter build apk --release --no-pub` 通过，已打包 `dist/zmusic-android-release.apk`，并同步现有 HD 命名副本 `dist/zmusic-android-hd-release.apk`，两个 APK SHA256 均为 `61899B9AE21B5292DE44FAAAF663558E4E29F5B1AA023B94A244235FCE3DB601`。
- 2026-07-08：补齐 Navidrome/Subsonic 播放上报，解决最近播放/最多播放长期无数据的根因。`SubsonicApiClient` 新增 `scrobbleTrack`，播放控制器新增 `playSessionId` 用于区分同一首歌的不同播放会话；`AppController` 监听播放器状态和进度，Subsonic 曲目开始播放时上报 `scrobble(submission=false)`，播放到 30 秒或歌曲时长 50%（两者先到）后上报一次 `scrobble(submission=true)`，同一播放会话只提交一次；本地文件和电台直播流不参与 scrobble，上报失败只打印调试信息不影响播放。新增测试覆盖 scrobble API 参数、提交阈值、播放进度触发一次提交、跳过本地/电台。验证：`dart format` 通过，`flutter analyze --no-pub` 通过，`flutter test --no-pub` 85 个测试通过；本轮未打包。
- 2026-07-07：按本轮反馈调整资料页与刷新行为：音乐页“我喜欢的 / 歌单 / 电台”入口都新增独立刷新按钮，分别调用 `getStarred2`、`getPlaylists`、`getInternetRadioStations` 拉取最新数据；歌单分页页、我喜欢的歌曲页和电台页也支持单独刷新；电台列表隐藏 URL，仅显示电台名称和类型；歌单/电台/分页类页面统一使用 `_PageBackButton` 返回按钮，去掉顶部重复标题和重复摘要卡片，分页组件不再使用玻璃框包裹。确认最近播放和最多播放已对接 Subsonic `getAlbumList2` 的 `recent` / `frequent` 类型，并保留测试覆盖。新增/更新 widget 与控制器测试覆盖三类独立刷新、电台 URL 隐藏、分页无框。验证：`dart format` 通过，`flutter analyze --no-pub` 通过，`flutter test --no-pub` 81 个测试通过；Windows `flutter build windows --release --no-pub` 在 VS 生成器阶段超时后改用缓存的 VS CMake/Ninja 增量构建通过，zip 内容检查 35 个条目且未包含 `userdata`、`zmusic.installed`、嵌套 zip 或安装包，绿色版 `dist/zmusic-windows-x64/zmusic.exe` 启动烟测并存活 5 秒；Android `flutter build apk --release --no-pub` 通过。已打包 `dist/zmusic-windows-x64.zip`、`dist/zmusic-windows-x64-setup.exe`、`dist/zmusic-android-release.apk`，并同步 `dist/zmusic-android-hd-release.apk`，Android APK SHA256 均为 `E0FFAA6105829EEB53D4FD6F099ECB88D6FCA7CD4375CCF46B9056E2D89C46AC`。
- 2026-07-07：修复 Navidrome/Subsonic 电台不能播放的问题。电台列表页点击项现在会把电台 `streamUrl` 转成临时播放 Track 并交给播放器；常见 `.m3u` / `.pls` 电台播放列表会解析出真实音频流地址，解析失败时回退原始 URL；电台 Track 使用 `customStream` 并以 `radio:` 标记，音频缓存层会跳过电台直播流，避免直播流被当作普通歌曲边播边缓存。新增测试覆盖 m3u/pls 解析、电台跳过缓存、点击电台进入播放链路。验证：`flutter analyze --no-pub` 通过，`flutter test --no-pub` 79 个测试通过；本轮未打包。
- 2026-07-07：按歌单权限需求调整 Navidrome/Subsonic 歌单体验：音乐页快捷入口“我的歌单”改为“歌单”，首页原“公共歌单”区块改为合并后的“歌单”区块，歌单列表和首页区块都按当前用户可管理歌单优先、公共非自己歌单靠后展示，并为公开歌单显示“公共”角标；首页歌单区块和歌单分页页新增“新建歌单”按钮，复用现有远端歌单创建弹窗；公共非自己歌单详情页只保留播放全部，隐藏添加歌曲、编辑、删除和歌曲移除操作；歌曲列表右键和移动端长按菜单新增“加入歌单”，只允许加入当前音源下自己可管理的歌单；加入歌单前会读取目标歌单现有歌曲并过滤重复 song id，避免同一首歌重复添加。新增/更新测试覆盖歌单排序/权限、公共非自己歌单只允许播放、歌曲长按菜单加入歌单和新版“歌单”入口。验证：`flutter analyze --no-pub` 通过，`flutter test --no-pub` 76 个测试通过；本轮未打包。
- 2026-07-07：按默认“打包”规则重新验证并打包当前版本，本轮无业务代码改动。验证：`flutter analyze --no-pub` 通过，`flutter test --no-pub` 73 个测试通过；Windows Release CMake 构建通过，重新生成 `dist/zmusic-windows-x64.zip` 和 `dist/zmusic-windows-x64-setup.exe`；zip 内容检查 30 个条目且未包含 `userdata`、`zmusic.installed`、嵌套 zip 或安装包；绿色版 `dist/zmusic-windows-x64/zmusic.exe` 使用临时 APPDATA/LOCALAPPDATA 启动烟测并存活 5 秒；Android `flutter build apk --release --no-pub` 使用 JDK 21 和项目内 Gradle home 构建通过。已打包 `dist/zmusic-android-release.apk` 并同步 `dist/zmusic-android-hd-release.apk`，两个 APK SHA256 均为 `54D20A793DD85C816970C23968326AADB5B2E03A6E401F70C0F53B566B83B7C3`。
- 2026-07-07：按本轮反馈继续压缩手机端信息密度：歌单详情头部四个操作按钮在手机端改为右侧横向一排小图标按钮，避免竖列占用高度；手机歌曲列表行高从 56 降到 52，封面从 40 降到 36，收藏/播放按钮从 36 降到 30；手机顶部临时状态消息完全隐藏，避免和搜索/音乐/设置顶部页签重叠，桌面宽屏状态消息继续显示；手机首页顶部页签、搜索框、音源卡、快捷入口卡、歌手/专辑区块标题间距、卡片间距和卡片高度进一步压缩。更新 widget 测试覆盖手机端状态消息隐藏、歌单详情四按钮同一行和歌曲行新高度。验证：`flutter analyze --no-pub` 通过，`flutter test --no-pub` 73 个测试通过；本轮未打包。
- 2026-07-07：按默认“打包”规则为当前 UI 收紧版本重新打包 Windows 绿色版、Windows 安装版和 Android release APK。打包前重新验证 `flutter analyze --no-pub` 通过、`flutter test --no-pub` 73 个测试通过；Windows Release CMake 构建通过。覆盖 `dist/zmusic-windows-x64` 时发现旧绿色版进程占用文件，已停止该 `zmusic.exe` 进程后重新覆盖并重建 zip；zip 内容检查 30 个条目且未包含 `userdata`、`zmusic.installed`、嵌套 zip 或安装包，绿色版 `dist/zmusic-windows-x64/zmusic.exe` 使用临时 APPDATA/LOCALAPPDATA 启动烟测并存活 5 秒；Android `flutter build apk --release --no-pub` 使用 JDK 21 和项目内 Gradle home 构建通过。已打包 `dist/zmusic-windows-x64.zip`、`dist/zmusic-windows-x64-setup.exe`、`dist/zmusic-android-release.apk`，并同步 `dist/zmusic-android-hd-release.apk`，Android APK SHA256 为 `B00D431F235A04BDB1265F1DE33632A731A76DABF6C61631080368F3EE4D9AF0`。
- 2026-07-07：按本轮反馈继续收紧 UI：Navidrome/Subsonic 歌单详情头部的播放全部、添加歌曲、编辑、删除四个操作改为纯图标按钮，手机宽度下固定放在歌单信息右侧 2×2 区域，不再显示中文按钮文案；手机底部迷你播放条移除内部底部进度线；全平台音频格式标签（MP3/APE/FLAC 等）进一步缩小约 50%，同步减小标签边框、内边距和标题旁间距。更新 widget 测试覆盖歌单详情图标化操作、手机迷你播放条无进度线、格式标签字号。验证：`flutter analyze --no-pub` 通过，`flutter test --no-pub` 73 个测试通过；本轮未打包。
- 2026-07-06：按本轮手机/桌面 UI 紧凑化需求调整播放器：搜索框输入区去掉前置放大镜，文字水平/垂直居中，保留右侧清空和搜索动作；歌曲列表改为自定义固定高度行，压缩封面、文字、收藏和播放按钮尺寸，长标题只省略不撑高行；首页/搜索页预览卡片固定高度并缩小封面、字体和内边距，避免长专辑名导致卡片高度割裂；手机底部迷你播放条去掉外层整条矩形遮罩，进度条内嵌在胶囊组件底部，标题、格式标签、播放按钮和歌单按钮缩小，封面改为圆形并在播放时旋转、暂停时停止；播放队列行和歌单详情操作按钮进一步压缩。新增/更新 widget 测试覆盖搜索框无前置图标、手机底部旋转封面和手机歌曲行固定高度。验证：`flutter analyze --no-pub` 通过，`flutter test --no-pub` 73 个测试通过；Windows Release CMake 构建通过，zip 内容检查 30 个条目且未包含 `userdata`、`zmusic.installed`、嵌套 zip 或安装包，绿色版 `dist/zmusic-windows-x64/zmusic.exe` 使用临时 APPDATA/LOCALAPPDATA 启动烟测并存活 5 秒；Android `flutter build apk --release --no-pub` 使用 JDK 21 和项目内 Gradle home 构建通过，当前无已连接 adb 设备所以未做真机运行时烟测。已打包 `dist/zmusic-windows-x64.zip`、`dist/zmusic-windows-x64-setup.exe`、`dist/zmusic-android-release.apk`，并同步 `dist/zmusic-android-hd-release.apk`，Android APK SHA256 为 `09424136AFE8F94E42CE233CE88B88ACD7BD0FF77D0FB5DB92CDE86AACA8FCFA`。
- 2026-07-06：按本轮移动端/桌面端 UI 调整需求更新播放器：顶部临时状态消息从桌面中间改到右侧显示，避免刷新歌曲数量时和顶部“搜索 / 音乐 / 设置”页签重叠；手机播放详情页改为全屏隐藏顶部三页签，只保留“歌曲 / 歌词”两页签并移除“推荐”，点击“歌词”切换到可滚动歌词面板；手机首页、音乐页、设置页和底部迷你播放条整体压缩间距，迷你播放条补回封面和底部播放进度条；歌曲列表播放按钮左侧新增收藏/取消收藏按钮，并通过 Subsonic `star` / `unstar` 对接 Navidrome 收藏；音乐页歌手卡片只显示头像和名称，不再显示“歌手”副标题。为受限网络/权限环境补充项目内 Gradle home init 脚本，使 Android 构建优先使用 `build/local-maven`。验证：`flutter analyze --no-pub` 通过，`flutter test --no-pub` 72 个测试通过；Windows Release CMake 构建通过，zip 内容检查 25 个条目且未包含 `userdata`、`zmusic.installed`、嵌套 zip 或安装包，绿色版 `dist/zmusic-windows-x64/zmusic.exe` 使用临时 APPDATA/LOCALAPPDATA 启动烟测并存活 5 秒；Android `flutter build apk --release --no-pub` 使用 JDK 21 和项目内 Gradle home 构建通过。已打包 `dist/zmusic-windows-x64.zip`、`dist/zmusic-windows-x64-setup.exe`、`dist/zmusic-android-release.apk`，并同步 `dist/zmusic-android-hd-release.apk`，Android APK SHA256 为 `E67353E54B22D8D0E7B07A9CDC8907E2D13F325F06CC449278619CC07E170E77`。
- 2026-07-06：按新固定规则补充并执行 `karpathy-guidelines`；播放队列右侧面板增强毛玻璃遮罩和深色填充，降低背景透出；歌单相关限定文案统一改为“歌单”；“我喜欢的”入口改为打开已加载的收藏歌曲列表；确认 Navidrome/Subsonic 发现数据中“我喜欢的”走 `getStarred2`，最近播放/最多播放/随机专辑走 `getAlbumList2 recent/frequent/random`；歌手缺图时使用同歌手专辑封面兜底，本地库也用曲目封面补齐歌手/专辑封面；确认生产代码图片路径不直接使用 `Image.network`/`NetworkImage`，统一走 `ArtworkCacheManager`/`_CachedArtworkImage` 缓存。新增 widget/API 测试覆盖歌手封面兜底和“我喜欢的”入口。验证：`flutter analyze --no-pub` 通过，`flutter test --no-pub` 69 个测试通过，Windows Release CMake 在 VS BuildTools 环境下构建通过，zip 检查 25 个条目且未包含 `userdata`、`zmusic.installed`、嵌套 zip 或安装包，绿色版 `dist/zmusic-windows-x64/zmusic.exe` 使用临时 APPDATA/LOCALAPPDATA 启动烟测并存活 5 秒，Android `flutter build apk --release --no-pub` 通过；已打包 `dist/zmusic-windows-x64.zip`、`dist/zmusic-windows-x64-setup.exe`、`dist/zmusic-android-release.apk`，并同步 `dist/zmusic-android-hd-release.apk`，Android APK SHA256 为 `6C958AAB5CCD76259A7FCFF9017EE73DB671D4F9CB1DF432E316F0C09C1A2379`。
- 2026-07-06：按“未限定端则跨端适配”规则同步调整移动端和桌面端首页：顶部 AppBar 统一显示“搜索 / 音乐 / 设置”页签，移动端去掉旧的 `Zmusic` 标题、右上音源状态和设置齿轮，中间内容区不再重复显示页签；音乐页快捷入口统一为“我喜欢的 / 我的歌单 / 电台”，移除“歌曲 / 专辑 / 歌手 / 公共歌单”等统计入口，“我的歌单”改为“我喜欢的”后面的计数入口并打开歌单分页，下面不再重复展示“我的歌单”区块；桌面设置页去掉“任务栏 / 托盘图标”上传设置。Android launcher 图标改为使用 `dist/zmusic-windows-x64/app_icon.ico` 重新生成 `mipmap-*` 传统图标、圆形图标和 adaptive foreground，保持手机图标和 Windows 包图标一致。新增/更新 widget 测试覆盖移动端顶部页签、移动端歌单入口、桌面设置去托盘图标和新页签点击方式。验证：`flutter analyze --no-pub` 通过，`flutter test --no-pub` 67 个测试通过；未打包。
- 2026-07-06：调整后续默认打包规则：用户只说“打包”时，默认产出 Windows zip、Windows 安装版，并追加 Android release APK；仍不默认打 iOS。验证：文档规则变更；未额外打包。
- 2026-07-06：Android 增加 `sw510dp` 适配边界，新增 `_androidHdWidthBreakpoint = 510`，低于 510dp 走窄手机布局，510dp 及以上的设置/音源/本地音源弹窗使用 HD/平板紧凑布局；播放详情和底部移动播放器仍保留 600dp 以下的移动体验。新增 widget 测试覆盖 510dp 下音源设置弹窗不使用手机专用关闭按钮、图标恢复 HD 尺寸，以及本地音源路径和“选择”按钮横向排列。验证：`flutter analyze --no-pub` 通过，`flutter test --no-pub` 64 个测试通过，`flutter build apk --release --no-pub` 通过；已打包 `dist/zmusic-android-release.apk`，并按本次手机/平板 HD 要求复制同内容为 `dist/zmusic-android-hd-release.apk`，两个 APK SHA256 均为 `4CAA9BDB7467873EEC85130F16920DE83C38FBDB8C3C999B3440A6CC6B3498C4`。
- 2026-07-06：按默认“打包”规则为移除本地歌单后的版本重新打包 Windows 绿色版和 Windows 安装版。使用 VS BuildTools 环境执行 `cmake --build build/windows/x64-ninja --config Release --target zmusic`，同步最新 `build/windows/app.so` 到 runner，覆盖 `dist/zmusic-windows-x64`，重建 `dist/zmusic-windows-x64.zip`，并用 C# 自解压安装器重新嵌入最新 zip 生成 `dist/zmusic-windows-x64-setup.exe`。验证：Windows Release CMake 构建通过，zip 内容检查 25 个条目且未包含 `userdata`、`zmusic.installed`、嵌套 zip 或安装包，绿色版 `dist/zmusic-windows-x64/zmusic.exe` 使用临时 APPDATA/LOCALAPPDATA 启动烟测并存活 5 秒；已打包 `dist/zmusic-windows-x64.zip` 和 `dist/zmusic-windows-x64-setup.exe`。
- 2026-07-06：移除本地歌单功能，删除 `LocalPlaylist` 模型、本地歌单持久化、导入导出 codec、设置页本地歌单管理、本地歌单详情页和歌曲右键“加入本地歌单”入口；保留 Navidrome/Subsonic 歌单、歌单编辑、播放队列和本地音乐文件夹功能。同步删除本地歌单导入导出/合并测试，并更新项目记忆。验证：`dart format` 通过，`flutter analyze --no-pub` 通过，`flutter test --no-pub` 62 个测试通过；未打包。
- 2026-07-06：修复 Windows 点击关闭按钮退出卡约 5 秒的问题。原因是关闭事件中同步等待托盘销毁和原生窗口销毁，Windows tray/native shutdown 偶发阻塞；现在直接退出模式会解除监听、后台清理托盘，给 `setPreventClose(false)` 和 `windowManager.destroy()` 各 250ms 上限，随后用 `exit(0)` 保证进程快速结束；最小化到托盘模式仍保持隐藏窗口不退出。验证：`flutter analyze --no-pub` 通过，`flutter test --no-pub` 65 个测试通过，Windows Release CMake 构建通过，zip 内容检查未包含 `userdata`、`zmusic.installed` 或嵌套包，安装目录临时数据强制 `closeButtonBehavior=exit` 后发送 `WM_CLOSE` 烟测 229ms 退出；已打包 `dist/zmusic-windows-x64.zip` 和 `dist/zmusic-windows-x64-setup.exe`。
- 2026-07-06：继续调整桌面端顶部与音乐页：Windows/macOS/Linux 宽屏下 AppBar 右侧不再显示音源状态块和独立设置图标，避免与音乐页音源卡片和顶部页签重复；桌面页签外层玻璃边框去掉，只保留“搜索 / 音乐 / 设置”按钮本身；音乐页音源卡片标题改为 `音源：xx`，副标题去掉 `Navidrome/Subsonic` / 本地类型文字，只保留歌曲数、扫描状态等必要信息。验证：`flutter analyze --no-pub` 通过，`flutter test --no-pub` 65 个测试通过；未打包。
- 2026-07-06：按默认“打包”规则重新打包 Windows 绿色版和 Windows 安装版，包含桌面端顶部页签和音乐页入口调整；同步最新 `app.so` 到 Windows runner，重建 `dist/zmusic-windows-x64` 和 `dist/zmusic-windows-x64.zip`，安装包使用 C# 自解压安装器重新嵌入最新 zip payload。验证：Windows Release CMake 构建通过，zip 内容检查未包含 `userdata`、`zmusic.installed` 或嵌套包，绿色版 `dist/zmusic-windows-x64/zmusic.exe` 启动烟测通过；已打包 `dist/zmusic-windows-x64.zip` 和 `dist/zmusic-windows-x64-setup.exe`。
- 2026-07-06：按桌面端需求调整首页布局：Windows/macOS/Linux 且宽屏时把“搜索 / 音乐 / 设置”页签移动到顶部 AppBar 中间，首页内容区不再重复显示页签，移动端和平板窄屏保持原页签位置；桌面端音乐页功能入口删去“歌曲 / 专辑 / 歌手 / 我的歌单 / 公共歌单”等统计型入口，只保留下面的歌手、专辑、歌单内容区以及“我喜欢的 / 电台”快捷入口。新增 widget 测试锁定桌面端页签位置和统计入口隐藏行为。验证：`flutter analyze --no-pub` 通过，`flutter test --no-pub` 65 个测试通过；未打包。
- 2026-07-03：完成 UI 大升级落地：主页改为搜索、音乐、设置三页签并默认打开音乐页签，顶部保留搜索组件、底部保留播放组件；搜索页签增加最新专辑、最近播放、最多播放、随机专辑；音乐页签增加音源概要、歌曲/我喜欢的/专辑/歌手/电台/我的歌单/公共歌单入口，并区分我的歌单和共享歌单；设置页签内嵌现有设置且不覆盖底部播放栏。Navidrome/Subsonic overview 增加最近/最多/随机专辑、收藏歌曲和电台读取，本地库补齐发现页专辑列表。Android 构建为受限网络环境补项目内 `build/local-maven` 依赖镜像、`dependencyResolutionManagement` 本地仓库配置，并将 `jni`/`jni_flutter` 插件源码复制到 workspace `.pub-cache` 以避开用户目录写入限制。验证：`flutter analyze --no-pub` 通过，`flutter test --no-pub` 64 个测试通过，Windows Release 构建通过并启动烟测通过，Android Release Gradle 离线构建通过；已打包 `dist/zmusic-windows-x64.zip`、`dist/zmusic-windows-x64-setup.exe`、`dist/zmusic-android-release.apk`。
- 2026-07-03：确认 UI 大升级采用统一三页签方案：搜索、音乐、设置，默认打开音乐页签，顶部保留搜索组件、底部保留播放组件，手机/平板/桌面共享同一信息架构但桌面保持更高信息密度；原有色调可保留。新增设计文档 `docs/superpowers/specs/2026-07-03-ui-redesign-design.md`，尚未修改应用代码。验证：文档变更，未运行 Flutter 测试；未打包。
- 2026-07-03：新增普通 Navidrome/Subsonic 歌单编辑能力，基于 Subsonic 兼容 API 增加 `createPlaylist`、`updatePlaylist`、`deletePlaylist` 调用并支持 `songIdToAdd`、`songIndexToRemove` 重复参数；歌单详情页增加播放全部、添加歌曲、编辑、删除，歌曲右键菜单支持从当前歌单移除；首页歌单区域在音源下显示“新建歌单”。设置页缓存目录和本地音源文件夹选择会默认打开当前已设置/已填写目录；非桌面平台设置页隐藏“窗口”和“任务栏 / 托盘图标”。音源设置弹窗在手机宽度下改为标题右侧关闭按钮、列表下方文本导入/导出/添加音源/添加本地按钮，小号测试/编辑/删除图标；手机播放栏改为无音量/无小封面的悬浮迷你条，点击进入独立手机播放详情页。验证：`flutter analyze` 通过，`flutter test` 59 个测试通过；未打包。
- 2026-07-03：修复 Android 安装后 launcher 图标不清晰/不同步问题，使用 `assets/branding/zmusic_note.webp` 的音符轮廓重新生成绿色圆底白音符的 `mipmap-*` 传统图标、圆形图标和 Android 8+ adaptive icon，并在 Manifest 增加 `roundIcon`；优化 Android/手机窄屏 UI，AppBar 音源状态在手机上使用短文本，设置页上传/选择/清除类操作在小屏改为纵向布局，音源设置弹窗使用响应式 inset/最大高度，手机上音源列表操作按钮下移，添加本地音源、添加音源、文本导入/导出、本地歌单和加入歌单等管理弹窗统一移动端约束；新增 320/390 宽度 widget 测试覆盖音源弹窗和本地音源路径选择布局。验证：`flutter analyze` 通过，`flutter test test/widget_test.dart` 54 个测试通过，Android `flutter build apk --release --no-pub` 通过；已刷新 `dist/zmusic-android-release.apk`。
- 2026-07-03：新增持久化封面缓存 `ArtworkCacheManager`，歌单、歌手/专辑卡片、歌曲列表、底部播放栏和播放详情页不再直接 `Image.network` 拉图，而是按去除 Subsonic/Navidrome 鉴权参数后的稳定 URL 指纹落到本地 `artwork` 缓存，避免同一封面因 token/salt 变化重复请求；音频边播边缓存开始时写入 `.part` 标记，只有完成后才写 `.complete` 并清除 `.part`，避免未完成缓存被误判为完整导致多首歌后卡在缓冲；Android 构建关闭 Kotlin 增量编译以规避 Windows 跨盘插件源码缓存错误。新增测试覆盖封面稳定缓存、音频 partial/complete 标记和既有缓存复用。验证：`flutter analyze` 通过，`flutter test` 52 个测试通过，Windows Release 构建通过，绿色版启动烟测通过，Android `flutter build apk --release --no-pub` 通过；已打包 `dist/zmusic-windows-x64.zip`、`dist/zmusic-windows-x64-setup.exe`、`dist/zmusic-android-release.apk`。
- 2026-07-03：顺序播放模式图标从两个并排 `arrow_forward` 图标改为自绘双层横向箭头，两个箭头上下排列并继承 `IconTheme` 的颜色/尺寸，使其和随机、循环等播放模式图标保持同一视觉风格；更新 widget 测试，要求顺序播放图标使用自绘 `CustomPaint` 且不再包含 `Icons.arrow_forward` 子图标。验证：`flutter analyze` 通过，`flutter test` 50 个测试通过；未打包。
- 2026-07-03：按默认规则重新打包 Windows 绿色 zip 和 Windows 安装包，包含 `Zmusic` 品牌显示名改动；安装包 payload 继续排除 `userdata`、`zmusic.installed` 和嵌套包。验证：`flutter analyze` 通过，`flutter test` 50 个测试通过，Windows Release 构建通过，绿色版启动烟测通过；产物为 `dist/zmusic-windows-x64.zip` 和 `dist/zmusic-windows-x64-setup.exe`。
- 2026-07-03：品牌显示名从 `Zmusic` 改为 `Zmusic`，覆盖 Flutter 页面标题、Windows 窗口标题/资源信息、托盘提示、Android/iOS 显示名、安装脚本显示名、导入错误提示、README 和项目记忆；保留 `zmusic` 包名、exe 名、Dart 内部类名和单实例 Mutex，避免扩大改动。新增 widget 测试锁定首页可见标题为 `Zmusic`。验证：`flutter analyze` 通过，`flutter test` 50 个测试通过；未打包。
- 2026-07-02：调整播放队列右侧抽屉位置，顶部下移到标题栏下方避免遮挡右上角音源/刷新/设置按钮，底部收紧到播放栏上沿附近以填满原有空隙；更新 widget 测试锁定抽屉上下边界。验证：`flutter analyze` 通过，`flutter test` 49 个测试通过，Windows Release 构建通过，绿色版启动烟测通过；已打包 `dist/zmusic-windows-x64.zip` 和 `dist/zmusic-windows-x64-setup.exe`，并清理旧嵌套打包目录，确认产物不携带 `userdata`、`zmusic.installed` 或嵌套包。
- 2026-07-02：搜索输入框在存在文本时于右侧搜索箭头前显示“清空搜索”按钮，点击后直接清空当前输入并隐藏清空按钮；新增 widget 测试覆盖按钮出现、清空文本和消失行为。验证：`flutter analyze` 通过，`flutter test` 49 个测试通过；未打包。
- 2026-07-02：播放队列改为右侧贴边抽屉显示，底部预留播放栏空间，避免弹窗居中或遮挡播放控制；顺序播放图标改为两个并排右箭头，并新增 widget 测试覆盖右侧队列面板和双箭头图标。验证：`flutter analyze` 通过，`flutter test` 48 个测试通过，Windows Release 构建通过，绿色版启动烟测通过；已打包 `dist/zmusic-windows-x64.zip` 和 `dist/zmusic-windows-x64-setup.exe`。
- 2026-07-02：调整音源设置弹窗：音源列表副标题去掉 `Navidrome/Subsonic` / 本地类型前缀，只保留 URL 或本地路径；底部按钮改为左侧关闭、右侧文本导入/文本导出/添加音源/添加本地，去掉音源配置的文件导入和文件导出。修复网络音频缓存复用：缓存文件名优先使用稳定的 `sourceServerId + sourceItemId`，避免 Navidrome/Subsonic 每次随机 token/salt 导致同一首歌重新缓存；玻璃容器内部补透明 `Material`，避免设置页 ListTile 水波纹断言。验证：`flutter analyze` 通过，`flutter test` 47 个测试通过；未打包。
- 2026-07-02：按默认规则重新打包 Windows 绿色 zip 和 Windows 安装包，包含顶部临时提示居中纯文本显示的最新代码；安装包 payload 继续排除 `userdata` 和 `zmusic.installed`。验证：`flutter analyze` 通过，`flutter test` 45 个测试通过，Windows Release 构建通过，绿色版启动烟测通过；产物为 `dist/zmusic-windows-x64.zip` 和 `dist/zmusic-windows-x64-setup.exe`。
- 2026-07-02：调整顶部临时提示显示位置：提示改为绘制在标题栏中间的纯文本，不再放入右上角音源状态框，也不再使用玻璃边框，避免 3 秒隐藏时挤动音源信息位置；新增 widget 测试锁定右上角音源摘要坐标稳定。验证：`flutter analyze` 通过，`flutter test` 45 个测试通过；未打包。
- 2026-07-02：补充默认打包规则：后续用户只说“打包”时，默认产出 Windows zip 版并在同级目录解压一份和 Windows 安装版，不默认打 Android/iOS。验证：文档变更，未运行 Flutter 测试；未打包。
- 2026-07-02：创建本文件，压缩记录本会话中关于 Zmusic 的产品需求、编码规则、缓存/播放行为、导入导出加密规则、打包方式和后续维护约定。验证：文档变更，未运行 Flutter 测试；未打包。
